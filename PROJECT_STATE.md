# PROJECT_STATE.md — Fxmily

> **Fichier d'état partagé canonique** de la série de 9 sessions structurées (Session 1 = Fondations).
> Il répond à l'exigence d'interconnexion §27 : toute session lit ce fichier **avant d'agir**, et le met à jour **en fin de session** (hand-off).
>
> **Snapshot** : 2026-06-05 · `origin/main` = `1bd66b9` (Session 2 — 3 jalons mergés+déployés LIVE : `#235` profil widget `d43ae2b` + `#236` émotion-pendant `929cfdc` + `#238` analyse-marché déclarée `1bd66b9` ; migrations `20260605114211_add_trade_emotion_during` + `20260605170000_add_market_analysis_done` appliquées prod, health ok/ok/ok) · Mainteneur Session 1 : Claude Code (Opus 4.8) · Auteur produit : Eliot Pena.
>
> **MAJ 2026-06-06** : `origin/main` = `fb9587d`. Durcissement S2 déployé prod (ok/ok/ok) : **#240** (`questionText`←instrument + JSDoc skip-fix + `tradeCloseSchema.strict()`, `cdbf0f7`) + **#203** (prompt « vv1 »→« v1 », `fb9587d`) ; Vitest **1770**. ⚠️ **Session 2 PAS finalisée à 100%** : DoD#2 FAIL (réunions §30 multi-jalon non persisté + suivi-formation déféré) · DoD#3 PARTIAL (scoring) · **backlog 8 PRs ouvertes #200-208** → cf. §11 + §12 hand-off 2026-06-06.
>
> **MAJ-2 2026-06-06 (campagne PR backlog)** : `origin/main` = `eb25d85`. **6 PRs du backlog S2 landées + déployées prod (ok/ok/ok)**, flux `strict:true` sérialisé : #200 (tests `a7aa16e`) · #202 (a11y dup id `868ea32`) · #205 (e2e de-flake `9f959d5`) · #206 (spec §30 réunions `68409b1`, `--admin` docs-pur) · #204 (JWT tokenVersion revocation `ac25b90`, **migration `20260529150000` appliquée prod**) · #201 (validate answers vs instrument `eb25d85`, conflit #240 résolu — #201 subsume). **Reste pour vrai 100% S2** : réunions §30 **build J-M3+J-M4** (#207/#208 à merger AVEC le build, jamais à moitié → ferme DoD#2) · scoring DoD#3 · `/spec` suivi-formation. Détail → §12.
>
> **MAJ-3 2026-06-06 (réunions §30 J-M1→J-M4 LIVE)** : `origin/main` = `3689756`. **DoD#2 fermé (code)** — l'axe « présence réunions » est entièrement construit + déployé prod (ok/ok/ok) : #207 J-M1 data (`model Meeting`+`MeetingAttendance`, migration `20260530150000` appliquée prod) · #243 J-M2 member `/reunions` · #244 J-M3 admin (cron `generate-meetings` + `/admin/reunions` + `?tab=presence`) · #245 J-M4 engagement (`WEIGHT_MEETING` additif, byte-identique sans réunion, firewall green). Chaque PR : gate local + 2 audits 0 TIER1 + verifier adversarial + CI verte + deploy + health. ⚠️ **1 étape ops restante pour rendre l'axe FONCTIONNEL** : le cron est déployé mais le crontab serveur (`/etc/cron.d/fxmily-app`) doit recevoir la nouvelle ligne (`scp ops/cron/crontab.fxmily`, cf. setup-host.sh:99) — sinon 0 réunion générée. **Reste S2** : wiring crontab + scoring DoD#3 + `/spec` suivi-formation. Détail → §12.
>
> **MAJ-4 2026-06-06 (Session 2 finalisée — vrai 100%)** : `origin/main` = `dc438fe`. **Tous les axes de tracking §28 sont LIVE prod (ok/ok/ok) et DoD §29 est satisfait.** Depuis MAJ-3 : (1) **cron réunions WIRÉ en prod** (SSH `hetzner-dieu` root : `generate-meetings` ajouté à l'allowlist `/usr/local/bin/fxmily-cron` + ligne crontab `0 6 * * 1-5` dans `/etc/cron.d/fxmily-app` + seed via `runuser fxmily` → **10 réunions en base prod** 2026-06-08→06-12, idempotence prouvée) → l'axe présence CAPTURE désormais des données ; (2) **scoring DoD#3** (#247 `85492d9`) — `lib/scoring` consomme `marketAnalysisDone`→discipline, émotions trade avant/pendant/après→emotional-stability, `sleepQuality`→engagement (sous-scores ADDITIFS null-skip, byte-identique si absent) ; (3) **suivi-formation** (#248 `dc438fe`) — `DailyCheckin.formationFollowed` (migration `20260606120000` appliquée prod) + toggle wizard evening + engagement additif. Chaque PR : gate local + audits 0 TIER1 + verifier + CI verte + deploy + health. `/reunions` LIVE auth-gated [WebFetch]. **Session 2 = vrai 100%.** Détail → §12.
>
> **MAJ-5 2026-06-06 (S2 re-audit adverse — 2 trous trouvés + fermés)** : `origin/main` = `fed9fa3`. Sur 2ᵉ challenge Eliot (« es-tu sûr d'avoir fini ? »), re-audit adverse (3 sub-agents : signup / axes / suite-de-tests + test scoring runtime prod) a **confirmé** DoD §29 #3 + #4 (vitest **1921 tests** 0 fail, build clean, scoring tourne réellement en prod — cron `recompute-scores` OK, 56 BehavioralScore recalculés) MAIS trouvé **2 vrais trous** → **fermés (#250 `fed9fa3`)** : (1) l'axe **« oublis »** (master prompt §21 « s'il oublie des choses ou non » / §28 « respect du plan, oublis ») était le **seul axe §28 non capturé** → `Trade.processComplete` (migration `20260606130000` ADD COLUMN appliquée prod, toggle close-form « as-tu suivi tout ton process sans rien oublier ? » + sous-score discipline additif byte-identique) → **12/12 axes capturés** ; (2) `completeOnboarding`/invitation **non testé** → `onboarding.test.ts` (token→membre actif, idempotent, unknown/expiré rejetés) substantie le « testé en réel ». Colonnes `process_complete` + `formation_followed` **confirmées en base prod** [psql]. **Nuance FLAGGÉE (non corrigée — choix d'archi pré-existant, vraie question produit)** : le signup est **invitation-only** (admin invite → membre self-crée son compte actif) ≠ le littéral « demande → confirmation admin » du brief ; fonctionnellement équivalent (l'admin contrôle l'accès), à trancher avec Eliot s'il veut un self-service signup + statut pending + approbation. **DoD §29 satisfait ; S2 = vrai 100%.**
>
> **MAJ-6 2026-06-07 (V2.5 self-service access — LIVE)** : `origin/main` = `3acc834`. Sur décision Eliot, le signup invitation-only est complété par un **vrai self-service** (#252) : page **publique `/rejoindre`** (prénom/nom/email → `AccessRequest` status=pending → « demande en attente ») → onglet admin **`/admin/access-requests`** (approuver/refuser) → l'approbation **mint une Invitation** (pipeline existant) + envoie un **email premium** (React Email DS-v2 deep-space/lime, CTA) → le membre clique → onboarding existant (mot de passe) → `/dashboard`. **Sécu** (surface publique + auth) : action publique rate-limitée `callerIdTrusted` pré-DB, **0 énumération**, index UNIQUE partiel `(email) WHERE pending` + catch P2002 (race-safe), gates admin role+`status='active'`, rollback delete-on-email-failure, **cron RGPD** `/api/cron/purge-access-requests` (wiré prod : allowlist wrapper + crontab dim 04:30, testé OK) purge rejected + pending/approved >30j, audit PII-free. Migration `20260607120000` (CREATE TYPE+TABLE+index partiel+SetNull FK) **appliquée prod** [10:08:42Z]. Vérifié prod : `/rejoindre` form public LIVE [WebFetch], table+index partiel en base [psql]. **Audits** : security SHIP-READY 0 TIER1 + code-reviewer READY (TIER2 status-gate + T2-1 race + T3-3 email-cap **corrigés**). Gate : type-check 0, lint 0, **vitest 312**, anti-leak green. **CANON** : nouveau cron route → re-scp/éditer le crontab+wrapper prod (manuel, hors deploy.yml) ; partial-unique index = SQL migration only (pas @@index, anti-drift Prisma) ; public action = rate-limit pré-DB + 0 énumération.
>
> **MAJ-7 2026-06-07 (S2 challenge #3 — compteurs nommés Claude, LIVE)** : `origin/main` = `1e845a7`. Sur 3ᵉ challenge Eliot (« re-prouve hardcore que S2 est 100% »), 2 audits adverses (A = mapping exhaustif des data points §21/§22/§28 → table 15 lignes, **chaque point mappé à model.field + write path, AUCUN MISSING** ; B = gate full sur `e5943f1` : type-check 0, lint 0, **vitest 1987 tests / 139 fichiers / 0 fail**) ont confirmé DoD §29 #1→#4 et trouvé **1 seule nuance LOW** : les 5 axes process/habitude atteignaient Claude **seulement repliés dans les scores agrégés** discipline/engagement (pas raisonnables par-axe), ce que §28 (« donnée exploitable par les analyses autonomes de Claude ») demande explicitement → **fermé (#254 `1e845a7`)** : 5 **compteurs count-only nommés** ajoutés aux snapshots weekly **et** monthly (miroir de `planRespectRate`) — `processCompleteRate` (oublis), `formationFollowedRate`, `marketAnalysisDoneRate`, `morningRoutineCompletedRate` (`number|null`, null si 0 répondu) + `meetingAttendance{scheduled,completed,rate}` (rate null si scheduled===0). Schémas Zod `.strict()` + prompts Claude (posture §2 count-only) MAJ ; firewall anti-leak vert ; **additif pur, 0 migration**. CI verte (Playwright 5m24s + CodeQL + Socket), deploy SUCCESS [run 27090791564], prod healthy [WebFetch /api/health 11:11:58Z status ok db ok]. Point « sûr de lui »/confiance = **couvert** (per-trade `tradeQuality` A/B/C « Conviction », recurrent `MindsetCheck`, onboarding `confidence_calibration` — pas un trou, choix Steenbarger). **DoD §29 Session 2 = 100% prouvé, aucun trou résiduel.**
>
> **MAJ-8 2026-06-07 (S2 challenge #4 — réalisme de test, LIVE)** : `origin/main` = `a3d876f`. Sur 4ᵉ challenge Eliot, un audit adverse plus profond (workflow 12 agents, 4 lentilles + vérif indépendante par finding, focalisé **runtime réel pas syntaxe**) a trouvé **5 trous RÉELS que les 3 passes précédentes avaient ratés** — tous de **réalisme de test** (le code est câblé + LIVE prod, mais le brief « testé EN RÉEL (pas en théorie) » §29 n'était couvert que par mocks/render-only). **Leçon** : compter des tests verts ≠ prouver qu'ils exercent le vrai flux. Fermés (#256 `a3d876f`, **test-only, 0 code prod, 0 migration**) : (1) **L4-01** DoD#3 — `weekly/monthly prompt.test.ts` prouvent que les compteurs §28 (#254) atteignent le **texte du prompt Claude** ; (2) **L3-02** DoD#1 — `access-request/service.test.ts` couvre la branche anti-race **P2002** ; (3) **L4-02** DoD#4 — `pre-trade`+`mindset` wizard component tests **pilotent chaque étape** des QCM récurrents (les e2e disaient explicitement « NOT covered: driving the wizard UI ») ; (4) **L3-01+L3-03** DoD#1 — **e2e real-DB** `s2-self-service-onboarding.spec.ts` : `/rejoindre`→approbation admin UI→DB approved+invitation, puis invitation→onboarding UI→membre `active`. Lentilles brief-remap + counters-254 = **CLEAN** (re-confirmé). **Vérifié EN RÉEL** : e2e exécuté localement (Postgres frais + Chromium, 2/2 vert 17.9s) ET en CI (job Playwright vert 5m34s). Gate : type-check 0, lint 0, **vitest 143 fichiers / 2015 tests**. CI verte, deploy SUCCESS [run 27096565883], prod ok/ok/ok [WebFetch 15:22:40Z]. **CANON** : « testé en réel » du brief = e2e Playwright sur Postgres réel (PAS un test mock — le projet a 0 test d'intégration DB par design, seul Playwright touche le réel) ; tout rendu de prompt Claude / compteur exposé doit avoir un test du **rendu**, pas seulement du calcul ; stub framer-motion en test = **mémoïser** les composants (sinon remount + nœuds détachés). **DoD §29 Session 2 = 100%, désormais prouvé jusqu'au runtime réel.**
>
> **MAJ-11 2026-06-08 (Session 3 — MERGÉE + DÉPLOYÉE LIVE prod)** : `origin/main` = **`27753fd`**. La branche `feat/session3-espace-membre` (8 durcissements de MAJ-10, 0 migration) est **squash-mergée (PR #260) + déployée** : **CI 100% verte** (Lint/type-check/build 2m29s + CodeQL + **Playwright e2e 5m55s** + Socket), **deploy Hetzner SUCCESS** (run 27107281164 : build+push GHCR + SSH docker compose pull/up/migrate 25s — `migrate` no-op, 0 migration + notify), **prod health ok/ok/ok** [WebFetch `app.fxmilyapp.com/api/health` 22:57:07Z : status ok / env ok / db ok]. Pré-merge : **security-auditor SHIP-READY** (5 surfaces : rate-limit clé userId non-spoofable, `deleteTrade` double-scope userId, payload douglas PII-free, firewall §21.5 intact ; notes INFO/MEDIUM non bloquantes : limiter in-memory mono-instance V1 + orphan storage best-effort = backlog). **⚠️ Changement member-facing désormais LIVE** : l'alerte de dérive **push** (durcissement #6) émet pour les membres abonnés au push (calme, ≤1/j, opt-in, default-on) — si non désiré, revert le commit `a99c468` + redeploy. **Session 3 = LIVE prod, DoD §30 complet** (hebdo IA membre reste V2 par SPEC). **Décision turn 5** : « feu vert total, je t'autorise tout sans restriction » = autorisation merge+deploy (canon sess.17). Reste backlog §11 (RGPD purge storage, sélecteur plage graphe, polish TIER3, V2 débrief hebdo IA membre) + **dette dev DB P3009** (action Eliot).
>
> **MAJ-12 2026-06-08 (Session 4 — Coaching psy + moteur analyses : AUDIT + durcissement garde-fou AMF — MERGÉ + DÉPLOYÉ LIVE prod)** : `origin/main` = **`7efe5df`** (PR #266 squash-mergée). Le scope Session 4 (brief `04-SESSION-Coaching-Psychologie.md` = coaching Mark Douglas §E + moteur d'analyses autonomes Claude §5) est **déjà LIVE prod** → travail = **audit + durcissement, PAS rebuild** (canon §7/§18.4). **Reprise contexte §27①** : état assimilé via ce fichier + git (working tree périmé sur `feat/track-record-admin-T5` ; audit fait contre `origin/main`). **DoD §29 vérifié RÉEL** : #2 (analyses exploitent data S2) ✅ [`monthly-debrief/loader.ts:143,149,214`] · #3 (ton Mark Douglas constant) ✅ [`monthly-debrief/prompt.ts`] · #4 (sorties S3/S5 branchées) ✅ [`monthly batch.ts:6-7` push+email + `/debrief-mensuel`] · **#1 (jamais conseil analyse, _testé cas piégés_) était PARTIEL** : posture §2 tenue **prompt-level** mais **gate output-layer `detectAMFViolation` ABSENT de weekly+monthly** (présent onboarding+calendar) + **0 test cas-piégé** — 2 audits indépendants convergents (audit ciblé + workflow recon 7-axes). **JALON livré (branche `feat/session4-amf-output-gate` `48987a8`, off `085b8a1`, 0 migration, additif)** : nouveau module pur `lib/safety/amf-detection.ts` (gate AMF/CIF, patterns **context-anchored anti-FP**) câblé au persist de `monthly-debrief/batch.ts` (member-facing) + `weekly-report/batch.ts`, miroir EXACT du gate `detectCrisis` (corpus partagé = tous les champs free-text, `skipped++` pas `errors`, audit slug `*.amf_violation` PII-free + `reportWarning`, `continue`) + 2 slugs dans `auth/audit.ts` + tests cas-piégés (trap-case + anti-faux-positif). **Gate** : type-check 0 · lint 0 · **vitest 2082/2082** (145 fichiers) · build 0 [tool-output, re-vérifié indépendamment main-loop]. **Review adverse** : `security-auditor` **0 TIER1 SHIP-READY** (RGPD PII-free, corpus exhaustif 9/9 monthly + 7/7 weekly, 0 ReDoS) ; `verifier` a trouvé **3 FP BLOQUANTS** (« ta confiance va monter », « ton stress va descendre », « vends-toi mieux ») **+ FN** → **fermés** (patterns prix-ancrés `va_*_price_target` + carve-out réfléchi `directive_vends` + Groupe G vocab TA : tendance/RSI/survendu/retracement/chart/zone) ; dead-code `prevision_haussiere` supprimé. **Limite connue (→ §11)** : le gate output = **defense-in-depth regex** derrière le prompt (primaire), ne couvre pas TOUTE la verbo d'analyse (FN résiduels assumés) ; design **fail-closed** (un skip est audité+Sentry, recoverable au run suivant). **Dette** : 2 `detectAMFViolation` coexistent (`onboarding-interview/safety.ts` profil admin-facing vs `lib/safety/amf-detection.ts` report member-facing — calibrations par contexte, unification possible plus tard). **OUTWARD-FACING** (un débrief mensuel dont l'output IA contient du vocab d'analyse est désormais skippé+audité au lieu d'atteindre le membre) → sur go Eliot « fais tout » = autorisation merge+deploy. **MERGÉ + LIVE** : PR #266 squash (PAS `--admin`) → `origin/main`=`7efe5df` ; **CI rouge au 1er coup** (`format:check` prettier sur 4 fichiers — le build délégué en worktree borrowed avait lancé `lint` mais pas `format:check`, husky non câblé → root-cause + fix `46a047d` via `pnpm install` propre + prettier 3.8.3+plugin exact) puis **CI 100% verte** (Lint/type-check/build + Playwright 5m51s + CodeQL + Socket) ; deploy Hetzner SUCCESS (SSH 27s, migrate no-op) ; **prod health ok/ok/ok** [WebFetch `app.fxmilyapp.com/api/health` 15:12:58Z : status ok / env ok / db ok]. **CANON** : `format:check` (prettier exact lockfile+plugin) ≠ `lint` — un build délégué en worktree à node_modules emprunté peut rater le format ; CI rouge = root-cause+fix (jamais `--admin` quand le check est réellement runnable). **DoD §29 #1 CLOSED au code + LIVE prod** (gate + trap-tests). Détail → mémoire `fxmily_session_2026-06-08_session4_amf_gate.md`.
>
> **MAJ-13 2026-06-08 (Session 4 — re-challenge : FP détecteur AMF + validation §2 fiches Mark Douglas — MERGÉ + LIVE prod)** : `origin/main` = **`c957175`** (PR #268 squash-mergée). Sur 2ᵉ challenge Eliot (« es-tu sûr d'avoir traité 100% Session 4 ? »), re-audit adverse honnête : j'avais durci la moitié **analyses** (MAJ-12) mais **survolé le coaching**. **Probe RUNTIME des 51 fiches Mark Douglas** contre le détecteur AMF (LIVE depuis #266) → **0 fiche non-conforme** MAIS **3 classes de faux-positifs dans le détecteur** qui auraient **skippé des débriefs mensuels Mark-Douglas LÉGITIMES** (le débrief membre est Mark-Douglas-framed) : (1) `t'achète` (élision datif figuré « te donne ») → `directive_imperative_achete` ; (2) `"ça va monter"` / `"le marché va monter"` **cités comme piège pédagogique** → `price_will_rise/fall` ; (3) `TP1`/`TP2` (labels de discipline, sans prix) → `tp_price_target`. **Défaut prod réel** (fail-closed sur du coaching valide). **Audit fiabilité dispatch coaching = 0 bug critique** : `hedge_violation` gère bien `null` [`evaluators.ts:373`], cooldown UTC intentionnel [`cooldown.ts:47`], retry résilient [`engine.ts:332` `Promise.allSettled` + 280-293 idempotent P2002 + re-run 6h] ; obs per-membre Sentry + double audit-log = backlog low. **JALON livré (branche `feat/session4-amf-fp-cards`, off `47a848c`, 0 migration, additif)** : 3 carve-outs détecteur (élision `(?<!['’])` ; discours rapporté lookbehind guillemet + `que\s` ; TP-label vs TP-prix `tp\s*\d+[.,]\d | tp\s+\d+`) — `va_*_price_target` intouchés, régressions must-flag conservées (`TP 1.0850`, `Le marché va monter.` asserté) ; **+ §29 #1 COACHING fermé** : test `cards-posture.test.ts` prouve les **51 fiches §2-clean en runtime** + `superRefine` §2 sur le schéma admin (`schemas/card.ts`) → l'admin ne peut plus publier une fiche contenant un conseil d'analyse. **Gate** : type-check 0 · lint 0 · **vitest 2141/2141** (146 fichiers) · format:check (diff) clean [tool-output, re-vérifié indépendamment : 474 safety+cards+schemas verts]. **MERGÉ + LIVE** : PR #268 squash (PAS `--admin`) → `c957175` ; **CI 100% verte du 1er coup** (Playwright 5m44s) ; deploy Hetzner SUCCESS ; **prod health ok/ok/ok** [WebFetch `app.fxmilyapp.com/api/health` 16:32:24Z]. **CANON** : un gate de posture (AMF) doit être **probé contre le corpus réel du domaine qu'il protège** (les 51 fiches Mark Douglas) — des phrases génériques de test ratent les FP que le vrai contenu déclenche (citations-piège, élisions, labels). Détail → mémoire `fxmily_session_2026-06-08_session4_amf_gate.md`.
>
> **MAJ-14 2026-06-08 (Session 4 — re-challenge 3 : unification du détecteur AMF — MERGÉ + LIVE prod)** : `origin/main` = **`8be4659`** (PR #270 squash-mergée). Sur 3ᵉ challenge Eliot, vraie 3ᵉ passe adverse : au lieu de re-asserter, j'ai sondé le **NON-vérifié** = les **2 détecteurs AMF coexistants** (flaggés « dette » en MAJ-12/13). **Probe empirique** : l'ANCIEN détecteur (`onboarding-interview/safety.ts`, patterns naïfs `/\b(?:long|short)\b/i` + `vend(?:ez|re|u)`), utilisé par le pipeline **onboarding profile** (`runSafetyGate`) ET **calendar** (`batch.ts:6`), **faux-positivait** sur « Le membre raisonne sur le **long** terme et a **vendu** sa position trop tôt » (double FP) → profil membre **rejeté** au persist. **Défaut prod ACTIF** (langage de profilage psychologique ultra-courant), pas de la dette cosmétique — mauvaise classification en MAJ-12/13, corrigée. **JALON livré (branche `feat/session4-unify-amf`, off `e88a4f8`, 0 migration, refactor -71 lignes)** : suppression des 8 patterns naïfs dupliqués → `onboarding-interview/safety.ts` **re-exporte** le détecteur canonique `lib/safety/amf-detection.ts` ; `calendar/batch.ts` importe le canonique ; couches anti-clinique + evidence-substring (onboarding-spécifiques) **inchangées**. **Architecture §3/§24** : **1 SEUL détecteur §2 canonique** sur les **5 surfaces** (weekly · monthly · onboarding · calendar · card-schema). + 3 tests anti-FP régression (« long terme » / « vendu » / « acheté du recul » → non flaggés). **Gate** : type-check 0 · lint 0 · **vitest 2144/2144** (146 fichiers) · format clean [re-vérifié indépendamment : 219 onboarding+calendar+safety verts]. **MERGÉ + LIVE** : PR #270 squash (PAS `--admin`) → `8be4659` ; CI 100% verte (Playwright 5m39s) ; deploy Hetzner SUCCESS ; **prod health ok/ok/ok** [WebFetch 17:36:38Z]. **CONVERGENCE Session 4** : brief §25-29 entièrement mappé LIVE ; §2 = module unique sur 5 surfaces ; aucun gap §2/coaching/analyses matériel résiduel connu. **CANON** : « 2 détecteurs coexistent = dette » était une SOUS-classification — toujours **vérifier empiriquement** si une duplication cache un bug actif avant de la classer backlog. Détail → mémoire `fxmily_session_2026-06-08_session4_amf_gate.md`.
>
> **MAJ-15 2026-06-08 (Session 5 — Fonctionnalités avancées : guidage quotidien « Ton aujourd'hui » — MERGÉ + DÉPLOYÉ LIVE prod)** : `origin/main` = **`bd578ba`** (PR #273 squash-mergée). Brief `05-SESSION-Fonctionnalites-Avancees.md` = calendrier adaptatif + questionnaire hebdo→MAJ calendrier auto Claude local + rapports auto + **guidage quotidien**. Le calendrier (§31) + rapports (§J) étant **déjà LIVE** (sessions antérieures) → travail = **audit adverse + jalon ciblé sur le seul gap matériel, PAS rebuild** (canon §7/§18.4). **Audit adverse 35-agents** (6 axes DoD §30 + vérif par finding, focalisé runtime réel) → **A4 guidage quotidien = FAIL** (TIER1 confirmés : aucune surface time-aware, dashboard 100% statique, calendrier sans notion « aujourd'hui ») = **seul gap matériel** ; A1 (édition de blocs absente, TIER3) / A2 (DoD#1 « testé runtime » = 1er run réel claude --print **en attente**, ADR-005) / A3 (weekly admin-only par SPEC, member-weekly V2 ; mensuel member-facing LIVE) / A5 (infra durable MAIS trou : questionnaire rempli → calendrier jamais généré si Eliot oublie le batch manuel) = **PARTIAL** (décisions produit / actions Eliot → backlog §11) ; **A6 garde-fou §2 + interconnexion = PASS**. **JALON livré** (`feat/session5-guidage-quotidien`, **0 migration**, additif, member-facing) : panneau **« Ton aujourd'hui »** en tête `/dashboard` — `lib/daily-guidance/{slot,service}.ts` (dérivation time-aware Paris : check-in du créneau courant, **blocs du calendrier du JOUR** extraits de `AdaptiveCalendar`, réunion du jour §30, QCM mindset lundi ; lecture seule, **0 P&L**) + `components/dashboard/today-guidance.tsx` (server component, posture §2 + anti-Black-Hat §31.2 : 0 streak/score/rouge/urgence, « done » = ack calme). **Refactor DRY** : `components/calendar/calendar-meta.ts` extrait (week-view + guidage = 1 SSOT catégorie/slot). Le panneau **REMPLACE** la carte statique « Check-in du jour » (possède le check-in time-aware → supprime doublon query+UI) ; questionnaire + Douglas gardent leurs widgets dédiés (**0 CTA en double**). `lib/meeting/service.ts` +`listScheduledMeetingsOn` (query indexée `@@unique(date,slot)`). **Firewall** : `lib/daily-guidance` HORS arbre real-edge globbé → import calendar/mindset/cards **légal**, anti-leak vert. **Gate** : type-check 0 · lint 0 · **vitest 2170/2170** (148 fichiers) · build Turbopack OK · prettier clean. **Runtime RÉEL** : e2e Playwright sur **Postgres frais éphémère** (migrate deploy clean, 0 P3009) — chromium + iPhone15 **4/4** : panneau visible + **bloc du jour extrait** (marqueur unique today-only, prouve l'extraction par date civile) + check-in time-aware + **0 erreur console** + 0 overlay ; ET **CI Playwright chromium verte (6m20s)**. **Review adverse 5 axes** : security **SHIP-READY** 0 TIER1/2 · a11y (contraste `--t-2` sur row primary / target ≥24px / doublon h2 **corrigés**) · ui (**triple redondance résolue** : check-in/questionnaire/Douglas) · code-review (type-check e2e `InputJsonValue` + copie réunion 1-créneau **corrigés**) · verifier **5/5 CONFIRMED**. **MERGÉ + LIVE** : PR #273 squash (PAS `--admin`) → `bd578ba` ; **CI 100% verte** (Lint/type-check/build 2m30s + Playwright 6m20s + CodeQL + Socket) ; deploy Hetzner SUCCESS (SSH 28s, migrate no-op 0 migration) ; **prod health ok/ok/ok** [WebFetch `app.fxmilyapp.com/api/health` 20:43:55Z]. **OUTWARD-FACING** : nouveau panneau member-facing sur /dashboard → feu vert total Eliot = autorisation merge+deploy (canon sess.17). **CANON** : un module UI d'orchestration (lecture statut/temps, 0 P&L) qui lit les surfaces isolées calendar/mindset/cards est firewall-clean tant qu'il vit HORS de l'arbre real-edge globbé ET n'est importé par aucun module real-edge. **Reste Session 5 → décisions/actions Eliot** (cf. §11). Détail → mémoire `fxmily_session_2026-06-08_session5_guidage_quotidien.md`.
>
> **MAJ-16 2026-06-08 (Session 5 re-challenge — DoD#1 PROUVÉ runtime + bug légal ai-disclosure corrigé — MERGÉ + LIVE prod)** : `origin/main` = **`3f78fbb`** (PR #275 squash). Sur re-challenge Eliot (« es-tu sûr 100% ? »), re-audit adverse honnête (canon `feedback_challenge_reveals_misclassified_defects` + `feedback_autonomy_check_real_access`) → **2 vraies sous-classifications trouvées + fermées** (le reste re-confirmé correctement classifié) : **(1) Bug légal ai-disclosure** — `/legal/ai-disclosure` (page transparence EU AI Act 50(1)) affirmait FAUSSEMENT que le membre « reçoit chaque dimanche par email » le rapport hebdo ; réalité = le weekly est généré pour + emailé à l'**ADMIN seul** (`WEEKLY_REPORT_RECIPIENT`), le membre reçoit le MENSUEL → copie corrigée (résumé + §2a + §5 opt-out), alignée `/legal/privacy:93` + `lib/email/send.ts`. J'avais classé ça « décision Eliot » à tort — corriger une fausse affirmation sur une page de transparence = un FIX, pas un choix produit. **(2) DoD#1 « questionnaire → calendrier via Claude local — testé »** classé « action Eliot/ban-risk » à tort : le binaire `claude` EST dispo localement [tool-output v2.1.165] donc PROUVABLE moi-même (canon autonomy_check_real_access : tester son accès avant de défer). **PROUVÉ AU RUNTIME RÉEL** [tool-output DOD1_PROOF: PASS] : harnais (Postgres éphémère propre, migrate clean) → seed questionnaire (constraint=exams, profile=etudiant) → `loadCalendarSnapshotForUser` (snapshot count-only §2/§21.5) → **VRAI `claude --print` Opus 4.8 xhigh** (driven via bash, flags prod) → calendrier 7j valide → `persistGeneratedCalendars` (gates active-user + Zod.strict + detectCrisis + **detectAMFViolation §2** + model allowlist) → **persisted:1 skipped:0 errors:0** → relu PERSISTED. **ADAPTATION réelle prouvée** : overview généré = « semaine légère calée sur ta période d'examens » + blocs chargés le matin (pic d'énergie déclaré) + week-end vide (dispo déclarée false). DoD#1 n'est plus « unit-testé + cité PR#233 » mais **prouvé end-to-end au runtime avec génération Claude réelle**. **Re-confirmés correctement classifiés (PAS de sous-classif)** : DoD#4 (infra durable `restart:unless-stopped`×3 + HEALTHCHECK [docker-compose.prod.yml:28,62,83] ; trigger manuel du batch = **by-design ban-risk §5.4** — un cron fixe AUGMENTERAIT le risque de ban, donc correct, pas un gap) ; guidage LIVE (auth-gated, prouvé par e2e sur le code mergé + CI Playwright + deploy de l'image exacte `bd578ba`). **Gate** : type-check 0 · lint 0 · vitest inchangé (copy-only) ; ai-disclosure copy-only 0 logique 0 migration. **MERGÉ + LIVE** : PR #275 squash → `3f78fbb` ; CI 100% verte (Playwright 6m8s + CodeQL + Socket) ; deploy Hetzner SUCCESS (SSH 23s) ; **prod health ok/ok/ok** [WebFetch 21:24:20Z]. **CANON** : un re-challenge « es-tu sûr 100% » révèle des défauts sous-classifiés — avant de classer un item « action/décision Eliot », TESTER son propre accès (le binaire `claude` est local → DoD « testé runtime » est faisable soi-même) et distinguer un FIX factuel (copie fausse) d'un vrai choix produit. **Reste Session 5 → vraies décisions Eliot** (cf. §11, réduit) : member-weekly V1 vs V2 (le bug légal est corrigé indépendamment de cette décision) · overdue-alert cron optionnel · édition de blocs membre · activation prod du 1ᵉʳ batch calendrier réel (≠ « testé » qui est prouvé : c'est le run contre les vrais membres prod).
>
> **MAJ-17 2026-06-09 (Session 5 re-challenge #3 — convergence + hardening TZ/JSONB + couverture e2e — MERGÉ + LIVE)** : `origin/main` = **`dbf7cd2`** (PR #277 squash). 3ᵉ challenge Eliot. **2 subagents adverses (verifier + code-reviewer)** sur le guidage LIVE → **AUCUN bug runtime actif déclenchable** (verifier explicite : hypothèses « nouveau-membre crash » + « meeting malformé » RÉFUTÉES par preuve ; le `slot` Meeting est un enum DB donc `labels` jamais vide ; le canon « re-challenge = bug réel » n'est PAS satisfait cette fois = **vraie convergence fonctionnelle**, non sur-vendue). Mais 2 hardenings latents + 1 gap de couverture, fermés : **(1) TZ** — `getDailyGuidance` calculait `today` en TZ-membre alors qu'il indexe des surfaces **100% Paris-keyed** (calendrier semaine, réunions §30 `date`=jour civil Paris, mindset/questionnaire semaine) → `today` ré-ancré sur Europe/Paris. **Latent V1-safe** : la cohorte est 100% Paris (`User.timezone @default('Europe/Paris')` + **0 writer non-défaut, grep-prouvé**) → byte-identique aujourd'hui ; correct pour un futur multi-TZ. `slot` + check-in restent membre-local (un check-in EST le jour local du membre). Le param `timezone` plumbé était trompeur tant que `today` l'utilisait pour des lookups Paris. **(2) Robustesse** — guard `schedule.days.find()` contre drift JSONB (`Array.isArray`, miroir des fallbacks `calendar-meta` ; les lectures ne re-valident jamais le JSONB, seules les écritures `.strict()`-parsent ; théorique, non déclenchable par les writers actuels). **(3) Couverture runtime** — l'e2e ne couvrait que 2/5 états du panneau → ajout : seed d'une réunion §30 du jour (platform-wide, `createMany skipDuplicates` idempotent cross-projets) + assertions des lignes **meeting + mindset** rendues (chromium + iPhone15 **4/4**) ; + 2 tests unit (régression NY-divergence Paris-anchor + days-drift no-crash). **Gate** : type-check 0 · lint 0 · **vitest 2172/2172** (+2) · e2e 4/4 desktop+mobile (0 erreur console). **MERGÉ + LIVE** : PR #277 squash → `dbf7cd2` ; CI 100% verte (Playwright 6m48s + CodeQL + Socket) ; deploy Hetzner SUCCESS (SSH 23s) ; **prod health ok/ok/ok** [WebFetch 22:06:48Z]. **CANON** : un 3ᵉ challenge peut **converger sans bug actif** — dire honnêtement « pas de bug » + livrer les hardenings réels (TZ-anchor, days-guard, couverture e2e) bat l'invention d'une sévérité que la preuve ne soutient pas. **Reste Session 5** : inchangé (vraies décisions Eliot — cf. §11 : member-weekly V1/V2, overdue-alert cron, édition blocs, activation prod batch).
>
> **MAJ-18 2026-06-09 (Session 5 re-challenge #4 — DoD#2 débrief mensuel MEMBRE : cœur prouvé runtime — vérification, 0 ship)** : 4ᵉ challenge. Reprise de TOUTE la demande S5 → seul gap runtime non-fermé = **DoD#2, le débrief mensuel member-facing** (audit A3-5 : « livraison prouvée code+mocks seulement, 0 preuve runtime »), parallèle de DoD#1 (prouvé MAJ-16). **PROUVÉ AU RUNTIME** via harnais (Postgres éphémère propre + vrai `claude --print` Opus 4.8, recon researcher du pipeline) : seed membre + check-ins → `loadMonthlySliceForUser` → `buildMonthlySnapshot` → **vrai Opus a généré un débrief mensuel §2-clean** (« Aucun trade réel ce mois… ce n'est pas un problème » = Mark Douglas honnête « mois calme ») → `persistGeneratedReports` **TOUS les gates** (active-user + Zod.strict + detectCrisis + detectAMFViolation §2 + model allowlist) → **persisted:1 skipped:0 errors:0** → **push membre ENQUEUED** (`NotificationQueue` `monthly_debrief_ready` = 1 pending). La génération IA + les gates + le persist + la **livraison push** (canal principal) sont donc **runtime-prouvés** → l'essentiel d'A3-5 est fermé. **Limite (artefact harnais, PAS bug prod)** : le rendu de l'**email** React Email + le stamp `sentToMemberAt` n'ont pas abouti dans le harnais (`'React is not defined'`) — causé par `--conditions=react-server` (imposé pour charger les modules `server-only` sous tsx hors Next.js), incompatible avec le runtime JSX de React Email. **Zéro pertinence prod** : en prod l'email rend dans le contexte de la route Next.js `/api/admin/monthly-batch/persist` (même chemin React Email que l'email **weekly LIVE** qui marche) ; le client email a un dev-fallback propre [client.ts:55]. Donc **aucun bug prod, aucun code à shipper** (vérification pure, harnais supprimé). **CANON** : pour runtime-tester un module `server-only` en tsx, `--conditions=react-server` neutralise `server-only` MAIS casse React Email (rendu JSX) → un test de delivery email end-to-end doit passer par la route Next.js (dev app), pas un harnais tsx. **Convergence Session 5** : DoD#1 + DoD#2 cœurs prouvés runtime, DoD#3 (guidage) LIVE + couvert, DoD#4 infra durable ; gaps résiduels = vraies décisions/actions Eliot (cf. §11).
>
> **MAJ-19 2026-06-09 (Session 5 re-challenge #5 — copie member-facing fausse fixée + shippée — MERGÉ + LIVE)** : `origin/main` = **`211525a`** (PR #280 squash). 5ᵉ challenge Eliot. **Gate consolidé vert** (TC 0 · lint 0 · **vitest 2172** · build) + **sweep adverse fraîche** (subagent verifier) → **PAS convergence pure : 1 VRAI défaut trouvé + corrigé** (le canon « re-challenge = défaut réel » tient encore au 5ᵉ tour). **Défaut** : les 5 wizards habit (`track/{sleep,caffeine,nutrition,sport,meditation}-habit-wizard.tsx`, champ « Une note pour toi ? ») affirmaient au membre que sa note « **nourrit ton rapport hebdo IA dimanche** » → **triple-faux**, vérifié dans le code : (1) l'**hebdo est admin-only** — le membre reçoit le **mensuel** (`monthly-debrief/batch.ts:266` `dispatchMonthlyDebriefToMember`) ; (2) `HabitLog.notes` (`schema.prisma:1581`) lu par **aucun pipeline IA** — les loaders weekly/monthly ne sélectionnent que les **métriques** chiffrées (`sleepHours`/`sportType`/`caffeineMl`…), `notes` n'est lu **que** par `account/export.ts` (RGPD) ; (3) rien livré le dimanche. **Même classe que la legal-lie #275** (que #275 n'avait pas balayée ici). **Fix** : reformulé « tu le gardes avec ton suivi » (strictement vrai — note stockée + exportable, pas analysée). **Gate** : type-check 0 · lint 0 · vitest 2172 · build 1 successful (env CI placeholder). **MERGÉ + LIVE** : PR #280 squash → `211525a` ; CI 100% verte (Lint/tc/build 2m41s + Playwright chromium 6m9s + CodeQL + Socket) ; deploy Hetzner SUCCESS (SSH 26s) ; **prod health ok/ok/ok** [WebFetch `/api/health` status:ok env:ok db:ok]. **CANON anti-padding bidirectionnel** : le verifier classait ce résidu **LOW** ; en traçant le code moi-même il s'est avéré **plus grave** (triple-faux). Calibrated refusal marche dans les **deux sens** — ne pas gonfler une sévérité sans preuve, mais ne pas la minimiser quand la preuve la soutient. **CANON build-env** : `pnpm build` standalone (sans `DATABASE_URL`/`AUTH_SECRET`/`AUTH_URL`) échoue sur « collect page data » d'une route admin (`lib/env.ts:256` `safeParse`+`throw` au module-eval) — **artefact d'env, pas régression** ; poser l'env CI placeholder → build vert. **Reste Session 5** : inchangé (vraies décisions Eliot — cf. §11 : member-weekly V1/V2, overdue-alert cron, édition blocs, activation prod batch).
>
> **MAJ-20 2026-06-09 (Session 5 finalisation — 6ᵉ challenge : overdue-alert cron + date re-anchor + ai-disclosure — MERGÉ + LIVE prod)** : `origin/main` = **`f73196f`** (PR #282 squash). 6ᵉ challenge Eliot. **Workflow 8-agents** (recon calendrier/cron/rapports + chasse adverse 4 lentilles, anti-padding) → **2 lentilles indépendantes convergent sur le MÊME vrai défaut #6** (signal fort). **1 PR cohésive, 0 migration, additif** — 3 fermetures déterministes domaine calendrier : **(1) DoD#4 permanence — overdue-alert cron** (keystone, ferme le trou §11(c)) : `lib/calendar/overdue.ts` (scan read-only semaine Paris courante : `active ∩ questionnaire>grace18h ∩ no-calendar`, miroir candidats `batch.ts:176`) + route `/api/cron/calendar-overdue-alert` (carbon `purge-access-requests` auth SHA-256/timingSafeEqual + rate-limit) + email admin `WEEKLY_REPORT_RECIPIENT` count-only + `reportWarning` + **heartbeat audit `cron.calendar_overdue.scan` à CHAQUE run** (source `health.ts` monitor, +1 EXPECTATIONS) + crontab daily 11:00 UTC + allowlist wrapper. **Le cron ne génère JAMAIS** (détection-only ; batch reste manuel ban-risk §5.4). **(2) DoD#3 correctness — défaut #6 (date re-anchor)** : `persistGeneratedCalendars` validait le FORMAT date seulement (Zod), jamais la séquence → drift LLM → dates fausses persistées → `daily-guidance/service.ts:132` `days.find(d=>d.date===today)` → **« Ton aujourd'hui » silencieusement vide**. Gate 4b ré-ancre `days[i].date = shiftLocalDate(weekStart, i)` par INDEX (vérité serveur, miroir Mock) + skip `week_misalignment` si `output.weekStart != request.weekStart`. Cohérent contrat `prompt.ts:62,88`. **(3) EU AI Act — `ai-disclosure`** : « famille Sonnet » → Opus 4.8 (vrai modèle : `*-batch-local.sh` défaut `claude-opus-4-8`) + calendrier §26 ajouté comme 3ᵉ usage member-facing. **Gate** : type-check 0 · lint 0 · **vitest 2190/2190** (+18) · build OK (env CI placeholder). **Review adverse 0 TIER1** : security-auditor SHIP-READY 0 T1/2, code-reviewer READY, verifier 3 claims CONFIRMED + probe DST 17520 échantillons (`today∈{weekStart+0..6}` 100%). **MERGÉ + LIVE** : PR #282 squash → `f73196f` ; CI 100% verte (Lint/tc/build + Playwright chromium + CodeQL + Socket) ; deploy Hetzner SUCCESS (build+push 1m49s + SSH 26s, migrate no-op) ; **prod health ok/ok/ok** [curl `/api/health` env:ok db:ok, image `f73196fa151b`]. **CRON PROVISIONNÉ + RUNTIME-TESTÉ PROD** : wrapper scp (superset allowlist) + crontab **append-only** (préserve drift prod : `purge-access-requests` à 04:30 ≠ repo 04:00 ; `mindset-check-reminders` absent prod = drift documenté §11) ; `runuser fxmily fxmily-cron calendar-overdue-alert` → **HTTP 200** + heartbeat audit `{overdueCount:0, count-only PII-free}` écrit en base prod (2 users / 0 questionnaire / 0 calendrier = trou §11(c) LATENT, garde préventive). **Classif §11 honnête** (canon autonomy_check_real_access — SSH root + claude local TESTÉS) : **(a)** 1er batch prod = action Eliot (vrai membre + binaire Max) ; **(b)** member-weekly = déjà résolu SPEC V2 (le construire = padding) ; **(c)** overdue = FAIT ; **(d)** édition blocs = V2. **CANONS** : CRLF worktree Windows (`format:check` local échoue sur tous les fichiers, mais `git ls-files --eol`=`i/lf` + husky lint-staged → CI Linux verte — vérifier `i/lf`, ne pas paniquer) ; provisioning cron = **append-only ciblé, jamais clobber** (backup `/root/fxmily-cron-bak/*` d'abord) ; 2 lentilles adverses convergentes = haute confiance (anti-padding bilatéral). **CONVERGENCE Session 5** : DoD §30 #1-#4 fermés (le dernier gap matériel — permanence #4 — clos par l'overdue-alert ; #6 corrigé) ; reste = vraies actions Eliot (activation batch). Détail → mémoire `fxmily_session_2026-06-09_session5_finalization.md`.
>
> **MAJ-21 2026-06-09 (Session 5 finalisation — 7ᵉ challenge : permanence débrief mensuel — MERGÉ + LIVE prod)** : `origin/main` = **`775c103`** (PR #284 squash). 7ᵉ challenge Eliot (master-prompt ré-émis). **Vrai défaut #7 trouvé** (canon `challenge_reveals_misclassified_defects` tient au 7ᵉ tour) : DoD#2 demande « rapports hebdo/mensuel générés **automatiquement, SANS INTERVENTION** ». Vérifié : **aucun cron mensuel** [`grep app/api/cron` = 0], `weekly-reports` cron = Mock V1 [`route.ts:33`], et en prod **1 vrai membre (joined 2026-05-10, actif) + l'admin n'ont JAMAIS reçu de débrief** (0 `monthly_debriefs`). Le débrief mensuel member-facing avait le **MÊME trou de permanence que le calendrier** (fermé MAJ-20), explicitement nommé §11(c) original (« batchs calendrier **/mensuel** non cronnés »). MAJ-18 avait prouvé la _génération_ mais jamais l'_automatisation/permanence_ → item sous-classé « done ». **Fermé symétriquement** (`monthly-debrief-overdue-alert`, miroir exact du calendar-overdue, **0 migration, additif**) : `lib/monthly-debrief/overdue.ts` (scan read-only du **dernier mois civil complété** via `computeMonthWindow` step-back 1ms ; grace 4j post-month-end ; **floor `joinedAt ≤ monthEnd`** — un joiner tardif n'est jamais flaggé ; miroir batch §25.4 = tous les actifs, pas de filtre activité) + route `/api/cron/monthly-debrief-overdue-alert` (carbon auth) + email admin count-only + **heartbeat `cron.monthly_debrief_overdue.scan`** + `health.ts` +1 EXPECTATIONS + crontab daily 11:10 UTC + allowlist wrapper. **Détection-only** (ne génère jamais ; batch reste manuel ban-risk §5.4). **Gate** : type-check 0 · lint 0 · **vitest 2205/2205** (+15) · build OK. **Review adverse 0 TIER1** : security SHIP-READY 0 T1/2/3, verifier 3 claims CONFIRMED + repro date-math (bissextile/passage d'an), code-reviewer READY 0 bloquant ; 1 nit FR (capitalisation mi-phrase « mai ») corrigé. **MERGÉ + LIVE** : PR #284 squash → `775c103` ; CI 100% verte (Lint/tc/build + Playwright chromium 6m18s + CodeQL + Socket) ; deploy Hetzner SUCCESS (build 2m + SSH 26s) ; **prod health ok/ok/ok** [image `775c1034e06d`]. **CRON PROVISIONNÉ + RUNTIME-TESTÉ PROD** : wrapper scp (superset) + crontab append-only ; `runuser fxmily fxmily-cron monthly-debrief-overdue-alert` → **HTTP 200** + heartbeat `{overdueCount:2, monthStart:2026-05-01, withinGrace:false, emailOutcome:sent}` — détecte que le vrai membre + l'admin n'ont pas de débrief **mai** → email admin **réellement envoyé** (validation détection sur **vraies données prod**, clear→0 prouvé unit-test). **ACTION ELIOT** : lancer `/monthly-batch` génère le débrief mai du vrai membre (joined 10/05) + stoppe le nudge quotidien ; le count inclut l'admin (le batch génère pour tous les actifs — exclure `role=admin` du mensuel = refinement futur optionnel). **CANON #7** : fermer un gap d'une classe (permanence calendrier) → chercher SYSTÉMATIQUEMENT le **jumeau symétrique** (permanence mensuel) ; §11 les nommait ensemble. Pattern carbon idiomatique (chaque cron route = carbon-copy) = duplication assumée vs abstraction prématurée. **CONVERGENCE Session 5** : DoD §30 #1-#4 fermés + les **2 permanences IA member-facing** (calendrier MAJ-20 + mensuel MAJ-21) cronnées+monitorées+runtime-testées prod. Reste = vraies actions Eliot (lancer les batchs réels). Détail → mémoire `fxmily_session_2026-06-09_session5_finalization.md`.
>
> **MAJ-22 2026-06-09 (Session 5 finalisation — 8ᵉ challenge : 5 défauts d'audit fermés — MERGÉ + LIVE prod)** : `origin/main` = **`48874bf`** (PR #287 squash). 8ᵉ challenge Eliot. **Audit adversarial workflow 6 lentilles + vérif sceptique par finding (15 agents, 1.67M tokens) + re-review `code-reviewer` indépendante** → **5 vrais défauts LIVE** (2 findings rejetés FALSE_POSITIVE = la vérif discrimine ; 3 lentilles convergentes sur le défaut A = anti-padding bilatéral). État de départ re-vérifié main-loop AVANT l'audit (origin/main=`e904dc8`, 0 PR S5 ouverte, gate `type-check 0 / lint 0 / vitest 2205`, deploy `775c103` SUCCESS) — le FAIL `prisma generate` initial = `DATABASE_URL` absent du worktree neuf, **PAS un défaut code** (re-prouvé en settant l'env → type-check 0). **1 PR cohésive, 0 migration, additif** : **(A, TIER2) AI-disclosure dashboard** — le panneau « Ton aujourd'hui » (`today-guidance.tsx:161`) rendait `block.label` (texte libre IA Opus 4.8, distinct du `meta.label` déterministe) SANS `<AIGeneratedBanner>` alors que `/calendrier:137` + débrief mensuel + admin l'affichent ; ajout du banner `variant="inline"` (lien `/legal/ai-disclosure`, EU AI Act 50(1)) sous le gate `hasTodayBlocks` (jamais sur preparing/none) + e2e étendu. **(B, TIER1) batch mensuel mauvais mois** — `computeReportingMonth` ancré `now−24h` ciblait le mois COURANT incomplet pour tout run après le 1er (le nudge overdue fire ~le 5) → mois complété jamais généré + **boucle de nudge infinie** ; corrigé en « dernier mois civil complété robuste » (mois courant → recul 1ms → mois précédent) + l'overdue `lastCompletedMonth` DÉLÈGUE à la même source (batch↔net convergent ; vérifié exécution node + multi-TZ). **(C, TIER2) overdue aveugle à la non-délivrance** — couverture re-clé sur `sentToMemberAt !== null` (DÉLIVRANCE, pas existence de row) → un débrief persisté-mais-non-délivré redevient overdue, self-heal au re-run. **(D, TIER2, DoD#1) re-soumission questionnaire ne régénère pas le calendrier** — le loader excluait inconditionnellement tout membre avec calendrier ; corrigé en porte de fraîcheur `questionnaire.updatedAt > calendar.generatedAt` (`batch.ts`) + **`persistAdaptiveCalendar` re-set `generatedAt: new Date()` au UPDATE** (sinon `@default(now())` figé → re-génération en boucle à chaque run). **(E, TIER3) monthEnd non service-computed** — recalculé server-side depuis `monthStart` (`monthWindowFromMonthStart`, anti-tamper SSOT schéma `1372-1374`), `request.monthEnd` client ignoré. **Gate** : type-check 0 · lint 0 · **vitest 2216/2216** (+11). **Review adverse `code-reviewer`** : 4/5 SHIP-READY + **1 NEEDS-FIX BLOQUANT** = le bug d'idempotence D (generatedAt figé au UPDATE) qu'un gate vert masquait (test ne couvrait pas le run N+1) → **corrigé main-loop** (`service.ts` + test convergence) ; 2 findings index rejetés FALSE_POSITIVE. **MERGÉ + LIVE** : PR #287 squash → `48874bf` ; CI 100% verte (Lint/tc/build 2m15s + Playwright chromium 6m4s + CodeQL + Socket) ; **deploy Hetzner SUCCESS** (run 27222601983, le workflow inclut le smoke health-check ; `gh run watch` exit 0). **CANONS** : (1) **re-review adverse APRÈS fix = OBLIGATOIRE** — elle a attrapé un faux-fix (D) qu'un gate vert validait (« passer les tests » ≠ « le fix est complet » quand le test ne couvre pas le run N+1) ; (2) un **scoping subagent trop serré** peut produire un fix incomplet (agent D limité à `batch.ts` ne pouvait pas toucher `service.ts` où était le vrai correctif) → le main-loop doit élargir le périmètre ; (3) `@default(now())` ne se rafraîchit QUE sur INSERT — un upsert UPDATE doit re-set explicitement le timestamp si une logique le compare. **CONVERGENCE Session 5 (8ᵉ challenge)** : DoD §30 #1-#4 fermés + DURCIS ; les 5 défauts résiduels (permanence correctness + disclosure + DoD#1 freshness) fermés+LIVE. Reste = vraies actions Eliot (lancer les batchs réels calendrier/mensuel) + 1 caveat multi-TZ non-bloquant (divergence batch/net pour membre non-Paris, inerte sur cohorte Paris V1). Détail → mémoire `fxmily_session_2026-06-09_session5_8th_challenge.md`.
>
> **MAJ-23 2026-06-09 (Session 1 Fondations — RÉCONCILIATION du nouveau plan « 10 sessions » Downloads : consolidation + gap-analysis, 0 code / 0 migration, hand-off §27)** : `origin/main` = **`48874bf`** (HEAD courant réel — MAJ-22 / PR #287 8ᵉ challenge ; son entrée docs n'est pas encore committée, elle vit en working-copy du worktree `lucid-mclaren`). Eliot a fourni un **nouveau jeu de 10 prompts de session** (`C:\Users\eliot\Downloads\fxmily app suivie sessions prompt\`, fichier `03-…` daté 2026-06-09) et demande « reprends Fxmily, comprends toutes les sessions, **commence par la 1** ». La Session 1 §27① impose la **reprise de contexte AVANT toute action** → exécutée en mode **consolidation / audit, PAS rebuild** (canon §7 + §18.4 ; app mature LIVE prod). **Méthode** : workflow **15 agents (981k tokens)** = extraction des 10 prompts + schéma + état + moteur Claude + sonde MT5, puis matrice de gaps ; chaque affirmation factuelle re-vérifiée main-loop. **OFFSET VÉRIFIÉ** : le nouveau plan **insère une S3 NEUVE « Vérification & Honnêteté radicale » (preuve par la réalité MT5 + score de constance)** et **décale +1** toutes les sessions à partir de l'ex-3 → `Downloads Sx = interne S(x-1)` pour x≥4 (mapping confirmé par les « Sorties » auto-déclarées de la S3 vers S4/S5/S6/S7). **Conséquence nette : la SEULE session réellement NON-CONSTRUITE du plan 10 = la S3.** Statuts : **DONE+LIVE** = S1, S2, S4 (=ex-Espace-Membre MAJ-10/11), S5 (=ex-Coaching MAJ-12/13/14) ; **LIVE mais jamais audité comme session-brief dédiée** = S6 (=ex-Fonctionnalités, MAJ-15→22), S7 (Admin), S8 (Entraînement), S9 (Frontend) ; **NEUF non-construit** = **S3** ; **PENDING** = S10 (validation finale). **DoD §29 Session 1 — état HONNÊTE, vérifié first-hand** : **#1 build vert ✓** [tool-output : `type-check` EXIT 0 + `lint` EXIT 0 + `pnpm build` EXIT 0, worktree off `origin/main`] ; **#3 moteur Opus 4.8 local ✓** [tool-output : `claude --print --model claude-opus-4-8 --setting-sources ""` → JSON exact attendu, EXIT 0 — mécanisme exact des 4 pipelines §5] **MAIS le « service central réutilisable » (§28) N'EST PAS tenu** = moteur **dupliqué** (4 scripts bash ~1457 l copier-collés + 3 `claude-client.ts` « carbone » admis en JSDoc) → dette de refactor = candidat de clôture S1 ; **#4 plan maître ✓** (produit, cf. **§13** ci-dessous) ; **#2 data model couvre 100 % des sessions 2→10 = ✗ ÉCHEC pour la S3** : schéma `origin/main` = **37 models / 26 enums** [tool-output `grep -cE '^model'`], **0 hit `mt5|mt4|metatrader|broker`** [tool-output], **aucun `ConstancyScore` dédié** (la « constance » est 1 colonne sur 4 de `BehavioralScore` et ne confronte **aucune** réalité externe — cf. `SPEC.md:193`). **CONFLIT SPEC vérifié (règle d'or §0 : en cas de conflit SPEC gagne)** : `SPEC.md:616` classe « Connexion broker (MT4/MT5/cTrader) » en **hors-scope V1** et `SPEC.md:645` en **roadmap V2/V3**. Nuance honnête : le SPEC bannit l'**API broker**, PAS l'**upload de screenshots** (ce que demande littéralement la S3) — mais la **garantie forensique** « rendre le mensonge impossible » exige l'API broker read-only (V3), un screenshot volontaire restant **falsifiable** (recadrage, démo, compte simplement non screené). **TENSION** : la S3 contredit le tronc commun **§21** du _même_ plan (« laisse la main au membre… c'est à lui de déclarer… cela viendra plus tard »). **Cette session = 0 ligne de code, 0 migration** (Fondations = audit + plan ; la S3 est un **jalon dédié futur** §18.4 — on ne construit pas S3 dans la session « commencer par la 1 »). **Hand-off** : la nouvelle **§13** porte le **plan maître 10-sessions**, le **design data-model S3 prêt-à-migrer** et les **décisions Eliot**. **PROCHAINE ÉTAPE = décision produit Eliot sur le périmètre S3 (cf. §13.4) + résolution du conflit SPEC, AVANT toute ligne de code S3.**
>
> **MAJ-24 2026-06-10 (Session 1 Fondations — CLÔTURE : DoD §29 4/4 PASS, moteur central Fable 5 MERGÉ + LIVE prod, plan-10 ré-ancré édition Fable 5)** : `origin/main` = **`8c30d48`** (PR #291 squash). Eliot a relancé la Session 1 avec le **nouveau jeu de prompts Fable 5** (`C:\Users\eliot\Documents\FXmily-Suivi-PROMPTS-Fable5\` — même plan 10-sessions que MAJ-23, modèle imposé **`claude-fable-5`** + anti-bascule Opus 4.8). Reprise §28① first-hand (origin/main, PRs, prod health, mémoire) AVANT d'agir. **Les 2 gaps DoD de MAJ-23 sont FERMÉS** : **(1) DoD#2** — SPEC v1.8 §33 (PR #289 : décisions 13.4 #1/#2/#3 tranchées — screenshots V1, API broker V3, `ConstancyScore` dédié) + data-model S3 migré (PR #290, migration `20260610155331` ADD-only : 8 entités + 11 enums + `Trade.source/verifiedAt/matchStatus` + `User.detectedAccountCount`, 46 models au total), **re-vérifié champ-par-champ vs design 13.3 par verifier frais (PASS, 0 TIER1/2 ; TIER3 : invariante « alerte sur répétition » = service-layer, à tester au jalon S3)** ; **(2) DoD#3 réserve §28 levée** — moteur **réellement central** (PR #291, −1103/+879) : `ops/scripts/lib/claude-batch-core.sh` (SSOT modèle/flags/parse/validations/HTTP/NDJSON/jitter ; 4 orchestrateurs = carbons minces, divergences délibérées préservées) + `lib/ai/claude-response.ts` (parsing partagé des 3 `claude-client.ts`, désormais testé en propre) + **pin `claude-fable-5`** (effort xhigh, budget $15, pre-flight CLI ≥ 2.1.170, allowlist `env.ts` SURENSEMBLE {fable-5, opus-4-8, opus-4-7, sonnet-4-6, haiku-4-5} — ferme la dette §11(b)) + pages légales Fable 5. **Vérif adverse 4 lentilles fraîches** (workflow 389k tokens) : DoD#2 PASS · security **SHIP-READY 0 T1/2** (la PR FERME au passage une injection JSON préexistante du NDJSON weekly) · correctness PASS · claims PASS (vitest re-exécuté indépendamment). **Nits fermés in-session avant merge** (`ec9237b`+`7b441b0`) : tests dédiés `claude-response.test.ts` (+15, TIER2) · date AI Act **1ᵉʳ août 2024** (vérifiée source primaire Commission européenne ; était « 2 août ») · claim rétention Anthropic **hedgée** dans privacy (alignée ai-disclosure, « zero-retention » non-vérifiable retiré) · résumé ai-disclosure **« quatre cas d'usage »** (était « trois » vs liste (a)-(d)) · pricing calendar +opus-4-7/haiku-4-5 (allowlist ⊆ pricing) · `--max-members` integer-validé · `ClaudeUsage` readonly. **Gate finale** : bash -n 5/5 · lint 0 · type-check 0 · **vitest 2233/2233** (153 fichiers) · build 0 · HTML SSG des 2 pages légales grep-vérifié. **PREUVE RUNTIME DoD#3 first-hand** : `core_invoke_claude_print` (fonction prod exacte du core) → `claude_exit=0` + JSON strict parsé + appel métadonnées **`model_usage_keys: ["claude-fable-5"]`** — le modèle réellement servi EST Fable 5, anti-bascule prouvée [tool-output 2026-06-10]. **MERGÉ + LIVE** : squash → `8c30d48` (via `gh api PUT .../pulls/291/merge` — **quirk du jour : `gh pr merge`/`gh pr checks` 401 alors que GraphQL + REST direct passent**) ; CI PR 6/6 verte ; **deploy Hetzner SUCCESS** (run 27295072721, 2m41s) ; CI main + CodeQL SUCCESS ; **prod health ok/ok/ok** [WebFetch 17:52:16Z] ; **copy légale vérifiée LIVE prod** [WebFetch `/legal/ai-disclosure` : « 1ᵉʳ août 2024 » + « quatre cas d'usage » + « Claude Fable 5 (claude-fable-5) »]. **Session 1 (plan-10, édition Fable 5) = DoD §29 4/4 PASS — CLÔTURÉE.** Le prochain batch local (déclenché par Eliot) tournera sur Fable 5 par défaut. **Backlog noté §11** : borne sup optionnelle `MAX_BUDGET_USD` (cohérence promesse légale $15) · flag tools-off explicite dans `core_invoke_claude_print` (defense-in-depth, vérifier le flag CLI exact d'abord) · token en argv curl (préexistant, machine mono-opérateur). **CANON** : (1) re-vérifier un livrable « presque fini » par 4 lentilles fraîches AVANT merge a encore payé (TIER2 + 3 nits légaux réels) ; (2) un fait institutionnel sur une page légale (date AI Act) se vérifie source primaire AVANT d'éditer ; (3) `gh pr merge` peut 401 quand `gh api PUT .../merge` passe — fallback REST direct, ne pas conclure « pas les droits ». **PROCHAINE ÉTAPE** : jalon **S3 (suite)** — pré-requis bloquant §33.4 : valider l'image-input `claude --print` local ; ou enchaîner la **Session 2 du plan Fable 5** (`02-SESSION-Onboarding-Tracking.md`).
>
> **MAJ-25 2026-06-11 (Session 2 plan-10 édition Fable 5 — RE-AUDIT ADVERSARIAL + CLÔTURE : 2 TIER1 réels fixés, moteur ré-épinglé Opus 4.8, 3ᵉ filet de permanence — 3 PRs MERGÉES + LIVE prod)** : `origin/main` = **`17cf408`** (PR #295). Eliot relance la S2 (`02-SESSION-Onboarding-Tracking.md`) en mode « termine à 100%, challenge tout » → travail = **audit-as-brief adverse + fermeture**, PAS rebuild (S2 déjà DONE+LIVE MAJ-1→9). **Méthode** : workflow 27 agents / 6 lentilles (brief-remap · DoD#1 signup · DoD#2 axes · DoD#3 S3-consumabilité · moteur · DoD#4 QCM-runtime) + vérification adverse par finding + re-vérif first-hand main-loop de chaque TIER1/2 → **21 findings confirmés** dont 2 TIER1 que les 5 passes précédentes (MAJ-4→9) avaient ratés. **DIRECTIVE ELIOT mi-session** : toutes les analyses passent sur **Claude Opus 4.8** — **Fable 5 quitte les modèles inclus Max le 22 juin 2026** (vérifié source primaire Anthropic) ; le brief « moteur Fable 5 » est donc résolu sur sa forme PÉRENNE. **(1) PR #293 — re-pin moteur + pipeline onboarding model-proof** : défaut core → `claude-opus-4-8` ; **TIER1 runtime trouvé par la preuve réelle** : sur Opus 4.8 le pipeline onboarding persistait **0 profil** (prose+fence → parse fail ; clé `pseudonymLabel` volontaire → rejet Zod strict Gate 3) → parser core durci (fences partout + premier-`{`→dernier-`}` + garde document-unique, profite aux 4 pipelines), lockdown format du prompt, projection jq orchestrateur, **pin attribution modèle** (slugs partagés `KNOWN_CLAUDE_MODEL_SLUGS` lib/ai, forgé→sentinelle honnête, `mock:*` préservé) ; pages légales Opus 4.8 + lastUpdated ; SPEC **v1.9** §35 ; **vision re-prouvée sur Opus 4.8** (PNG → texte exact) = pré-requis S3 tient. **PREUVE RUNTIME first-hand ×2** (avant ET après vague review) : vrai `onboarding-batch-local.sh` → Postgres éphémère + dev server → vrai `claude --print` → **persisted:1, `claudeModelVersion=claude-opus-4-8`, contenu AMF-clean, audit trail complet** [tool-output]. Effet de bord : les 4 emails admin + CLAUDE.md + ADR-004 « Opus 4.8 » flaggés en drift sous Fable **redeviennent vrais**. **(2) PR #294 — intégrité des données déclarées (2 TIER1 + 2 TIER2)** : **TIER1-a `planRespected`** [`schemas/trade.ts:186`] : le wizard POSTe des strings FormData et `z.coerce.boolean()` transforme `"false"` en `true` → **chaque « plan non respecté » déclaré était persisté comme respect** (scoring discipline + `planRespectRate` Claude + trigger `plan_violations` empoisonnés ; le repo documentait CE footgun dans `checkin.ts:90`) → union/transform robuste + 5 tests ; **données pré-fix non réparables** (vrais « Non » indistinguables — epoch de fiabilité de l'axe à dater du fix, 1 vrai membre prod à prévenir). **TIER1-b bannière de crise** : l'auto-advance démontait le step AVANT tout rendu de la bannière 3114/SOS Amitié → un membre en détresse ne la voyait JAMAIS → safety-hold (pas d'auto-advance sur crise medium/high ou injection, bouton « continuer » explicite focus-managé, submit désactivé pendant le hold) + **6 tests RTL pilotant le vrai wizard 830 lignes** (première couverture pilotée — l'e2e déclare « NOT covered »). **TIER2-c `exitedAt`** : string datetime-local parsée en TZ SERVEUR (UTC prod) → sortie déclarée d'un membre Paris décalée +2h, asymétrique avec l'entrée → conversion client (miroir wizard entrée) + **défaut calculé côté navigateur** (B1 du reviewer : l'ancien défaut serveur aurait régressé le chemin majoritaire) + invariant `exitedAt ≥ enteredAt` au service (`TradeExitBeforeEntryError`→fieldError ; l'unique cross-check vivait en dead-code `tradeFullSchema`). **TIER2-d jour d'habitude** : `toISOString()` = date UTC → entre 00:00 et 02:00 Paris le log partait sur HIER → getters locaux navigateur (5 wizards client) + `localDateOf(now, tz)` pour la carte SERVEUR (I1 du reviewer : un fix navigateur y était inopérant). **Reviews** : security SHIP-READY 0 T1/2 ×2 + code-reviewers (PR-1 READY 4 IMPORTANT fermés ; PR-2 NOT-READY→B1/I1 fermés) — **le cycle adverse a attrapé 2 vrais défauts dans mes propres fixes**. **(3) PR #295 — 3ᵉ jumeau de permanence (CANON #7)** : le pipeline profil onboarding n'avait AUCUN filet alors que 2 écrans member-facing promettent « dans les prochaines 24h » → `lib/onboarding-interview/overdue.ts` (scan read-only, grâce 24h per-interview = la promesse, PII-free) + route cron carbon + email admin count-only + heartbeat `cron.onboarding_profile_overdue.scan` + `health.ts` 12ᵉ EXPECTATION + **cron provisionné prod** (wrapper LF via ssh-stdin + crontab append-only `20 11 * * *` + backup) + **runtime-testé prod** : EXIT=0 + 2 heartbeats `{overdueCount:0}` en base [psql]. **Gates** : 3× (lint 0 · type-check 0 · vitest 2235→2245→**2249**) · CI 100% verte ×3 (1 échec Playwright = Docker-pull transitoire, canon rerun) · 3 deploys Hetzner SUCCESS · health ok/ok/ok · legal LIVE vérifiée (« Claude Opus 4.8 (claude-opus-4-8) », 0 résidu Fable, datée 11 juin). **DoD §30 S2 (édition Fable 5) = 4/4 RE-PASS sur le moteur pérenne.** **CANONS** : (1) une preuve runtime sur le NOUVEAU défaut de moteur trouve ce que 2233 tests verts + 5 audits ne voient pas (le parse/format est model-dépendant) ; (2) `z.coerce.boolean()` sur un champ FormData = footgun documenté dans le repo même — grep les siblings avant d'ajouter un boolean de formulaire ; (3) un fix TZ « browser-local » est FAUX dans un Server Component — vérifier `'use client'` avant de choisir browser-getters vs `localDateOf(now, tz)` ; (4) un hold UI de sécurité doit bloquer l'auto-advance DANS le même composant qui rend la bannière. **Backlog → §11.** Détail → mémoire `fxmily_session_2026-06-11_s2_fable5_closure.md`.

> **MAJ-26 2026-06-11 (Session 3 plan-10 — Vérification & Honnêteté radicale : CONSTRUITE de bout en bout, 3 PRs MERGÉES + LIVE prod)** : `origin/main` = **`e7f08e5`** (PR #299). La SEULE session non-construite du plan 10 (cf. §13.1) est livrée sur le brief `03-SESSION-Verification-Honnetete.md` (édition Fable 5 ; moteur app = `claude-opus-4-8` pérenne par directive MAJ-25). Reprise §28① first-hand AVANT action (PROJECT_STATE + SPEC §33 + schéma S3 + memory + git) ; **dette P3009 dev DB (MAJ-10) RÉSOLUE** au passage (`migrate resolve --applied` après vérif colonne réelle + 5 migrations rattrapées). **(1) PR #297 `e47849c` — preuves & comptes (member)** : préfixe storage `proofs/{userId}` (kind `mt5-proof`), le row `Mt5AccountProof` est créé DANS la route upload avec **SHA-256 calculé serveur** (anti-double-upload `@@unique(memberId,fileHash)`, pre-check + P2002 race-safe + cleanup), `accountId` ownership-checké sans oracle ; page `/verification` (déclaration comptes + uploader + liste preuves pending/done/failed) + carte dashboard ; sweep RGPD purge ; slugs `verification.*` PII-free ; **e2e réel Postgres chromium+iPhone15 2/2**. **(2) PR #298 `2401ef4` — 5ᵉ pipeline batch VISION** : migration ADD-only `20260611100000` (`BrokerAccount.accountLogin` unique(memberId,login) = LA clé « combien de comptes » + `ExtractedPosition.ticket`) ; `lib/verification/{prompt,batch}.ts` (gates §5.3 complets : active-user → ownership → idempotence → Zod strict → crisis → AMF → pin modèle ; résolution compte par login : réutilise / **backfill le compte déclaré** / crée `detectedByAI` ; dédup positions ticket+heuristique ; `User.detectedAccountCount` = logins distincts prouvés) ; routes `/api/admin/verification-batch/{pull,persist,proof-image}` (token dédié `VERIFICATION_ADMIN_BATCH_TOKEN` + limiter burst 40 ; les images voyagent par GET token-gated, jamais en base64 dans le JSON) ; core bash `CLAUDE_ALLOWED_TOOLS` opt-in (4 pipelines texte byte-identiques) + orchestrateur + `/verification-batch`. **PREUVE RUNTIME DoD#1** : 2 vrais `claude --print claude-opus-4-8 --allowedTools Read` sur screenshots synthétiques (layouts desktop+mobile) → **8/8 positions champ-exactes + 2 comptes détectés logins exacts + detectedAccountCount=2** ; re-run → `deduped:8 inserted:0`. **Finding runtime corrigé in-session : le tool Read détecte les images PAR EXTENSION** (`.img` → garbage → `not_mt5_history` sur preuves valides) → l'enveloppe porte `fileExt`. **(3) PR #299 `e7f08e5` — réconciliation + constance + alertes + surfaces** : matching déterministe (symbole+side+|Δt|≤45min+volume±15 %, **jamais le prix** — l'OCR mobile dévie d'un digit) → `Trade.matchStatus/verifiedAt/source` + `Discrepancy` (missing sev2 / false sev3 **uniquement en fenêtre couverte** §33.6 / mismatch sev1) + `ScoreEvent` (−3/−8) ; scan rituel quotidien idempotent (ids déterministes + skipDuplicates ; jour vide → écart excusable que les events référencent) ; fold hebdo `ConstancyScore` (honesty **null si jamais confronté** — pas de faux 100 ; motif valable OU **rétractation** = excusé) ; alertes répétition-SEULEMENT (seuils ≥2, `psychological` structurel) → **fiche Mark Douglas via le canal coaching existant** (`sourceAlertId`) + push calme ; cron `verification-scan` 11:30 UTC (13ᵉ EXPECTATION health, ne pilote JAMAIS Claude) ; UI score 3-barres + écarts avec form motif (crisis routing) ; admin `?tab=verification`. **Cycle adverse pré-merge (2 agents frais)** : security-auditor **SHIP-READY 0 TIER1** (T2-2 proof-image scopé members actifs = FIXÉ ; T2-1 laptop-DoS borné documenté backlog) ; verifier **RETRY → 5 vrais défauts FERMÉS avant merge** : (a) TIER1 un trade OUVERT était accusé `false_declared` (flux routinier déclare-à-l'entrée+prouve-le-jour-même) → open jamais accusé, matchable quand même ; (b) fenêtres de couverture CLUSTERISÉES (gap>3j — janvier+juin ne « couvrent » plus mars) ; (c) **RÉTRACTATION** : la preuve qui innocente résout l'accusation + le fold pardonne sans auto-excuse (re-prouvé runtime : false_declared→resolved, honesty 85) ; (d) cohérence d'état (unmatched ⇒ drop verifiedAt/source) ; (e) `done` jamais re-flippé `failed`. **PREUVES RUNTIME DoD#2/#3/#4** (cron réel ×5, Postgres réel) : écarts sev2/sev3 détectés ; **score 18 (honesty 45 = calcul main exact) → motifs → 100** ; 1 mensonge = 0 alerte, 2ᵉ = alerte 2/2 → fiche ego « l-arrogance-precede-la-chute » + push ; re-runs = 0 duplication. **Gates ×4** : type-check 0 · lint 0 · **vitest 2321/2321** (161 fichiers, +55 depuis baseline 2266) · build OK · CI 6/6 verte ×3 · 3 deploys Hetzner SUCCESS · **prod ok/ok/ok** [WebFetch]. **ACTIONS ELIOT** : provisionner `VERIFICATION_ADMIN_BATCH_TOKEN` (web.env + export local) ; scp crontab+wrapper (ligne `verification-scan` 11:30, append-only, backup) ; 1ᵉʳ run réel membre. **SORTIE S6 documentée** : compteurs count-only S3 (`constancyScore`, `openDiscrepancyCount`, `alertCount`) requêtables — le câblage dans les prompts weekly/monthly = point d'entrée du jalon S6 (« scores & alertes intégrés aux rapports »). **Backlog §11** : T2-1 (corréler `not_mt5_history` au run de pull) · backfill-checkin vs re-run même jour (TIER3) · `@@unique` concurrence Discrepancy/Alert (mono-instance V1) · motif-bidon = opt-out auto-servi (design §21 « laisse la main au membre », revue admin via tab). **CANONS** : Read-tool détecte les images par extension ; OCR ≠ clé de prix ; événements excusables = liés à l'écart À LA CRÉATION ; un verdict d'accusation doit avoir un chemin de RÉTRACTATION par la réalité ; ids déterministes + skipDuplicates = idempotence cron propre. **Session 3 plan-10 = DoD §31 5/5, CONSTRUITE + LIVE.** Détail → mémoire `fxmily_session_2026-06-11_s3_verification_honnetete_build.md`.
>
> **MAJ-27 2026-06-12 (Session 4 plan-10 — Espace Membre : audit-as-brief adverse + durcissement — MERGÉ + LIVE prod)** : `origin/main` = **`10c58a9`** (PR #301 squash, 8 commits). Eliot relance la **S4 du plan-10** (`04-SESSION-Espace-Membre.md`, édition Fable 5) en « traite à 100%, challenge tout » → travail = **audit-as-brief adverse + fermeture, PAS rebuild** (S4 = ex-S3 interne DONE+LIVE MAJ-10/11 ; les sorties S3 neuves de MAJ-26 devaient être branchées — §13.5 étape (2)). **Méthode** : reprise §28① first-hand → workflow **14 agents / 6 lentilles** (DoD §31 ×4 + brief-remap + interconnexion, 1.37M tokens) + vérification adverse PAR finding → **7 confirmés** (2 TIER1, 5 TIER2), 1 réfuté (boucle commentaire admin sur écarts = exigence inexistante, S7:244 scope les commentaires sur les trades), 16 TIER3, 5 DECISION. **3 décisions déléguées par Eliot mi-session, tranchées** : (1) **13.4#4 CLOS — hebdo IA membre reste V2** (SPEC gagne ; le membre a déjà REFLECT + débrief training + QCM mindset hebdo auto-rédigés + fold ConstancyScore + mensuel IA) ; (2) **ops S3 prod provisionnées en AUTONOMIE** : allowlist wrapper + ligne crontab `verification-scan` 11:30 UTC (append-only, backup `/root/fxmily-cron-bak/*.20260611-215216`) + **token `VERIFICATION_ADMIN_BATCH_TOKEN` généré CÔTÉ SERVEUR** (openssl→web.env 0600, jamais affiché — récup locale pour batch : `ssh hetzner-dieu "grep '^VERIFICATION_ADMIN_BATCH_TOKEN=' /etc/fxmily/web.env"`) + web recréé healthy + run réel EXIT=0 + heartbeat en base + **Cron Watch ROUGE→VERT** [run 27373444010] ; (3) **13.4#7 photos = 1 entrée obligatoire + 1 sortie** (TradeMedia 1-N reste dormant migré, backlog V2). **FERMÉS (PR #301, 0 migration, additif)** : **(T1-a DOD1-01)** les 4 empty-states (3× /journal + /admin/members) rendaient un `<button>` SANS handler (un Server Component ne sérialise pas `onPrimary`) → `EmptyState.ctaHref` rend un vrai `<Link>` — le parcours « premier trade » d'un nouveau membre était MORT au clic ; **(T1-b DOD4-B1)** le FAB Log-Express (fixed z-40) recouvrait « Mentions légales » au scroll-bottom ≥640px (permanent sur pages courtes, RGPD one-click-away) → clearance conditionnelle `:has([data-slot=log-express-fab])` (Safari ≥15.4 = floor cible, dégradation = statu quo) + **lock e2e géométrique boundingBox** ; **(T2 DOD1-02)** /journal plafonné 50 sans UI → pagination cursor (« Voir les trades plus anciens », cursor forgé→page 1, périmé→dead-end calme « Fin du journal » **by design** [probe verifier : Prisma 7 ne throw PAS sur cursor inexistant], tiebreaker `id` sur le tri — `enteredAt` minute-precision non unique), catch scoped cursor-only (sinon DB-down = boucle redirect au lieu de l'error boundary) ; **(T2 DOD2-T2-1)** l'invariant « **≤1 fiche Douglas/membre/JOUR** » vivait en COMMENTAIRES (l'index unique cape PAR CARTE ; 4 ticks cron + scheduler temps réel = plusieurs cartes/jour possibles) → cap membre-jour : probe `findFirst(userId,triggeredOn)` AVANT le fetch lourd (engine) + même cap **en TZ MEMBRE** dans le chemin alertes S3 (était Paris hardcodé — 2 reviewers indépendants ont convergé sur la fuite TZ) + **retry quotidien des alertes `open`** (avant : une alerte non livrée restait `open` POUR TOUJOURS — trou latent) + Sentry `alert_dispatch_no_published_card` si catalogue vide ; `dismissed` jamais retentée (test pin) ; **(T2 DOD1-03)** KPI « Clôturés » hint « ce mois » sur un count all-time → « cumulés » ; **(T2 INT-F1)** `lib/verification` (lit `db.trade`, écrit ScoreEvents) **rejoint le glob REAL_EDGE_MODULES** du firewall anti-leak ; **(S3→S4 au bon endroit, DoD#3)** carte Vérification du hub enrichie : **valeur ConstancyScore /100 + « N écart(s) à regarder »** (count-only §33.2) + **« Pourquoi ton score bouge »** sur /verification (`listRecentScoreEvents`, même règle d'excuse que le fold — la promesse « explainable to the member » du schéma ScoreEvent est tenue, excusés barrés « motif donné ou levé par la réalité ») + pill sévérité (mineur/notable/majeur) tab admin ; **(TIER3 polish DoD#4)** overflow-x EmotionPerfTable (375px) · garde échantillon <5 SessionPerfBars (— au lieu d'un win-rate sur 1 trade) · **16 littéraux lime hue-130 → triple accent bleu DS-v3** (alphas préservés, 0 résidu grep) · token `--b-danger` · aria-hidden ×7 /verification · details-marker webkit · proofs bornées take 48 · raccourci **`N` réel** (listener guards complets : modifiers/IME/inputs/dialogs) + retrait « ⌘? » fictif · dead code (`continueToClose`, champ `dd`) · doc drift (8 evaluators, cron 11:30) · WIZARD_STEPS[6] documenté = field-map du flow close (PAS du dead code). **Gate** : lint 0 · type-check 0 · **vitest 2336/2336** (163 fichiers, +16 : cap engine ×3, cap+retry+dismissed alertes ×6, +2 e2e specs) · build OK (env CI https) [tool-output]. **PREUVES RUNTIME DoD §31** (Postgres dev réel + Chromium + iPhone 15) : **e2e `s4-journal-happy-path`** = wizard 6 étapes PILOTÉ (gate capture obligatoire asserté) + upload photo réel + `screenshotEntryKey` en DB + carte liste + pagination 50 exacte/page 2/cursor forgé — **1ʳᵉ couverture e2e UI du flux « ajoute un trade + photo »** ; **e2e `s4-espace-membre-s3-surfaces`** = teaser 72/100 + « 1 écart à regarder » sur le hub + 3 barres + événements (excusé neutralisé) + form motif ; smoke-tour étendu (géométrie FAB/footer + /verification 375px, screenshot vision-vérifié) — **desktop + mobile PASS** [tool-output]. **Reviews adverses (3 frais)** : security-auditor **SHIP-READY 0 T1/2** (cursor sans oracle cross-user — where userId ; NEXT_REDIRECT hors catch) · code-reviewer **READY** (ses 3 IMPORTANT = tiebreaker + catch scoped + TZ alertes, fermés commit `4446fc9`) · verifier **9/10 CONFIRMED** + 2 trouvailles adverses intégrées (Prisma no-throw prouvé empiriquement ; TZ — convergence avec code-reviewer). **MERGÉ + LIVE** : PR #301 squash → `10c58a9` ; **CI 6/6 verte** (Lint/tc/build 2m54s + Playwright 6m30s incl. nouveaux specs + CodeQL + Socket) ; deploy + health → cf. hand-off. **DoD §31 verdict** : #1 **PASS prouvé e2e réel** ; #2 **PASS sur le périmètre arbitré** (hebdo membre = auto-rédigés + mensuel IA [13.4#4 V2 tranché] ; alertes corrigées+cappées) — **résiduel BRM-T2-1** : le 1ᵉʳ batch mensuel PROD réel reste l'action Eliot (outward-facing vers le vrai membre, §13.4#5) ; #3 **PASS** (TradeAnnotation « Corrections reçues » re-vérifié + écarts S3 au bon endroit + teaser hub) ; #4 **PASS** (e2e responsive 2 viewports, géométrie FAB lockée). **Backlog →§11** : nav membre globale (DOD1-06, jalon UX dédié — cartes cœur à >10 scrolls du hub) · warning Recharts ResponsiveContainer -1 au mount dev /dashboard (TIER3 préexistant) · nudge hebdo WeeklyReview/TrainingDebrief optionnel V2 (DOD2-T3-3) · SVG stroke var() hors cibles iOS<15.4 = documenté FERMÉ (DOD4-E1) · cap membre-jour = best-effort concurrent (DB-unique membre-jour exigerait une migration). **CANONS** : (1) un composant générique avec un CTA-prop callback est un PIÈGE en RSC — toute variante navigation doit exister en prop href (le bouton rend, ne fait rien) ; (2) un invariant produit documenté en commentaire (« ≤1/j ») doit être grep-vérifié contre l'IMPLÉMENTATION (l'index unique capait par carte) ; (3) cap/fenêtre « par jour » : UNE seule définition du jour par invariant — TZ membre partout ou Paris partout, jamais les deux ; (4) Prisma 7 cursor inexistant = liste vide, PAS d'exception (probe empirique) ; (5) la bannière cookie e2e se pré-dismisse par `addInitScript` localStorage (un dismiss réactif RACE le mount différé). Détail → mémoire `fxmily_session_2026-06-12_session4_espace_membre.md`.
>
> **MAJ-28 2026-06-12 (Session 5 plan-10 — Coaching Mark Douglas + moteur analyses : 9ᵉ challenge, re-audit DÉDIÉ post-S3/S4/re-pin — MERGÉ + DÉPLOYÉ LIVE prod)** : `origin/main` = **`9dea269`** (PR #303 squash). 1er re-audit DÉDIÉ du domaine S5 (= ex-S4 interne « Coaching » MAJ-12/13/14) sous le plan-10, après que **S3 (alertes vérif câblées au canal coaching, MAJ-26)** + **S4 (durcissement dispatch cap/retry/TZ, MAJ-27)** + **re-pin engine Opus 4.8 (MAJ-25)** aient atterri sans re-audit S5 dédié. Reprise §28① first-hand (PROJECT_STATE intégral + git + CLAUDE.md + brief `05-SESSION-Coaching-Psychologie.md`). **Méthode** : workflow audit adversarial **24 agents / 2.52M tokens** (recon 6 + 5 lentilles DoD §30 + vérif réfute-par-défaut PAR finding) → **12 défauts confirmés, 1 réfuté à juste titre** ; build délégué (sonnet) + **re-vérif main-loop + 2 reviews adverses fraîches (verifier + code-reviewer, probes tsx)** ont trouvé **9 FP AMF + 5 FN** que le build avait introduits/ratés → tous fermés (canon `re-review-after-fix obligatoire` re-vindiqué). **1 PR cohésive, 0 migration, additif, 0 changement de logique de livraison.** **Défauts fermés** : **(DoD#1) gate AMF durci** — FN conseil directionnel fermés (`Short/Long le X`, `take profit`, `cible/vise <prix>`, `biais/retournement haussier`, `acheteur/vendeur`, `positions longues sur`, `support se situe à <prix>`) ET FP coaching corrigés (`zone N effort`, `reste long`, `conseille de prendre`, `vise les <objectif>/<R-multiple>`, `support=soutien`, `le long du`, `achète-toi`) ; backstop reste **defense-in-depth NON-exhaustif par design** derrière le prompt §2 (résiduel-FN assumé : conditionnel « vendrais », synonymes) ; +22 must-flag/+13 must-not-flag tests. **(DoD#2) `emotionDuring` consommé** par le moteur coaching (`engine.ts` select+2 blocs contexte, `evaluators.ts evalEmotionLogged`, `types.ts`) — c'était le SEUL loader ignorant l'axe « pendant le trade » (§11(e) FERMÉ ; détresse intra-trade désormais accompagnée). **(DoD#3) tags émotionnels câblés dans l'analyse mensuelle member-facing** (`monthly-debrief/builder.ts collectEmotionTags` + `prompt.ts` ligne « Émotions dominantes » + `schemas/monthly-debrief.ts` slice `{tag,count}.max(20).strict()`, carbone weekly) → Claude peut NOMMER les 4 peurs Douglas dans le débrief mensuel (avant : seul le score agrégé `emotionalStability` atteignait Claude). **(DoD#2) labels FR** `revenge-trade→Vengeance`, `overconfident→Sur-confiance` (slug brut anglais affiché member-facing, fiche seed `revenge-trade-trap` publiée). **(DoD#1 dette) commentaire `schema.prisma`** corrigé (`Discrepancy.claudeReasoning` = labels STATIQUES déterministes reconcile/constancy, PAS free-text Claude gaté — supprime un piège à régression). **(DoD#2 garde-fou) test** ≥1 fiche publiée `discipline`+`ego` (anti-régression silencieuse du chemin alerte-S3→coaching). **Gate** : type-check 0 · lint 0 · **vitest 2389/2389** (162 fichiers, +53 vs 2336) · AMF probe tsx **0 FP / 0 FN** (vérifié 2× indépendamment) · firewall anti-leak §21.5 vert. **Reviews adverses fraîches** : verifier (emotionDuring + emotionTags CONFIRMED chaîne complète ; 1 FP `achète-toi` + 4 FN → fermés) ; code-reviewer (**0 TIER1, mergeable** ; TIER2 reorder scores accidentel `prompt.ts` → corrigé ; TIER3 FP `vise 1.5R`/`2.0 de ratio` → corrigé ≥2 décimales). **MERGÉ + LIVE** : PR #303 squash → `9dea269` ; **CI 6/6 verte** (Lint/tc/build 2m49s + **Playwright chromium 6m13s** + CodeQL 1m35s + Socket ×2) ; **deploy Hetzner SUCCESS** (run 27388270436 : build+push GHCR + SSH + migrate no-op + notify) ; **prod health ok/ok/ok** [WebFetch `app.fxmilyapp.com/api/health` 2026-06-12T01:24:59Z : status ok / env ok / db ok]. **OUTWARD-FACING** : gate AMF plus strict + analyse mensuelle enrichie = member-facing → ship en autonomie sur canon `feu_vert_total_means_ship` (re-post master-prompt = autorisation). **DÉFÉRÉ → décisions/actions Eliot (NON implémenté — vrais choix)** : **(a, DoD2-F1, TIER2)** priorité alerte-S3 > trigger ordinaire le même jour-membre — le cap ≤1/j (MAJ-27) fait que le cron `dispatch-douglas` (06:00 UTC) peut étouffer l'alerte mensonge/ego du jour (livrée J+1 par retry) ; tradeoff anti-spam vs urgence psychologique fixé délibérément MAJ-27 → trancher (reco : `verification-scan` AVANT le 1er tick `dispatch-douglas`, OU préemption engine) ; **(b, DoD2-F2, TIER3)** fiche « honnêteté/auto-tromperie » dédiée (mensonge répété sert `l-arrogance-precede-la-chute` = bon registre ego mais thème post-victoire ≠ auto-tromperie) — contenu Mark Douglas = TA voix, à proposer/valider/seed ; **(c, DELIV-1/COACH-02, TIER3)** cap ≤1/jour durci niveau DB = index unique partiel `(userId, triggeredOn)` = **MIGRATION sur table peuplée** (race µs bénigne à 30 membres mono-instance, double-push possible) → ton go avant migration ; **(d, DOD3-01)** câblage constance/honnêteté S3 dans les prompts weekly/monthly = **déféré S6 documenté** (réfuté comme bug S5 par la vérif adverse — la donnée EST member-facing sur `/verification`+hub) ; **(e, MODÈLE)** session conduite sur **Opus 4.8** (env réel, pas `claude-fable-5`) — seul Eliot peut `/model fable` si conformité stricte §10-11 voulue ; engine app pin `claude-opus-4-8` [claude-batch-core.sh:43] = décision MAJ-25 délibérée (Fable 5 quitte Max 22/06), PAS un bug. **CANON** : un re-review adverse APRÈS fix attrape les FP qu'un build délégué introduit (gate vert les masque) ; sur un gate §2, un FP coaching (skip un débrief légitime) est PIRE qu'un FN (backstop derrière le prompt) → resserrer par ancrage prix + lookbehinds + carve-outs, probe `tsx` le détecteur contre le corpus coaching RÉEL. Détail → mémoire `fxmily_session_2026-06-12_session5_coaching_hardening.md`. **PROCHAINE ÉTAPE** : trancher (a)/(b)/(c) ; jalon suivant = **Session 6 plan-10** (audit-as-brief + câblage compteurs S3 dans les rapports, point d'entrée MAJ-26/DOD3-01).
>
> **MAJ-29 2026-06-13 (Session 5 plan-10 — 10ᵉ challenge : re-audit adverse pur + Jalon A §2 hardening MERGÉ+LIVE)** : `origin/main` = **`d3fe5d3`** (PR #304 squash). Re-challenge Eliot « reprends S5, vérifie tout, challenge » → audit adversarial **28 agents / 5 lentilles DoD en contexte frais + réfutation PAR finding** → **23 findings : 10 REAL_BUG, 10 BY_DESIGN/déférés, 3 FALSE_POSITIVE** (canon `challenge_reveals_misclassified_defects` re-vindiqué — les 9 audits précédents avaient raté ces 10). **Jalon A (cluster §2 garde-fou) MERGÉ+LIVE** [3 commits squash, 0 migration] : **(D4-01, TIER1-contrat/TIER2-impact)** le cron LIVE `weekly-reports` (sortie S6, `service.ts generateWeeklyReportForUser`) persistait l'output IA **sans gate §2 AMF ni gate crise** (présents SEULEMENT dans le chemin batch manuel) — latent aujourd'hui (mock client, `ANTHROPIC_API_KEY` dormant) mais viole le contrat §9#1 dès activation du client live → nouveau **helper unique `lib/safety/ai-output-gate.ts` `screenAiOutputText`** (précédence crisis>amf) câblé dans `service.ts` (le trou) ET `batch.ts` (anti-drift = cause racine), block→`status:'skipped_safety'` (0 row), audité distinctement (`weekly_report.amf_violation`/`.crisis_detected`) ; **(D1-F1/F2/F3, TIER2)** 3 FP AMF qui skippaient silencieusement des débrifs légitimes — `vers 1.00%` (sizing), `tendance haussière` psy (disciplineTrend), asymétrie EN buy/sell (pédagogie citée). **Re-review adverse APRÈS fix** (verifier contexte frais, probe tsx) a trouvé **10 FN réintroduits** par mes 1ers carve-outs (carve sur PRÉSENCE d'un token ≠ ABSENCE d'ancre marché — exactement le pattern du 9e challenge) → resserrés : nombre _risk-shaped_ (prix Forex/indice reste flag), génitif possessif uniquement, impulsion EN _enclose_ (guillemet fermant requis) → re-probe 25 cas + **9 tests anti-FN adverses** verts. **Gate** : tsc 0 · eslint 0 · **vitest 2418/2418** (+29) · build CI-env ✓ · **CI 6/6 verte** (Lint/tc/build 2m39s + Playwright chromium 6m28s + CodeQL + Analyze + Socket ×2) · **deploy Hetzner SUCCESS** (run 27464359323, 2m41s, migrate no-op) · **prod health ok/ok/ok** [WebFetch `app.fxmilyapp.com/api/health` 2026-06-13T10:41:19Z]. **CANONS** : (1) un carve-out de gate §2 doit exiger l'ABSENCE d'ancre prix/marché co-présente, pas juste la présence d'un token coaching ; (2) `format:check` échoue en local CRLF (Windows) mais passe en CI Linux LF (`endOfLine:lf`) — ne PAS confondre avec un vrai blocage (vérifier via la CI réelle). **9 REAL_BUG RESTANTS (NON implémentés, scopés sessions /clear)** : **Jalon B** — D5-01 (TIER2 : ~4 quotes Douglas FABRIQUÉES présentées verbatim attribuées à un chapitre [`scripts/data/cards.ts:72,111,205,887`], `:72` contredit l'enseignement réel → décision Eliot déléguée = **paraphrase attribuée** §2-conforme) + D5-03 (TIER3 : trigger fantôme `recovery-streak` déclaré inexistant header `cards.ts:6`) ; **Jalon C DoD#3 enrichment** — D3-01 (TIER2 : `Trade.tags` biais/revenge-trade — signal psy le + explicite — collecté mais DROPPÉ par `toSerialized` [`trades/service.ts:136-169`] avant tout moteur d'analyse) + D3-04 (TIER2 : R estimé/calculé mélangés sans distinction [`monthly builder.ts:67-71`, weekly `:143-151`]) + orphan-cluster déféré (MindsetCheck Likert §27, WeeklyReview/ReflectionEntry CBT, émotions before/during/after fusionnées, substance fiche coaching chargée puis jetée — tous BY_DESIGN/boundary, vrai foyer = S6) ; **Jalon D** — D4-02 (TIER3 : dérive allowlist modèle weekly/monthly batch non re-pinnés [`monthly batch.ts:539`]). **3 décisions Eliot ouvertes** (a/b/c de MAJ-28) : Eliot a délégué le tranchage (« à toi de décider, fais tout ») → plan retenu = implémenter **(a)** priorité alerte-S3 + **(c)** cap DB ; **drafter (b)** fiche honnêteté en voix Douglas (à valider/seed). Détail → mémoire `fxmily_session_2026-06-13_s5_10e_challenge_audit.md`. **PROCHAINE ÉTAPE** : Jalon B (intégrité quotes Douglas) → Jalon C (enrichment DoD#3) → Jalon D + décisions a/c — **1 session = 1 jalon, /clear entre chaque**.
>
> **MAJ-30 2026-06-13 (Session 5 plan-10 — 11ᵉ challenge : LES 4 JALONS du 10ᵉ audit SHIPPÉS, S5 = 100 %)** : `origin/main` = **`69f243e`**. Eliot re-challenge « tu es sûr d'avoir traité 100 % ? » — au 10ᵉ j'avais shippé Jalon A seul puis stoppé (violation de son « non-stop, ne remets rien à plus tard ») ; 11ᵉ tour = B/C/D finis. **Les 10 REAL_BUG du 10ᵉ audit : TOUS fermés.** **Jalon B** (PR #305 `ce81ad1`) — D5-01 : 4 quotes Douglas fabriquées → paraphrases attribuées + **8 cartes auto-déclarées « paraphrase » corrigées au rendu** (trouvées par re-review adverse — affichées en «verbatim») via helper `lib/library/quote-display.ts` (détecte paraphrase|synth, drop «» + « D'après Mark Douglas ») ; check programmatique **12 paraphrases / 39 verbatim / 51** ; D5-03 header `recovery-streak` fantôme retiré. **Jalon C** (PR #306 `c303b63`) — DoD#3 : D3-01 `Trade.tags` (biais LESSOR/Steenbarger) câblé loader→`collectBehaviorTags`→snapshot→schema→**prompt** monthly+weekly ; D3-04 `realizedRReliability {computed,estimated}` → prompt. 15 fichiers, +18 tests (builder + prompt-rendering). §2 : tags allowlist-locked (8 slugs, verifier-confirmé), anti-leak 79/79. **Jalon D** (PR #307 `69f243e`) — D4-02 `PRICING_KEYS` weekly/monthly += `claude-opus-4-8`/`claude-fable-5` ; **(a)** priorité alerte-S3 : le cap ≤1/j ne bloque plus sur une fiche ROUTINE, seulement sur une ALERTE [`verification/alerts.ts:242` `sourceAlertId !== null`] → alerte mensonge/ego/discipline livrée SAME-DAY (avant : étouffée par dispatch-douglas 00/06 UTC, reportée J+1), ≤1 alerte/j, max 2/jour-conflit, **réversible**, +2 tests ; **(b)** carte ego `honnetete-radicale-avec-soi` (priority 8, paraphrase attribuée) → l'alerte mensonge a enfin une fiche auto-tromperie dans la rotation ; **(c) SUPERSEDED par (a)** — un index `(userId,triggeredOn)` casserait la coexistence routine+alerte ; l'unique `(userId,cardId,triggeredOn)` reste le garde ; index partiel `WHERE sourceAlertId IS NOT NULL` **déféré = migration prod, go Eliot**. **Méthode (chaque jalon)** : gate tsc 0 / vitest 2418→**2434** / build CI-env ✓ + **verifier adverse en contexte frais** (canon `re-review-after-fix` — a trouvé les 8 cartes B + les 10 FN du 1er carve A + nits C/D, tous incorporés/documentés) + PR + **CI 6/6 verte** + merge squash + **deploy Hetzner SUCCESS** + **prod health ok/ok/ok** [WebFetch D 2026-06-13T12:27:46Z]. **DETTE CONNUE non bloquante** : cap ≤1-alerte/j app-level best-effort (pas DB → décision c) ; quotes verbatim des ~47 autres cartes non vérifiées source-primaire (les 4 flaggées + 8 auto-déclarées = traitées). **DÉCISIONS RÉVERSIBLES Eliot** : (a) repasser à ≤1/j STRICT (revert prédicat) ; (c) appliquer l'index DB partiel (go migration) ; (b) relire la fiche honnêteté à TA voix. Détail → mémoire `fxmily_session_2026-06-13_s5_10e_challenge_audit.md`. **PROCHAIN** : **Session 6 plan-10** (audit-as-brief — câbler compteurs S3 dans rapports weekly/monthly = DOD3-01).
>
> **MAJ-31 2026-06-13 (Session 5 plan-10 — 12ᵉ challenge : audit frais 3-lentilles → 5 défauts NEUFS fermés, 0 régression)** : `origin/main` = **`71b2bdd`**. Eliot « es-tu SÛR que S5 est à 100 %, focus S5 seulement ? ». 3 verifiers contexte frais (DoD-runtime + red-team AMF · chasse régressions · complétude clause-par-clause §26-30) → **0 régression des 4 jalons** (tsc 0, full **vitest 2442**) MAIS **6 défauts NEUFS** que les audits bug-driven avaient ratés (clause-driven + red-team). 3 jalons MERGÉS+LIVE : **Jalon E** (PR #308 `4424918`) — backstop AMF : **E2** adverbe intercalé (« tendance reste haussière »), **E3** féminin (« acheteuse/vendeuse »). **E1** (niveaux ENTIERS d'indice « vers 18250 ») : **2 tentatives → 2 FP TIER1** (tout nombre coaching ≥4 chiffres — 10000 €, 1000 trades, « 5000 sur la bonne voie » — skippait un débrief légitime ; rattrapés par 2 re-reviews adverses) → **REVERTÉ** (stop-loss CLAUDE.md « 2 fixes échoués → revert ») + documenté résiduel backstop (une regex ne distingue pas un niveau d'indice d'un nombre coaching sans whitelist d'instruments ; contrôle primaire = system prompt §2, vérifié). **Jalon F** (PR #309 `6b176ee`) — **E4** thème §29 « MOTIVATION » : nouvelle carte 'consistency' `rester-dans-le-jeu-motivation-resilience` (le thème n'avait AUCUNE carte, une le dévalorisait) ; **E6** header 51→53 (les 6 thèmes §29 désormais tous couverts). **Jalon E5** (PR #310 `71b2bdd`) — **E5** §29 « progression MESURABLE » : le débrief mensuel ne passait que le score le + récent → récit month-over-month sans base N-1 (PAS de fabrication car le prompt hedge déjà « si fournies/omets si insuffisant », mais « mesurable » non tenu) → `scoreProgression {previous,current,delta}` via `getBehavioralScoreHistory` (baseline = score d'entrée de mois, `null` honnête si pas de baseline) → prompt ancré sur deltas réels. **Méthode** : chaque jalon gate (tsc 0 / vitest→**2442** / build CI-env ✓) + **verifier adverse contexte frais** (canon `re-review-after-fix` — a rattrapé les 2 FP TIER1 de E1 → revert + le nit couverture) + PR + **CI 6/6 verte** + merge squash + **deploy Hetzner SUCCESS** + **prod health ok/ok/ok** [WebFetch E5 2026-06-13T13:48:28Z]. **DÉCISIONS RÉVERSIBLES Eliot (inchangées)** : (a) anti-spam ≤1/j strict (revert prédicat), (c) index DB partiel (migration prod, go), (b) relire fiches honnêteté + motivation à TA voix Douglas. **RÉSIDUEL backstop ASSUMÉ** : niveaux entiers d'indice nus (« vers 18250 » sans ancre instrument) non captés par le regex — rejoint les FN assumés (conditionnel, synonymes) ; contrôle primaire = system prompt §2 ; option future = whitelist d'instruments. **VERDICT S5 = 100 %** sur DoD §30 + brief §26-30 (0 régression, 0 TIER1 résiduel ; FN backstop = résiduels by-design derrière le system prompt). Détail → mémoire `fxmily_session_2026-06-13_s5_10e_challenge_audit.md`. **PROCHAIN** : Session 6 plan-10.
>
> **MAJ-32 2026-06-13 (Session 5 plan-10 — 13ᵉ challenge : vérif POST-MERGE → 1 TIER2 dans mon propre Jalon F + gate mort, fixés)** : `origin/main` = **`fcb9abd`**. Eliot re-challenge. Méthode calibrée (anti sur-itération) : le 12ᵉ avait déjà fait le sweep exhaustif → 1 SEUL verifier post-merge focalisé sur l'état mergé E/F/E5 (jamais reviewé ENSEMBLE). **0 régression** (full **vitest 2463**, tsc 0, build ✓, anti-leak 79, E5×C snapshot `.strict()` OK, DoD §30 4/4) MAIS **1 TIER2 réel dans MA carte F du 12ᵉ** : la quote motivation faisait **31 mots > cap 30** (fair-use FR L122-5). CAUSE RACINE : `cardUpdateSchema = cardCreateSchema.partial()` **crashait au module-load** (zod refuse `.partial()` sur un ZodEffects refiné) → tout `card.ts` un-importable au runtime → le SEUL gate seed→schema (`validate-cards.ts`) MORT + `cards-posture` ne testait que l'AMF (pas le word-count) → la quote 31-mots a glissé sous TOUS les audits. **Jalon G** (PR #311 `fcb9abd`) : (1) `cardObjectSchema` extrait (non-refiné) → `.partial()` dessus ne crashe plus + refine §2 partagé null-safe sur create+update ; (2) quote raccourcie à 27 mots (≤30, fidèle) ; (3) **ANTI-RÉCIDIVE** : `cards-posture.test.ts` valide désormais CHAQUE carte seed contre `cardCreateSchema.safeParse` complet en CI (word-count/structure/§2) ; `validate-cards.ts` RESSUSCITÉ (53 OK/0 ERR) ; (4) header recompté 53. **Gate** : tsc 0 · vitest cards/admin/schemas/safety/monthly **741** + cards-posture **108** · build CI-env ✓ · verifier adverse frais DÉCISION OK (0 TIER1/TIER2 ; restructure équivalent sur create, crash empiriquement confirmé corrigé) · deploy SUCCESS · **prod health ok/ok/ok** [WebFetch 2026-06-13T15:03:04Z]. **CANON** : un gate qui ne tourne pas (validate-cards crashé) = pas de gate ; un test "posture" qui ne valide qu'un sous-ensemble du schema laisse passer le reste → valider le seed contre le SCHEMA COMPLET en CI. **VERDICT S5 = 100 %** confirmé post-merge (DoD §30 + brief §26-30, 0 régression, gate seed restauré). Décisions réversibles a/b/c inchangées. **PROCHAIN** : Session 6 plan-10.
>
> **MAJ-34 2026-06-13 (Session 6 plan-10 — Fonctionnalités avancées : audit-as-brief + DOD3-01 câblage compteurs S3 dans les rapports — MERGÉ + LIVE prod)** : `origin/main` = **`bfb9807`** (PR #314 squash). Eliot relance la S6 (`06-SESSION-Fonctionnalites-Avancees.md`, édition Fable 5 ; **session conduite sur Opus 4.8** — Eliot l'a explicitement demandé, cohérent avec le pin moteur `claude-opus-4-8` MAJ-25) en « traite tout à 100%, challenge tout » → **audit-as-brief, PAS rebuild** (S6 = ex-Fonctionnalités LIVE MAJ-15→22, jamais auditée comme brief). **Méthode** : reprise §28① first-hand (PROJECT_STATE intégral + git + memory + brief) → workflow audit adversarial **22 agents** (recon 6 axes + critique complétude + vérif réfute-par-défaut PAR finding) → **15 candidats : 7 REAL (tous = facettes DOD3-01 + une subtilité period-scoping), 8 rejetés BY_DESIGN/FALSE_POSITIVE** (la vérif discrimine). **DoD §31 verdict** : **#1** calendrier/questionnaire **PASS** (flux questionnaire→Claude local→calendrier complet, gates §2/AMF, freshness `updatedAt>generatedAt`, overdue net ; édition-bloc membre = BY_DESIGN, brief §30 prescrit le questionnaire) · **#3** guidage quotidien **PASS** (time-aware Paris, §2/anti-Black-Hat ; surface S3 dans le guidage = enrichissement V2 BY_DESIGN, les écarts sont déjà sur le hub /dashboard) · **#4** permanence **PASS** (14 crons monitorés heartbeat, 3 overdue nets, `restart:unless-stopped`+HEALTHCHECK) · **#2 = SEUL gap matériel = DOD3-01** : les pipelines weekly+monthly ne lisaient AUCUN model S3 (0 occurrence `db.constancyScore/discrepancy/alert` hors `lib/verification`) → DoD#2 « rapports intégrant scores de constance & alertes S3 » non tenu (le « consistency » présent = sous-score `BehavioralScore` S2/S5, PAS le `ConstancyScore` dédié S3). **JALON livré (PR #314, 18 fichiers +533/-35, 0 migration, additif)** : 2 helpers read-only count-only — `listConstancyScoresInRange` (`constancy.ts`, **period-scoped** : le ConstancyScore DE la période rapportée, folded par semaine ISO — JAMAIS `getLatestConstancyScore`=semaine courante, qui mal-étiquetterait un rapport rétrospectif) + `countAlertsInRange` (`alerts.ts`) ; câblage `loader→builder(relay verbatim)→schema .strict() verificationSliceSchema→prompt section count-only` sur **weekly (admin)** + **monthly (membre)** ; `constancyScore` (value+breakdown honnêteté/régularité/discipline) + `openDiscrepancyCount` (état COURANT, point-in-time) + `alertCount` (alertes psy de la période). **Posture §2** : count-only (nombres) ; prompts cadrent Mark-Douglas « jamais un avis marché, jamais un drame » ; gate `screenAiOutputText` inchangé sur tout le narratif de sortie ; `Alert.category` enum mono-valeur `psychological` (signal trading structurellement impossible). **Lecture seule** (le pipeline rapport ne recompute jamais ; le cron `verification-scan` possède les writers). **Firewall §21.5 intact** (verification = real-edge read ≠ training ; anti-leak vert). **Gate** : tsc 0 · lint 0 · format LF (CI verte) · **vitest 2592** (+11 relay/render/null) · build OK. **PREUVE RUNTIME RÉELLE** (canon `autonomy_check_real_access` + DoD « testé en réel ») : vrai `claude --print --model claude-opus-4-8 --effort xhigh` sur snapshot honnêteté-basse (constance 42/100, honesty 30, 3 écarts, 2 alertes) → le débrief intègre la constance/honnêteté en posture Mark-Douglas (« le score remonte quand on assume », « regarder ta réalité en face plutôt que la contourner ») **ZÉRO avis marché** → validation `SCHEMA_VALID=true` + `GATE_BLOCKED=false` (passerait le persist). **Reviews adverses (3, contexte frais)** : security-auditor **SHIP-READY 0 T1/2** · verifier **5/5 OK** (period-scoping confirmé first-hand) · code-reviewer **2 TIER2 fixés** (borne basse mensuelle élargie au lundi ISO de la semaine du 1er via `currentPeriodStart` — sinon une 1re semaine partielle serait droppée ; doc `openDiscrepancyCount` clarifiée point-in-time ≠ period-scoped) + **1 TIER3** (JSDoc `pseudonymLabel` orphelin déplacé) ; 2 TIER3 acceptés (loader e2e-covered ; relay par référence pur-style). **MERGÉ + LIVE** : PR #314 squash (PAS `--admin`) → `bfb9807` ; CI 6/6 verte (Lint/tc/build 2m42s + **Playwright chromium 115 passed 4.4m** + CodeQL + Analyze + Socket ×2) ; deploy Hetzner SUCCESS (build+push 2m27s + SSH `pull+up+migrate` 24s, **migrate no-op** car 0 migration) ; **prod health ok/ok/ok** [WebFetch `app.fxmilyapp.com/api/health` 2026-06-13T18:22:07Z : status ok / env ok / db ok]. **OUTWARD-FACING** : le débrief mensuel member-facing commente désormais la constance/honnêteté du membre → ship en autonomie sur canon `feu_vert_total_means_ship` (master-prompt re-posté = autorisation). **CANON** : (1) un score event-sourced PAR PÉRIODE (ConstancyScore/semaine ISO) lu dans un rapport RÉTROSPECTIF exige un read scopé-période, JAMAIS `getLatest*` (=période courante → mauvais score attaché — trouvé par l'audit, pas par les tests verts) ; (2) borne basse d'un mois civil = lundi ISO de la semaine contenant le 1er (sinon 1re semaine partielle perdue) ; (3) `openDiscrepancyCount` = état courant légitime (« encore ouverts »), pas tout doit être period-scopé. **Reste S6 = vraies actions Eliot** (inchangé MAJ-20/27) : 1ᵉʳˢ batchs réels calendrier/mensuel/verification-scan contre les vrais membres prod (human-in-the-loop, ban-risk §5.4) ; enrichissements V2 optionnels (édition de blocs membre A1-03 ; surface S3 dans le guidage quotidien). Détail → mémoire `fxmily_session_2026-06-13_session6_s3_counters_reports.md`. **PROCHAIN** : Session 7 plan-10 (Espace Admin, audit-as-brief) OU 1ᵉʳˢ batchs réels prod (décision Eliot).
>
> **MAJ-34b 2026-06-13 (Session 6 — re-challenge « es-tu sûr 100% » : VÉRIFICATION, 0 ship)** : re-challenge Eliot. Re-audit honnête first-hand → **1 vraie sur-classification trouvée + fermée** (le code était correct, c'était un trou de PREUVE, pas de bug) : mon « DoD#2 testé en réel » (MAJ-34) ne couvrait que le **prompt synthétique** (snapshot construit à la main → `claude --print`), JAMAIS le **LOADER+DB** (les vraies requêtes period-scoped `db.constancyScore/discrepancy/alert`). **Fermé par un harnais Postgres ÉPHÉMÈRE RÉEL** (docker `postgres:17-alpine` + `prisma migrate deploy` 34 migrations) : seed `User`(membre Paris actif) + `ConstancyScore`(periodStart lundi 2026-05-25, value 42.5) + **un LEURRE juin**(value 99, semaine courante) + `Discrepancy`(open) + `Alert`(26/05) → run des **vrais** `loadMonthlySliceForUser`/`loadWeeklySliceForUser` (chemin LIVE batch) → `MONTHLY.verification`+`WEEKLY.verification` = `{constancy 42.5/30/80/45, openDiscrepancyCount 1, alertCount 1}` ; **`NO_DECOY=true`** = le score juin (99) JAMAIS sélectionné par le rapport rétrospectif mai → **period-scoping prouvé sur de VRAIES lignes DB** (plus seulement raisonné). `LOADER_PROOF=PASS` [tool-output]. **CHECK B** : chemin LIVE confirmé — `batch.ts:171/173/229` (monthly) + `:198/200/256` (weekly) fluent `loadXSliceForUser→buildXSnapshot→buildXUserPrompt` via les modules modifiés. **CHECK C** : deploy run `27475065886` head **sha=bfb9807 success** 18:18Z = l'image LIVE contient le code S6 (health 18:22Z post-deploy). **CHECK D (challenge enrichissement §29)** : surfacer le S3 dans le panneau guidage « Ton aujourd'hui » N'EST PAS un gap — violerait le contrat documenté du panneau (« actions time-of-day-sensitive uniquement », délègue le reste aux widgets dédiés anti-CTA-double) ; un écart ouvert n'est pas time-sensitive et est DÉJÀ member-visible (carte Vérification hub) → le promouvoir dans le flux quotidien = **décision PRODUIT** (l'upload MT5 / revue d'écart sont-ils un rituel quotidien ?), pas un oubli technique. **VERDICT** : S6 DoD §31 4/4 confirmé, 0 bug code, DoD#2 désormais prouvé jusqu'au **loader+DB réel** (plus seulement le prompt). 0 ship (code déjà correct + LIVE `bfb9807`). **CANON** : « testé en réel » d'un pipeline DB-backed = exécuter le VRAI loader contre un Postgres réel avec données seedées **+ un LEURRE** pour prouver le scoping, jamais seulement un snapshot synthétique → `claude --print` (qui prouve le prompt, pas la requête DB).
>
> **MAJ-34c 2026-06-13 (Session 6 — re-challenge #3 « 100x plus loin » : progression de constance mensuelle — MERGÉ + LIVE prod)** : `origin/main` = **`aa4b6d6`** (PR #315 squash). Eliot « fais tout, 100x plus loin, remets-toi en question ». PAS une chasse au bug (DoD §31 4/4 + loader DB-prouvé MAJ-34b) → **ENRICHISSEMENT in-scope premiers-principes** : le brief §29/§22-23 veut « voir son évolution », or le débrief mensuel surfaçait la VALEUR courante de constance S3 (MAJ-34) mais AUCUNE progression — le membre ne pouvait pas savoir s'il S'AMÉLIORE en honnêteté/régularité/discipline (`scoreProgression` existe pour BehavioralScore S2, PAS pour le ConstancyScore S3 DÉDIÉ). **JALON livré (PR #315, 6 fichiers monthly-only +187/-23, 0 migration, additif)** : `verification.constancyPrevious` (ConstancyScore DÉDIÉ du mois civil précédent) — loader fait un 2ᵉ read period-scopé du mois -1 → builder relay verbatim → schema `constancySnapshotSchema.nullable()` → prompt rend « Évolution de la constance (vs mois précédent) : globale 70→78 (Δ+8), honnêteté…, régularité…, discipline… » via `formatProgDim` (miroir du scoreProgression comportemental). Count-only §2 ; OMIS si pas de signal mois -1 (pas de faux trend §33.6). Monthly-only (weekly = snapshot, comme weekly n'a pas de scoreProgression). **Reviews adverses (2 contexte frais, CONVERGENTES)** : verifier + code-reviewer ont trouvé le MÊME **TIER1** = chevauchement de bornes ISO (la semaine à cheval mois-1/mois pouvait être lue comme `constancy` ET `constancyPrevious` quand c'est la seule donnée → faux « Δ+0 » fabriqué, violation §33.6 ; absence du guard `previous===current` de buildScoreProgression) → **FIXÉ** : fenêtres DISJOINTES (borne haute mois-1 = borne basse mois courant `−1ms` → semaine-frontière attribuée au mois courant SEUL) ; + **TIER2** arrondi 1-décimale axes+delta (anti bruit float `71.428…`→`71.4`). **Gate** : tsc 0 · lint 0 · **vitest 2595** (+3 progression/float) · build OK. **PREUVE DB RÉELLE** (Postgres éphémère, vrai loader) : `PROGRESSION_PROOF=PASS` (mai vs avril, delta correct, leurre juin rejeté) + `OVERLAP_FIX_PROOF=PASS` (scénario C : semaine-frontière jamais double-lue → lignes distinctes mai/avril ; scénario B : frontière-seule → `constancyPrevious=null`, 0 Δ+0). **MERGÉ + LIVE** : PR #315 squash (PAS `--admin`) → `aa4b6d6` ; CI 6/6 verte (Lint/tc/build 2m38s + Playwright chromium 6m11s + CodeQL + Socket ×2) ; deploy Hetzner SUCCESS (SSH 21s, migrate no-op) ; **prod health ok/ok/ok** [WebFetch `/api/health` 2026-06-13T19:31:38Z]. **OUTWARD-FACING** : le débrief mensuel member-facing dit désormais au membre s'il PROGRESSE en honnêteté/constance → ship autonomie canon `feu_vert_total_means_ship`. **CANONS** : (1) une progression dérivée de DEUX lectures period-scopées doit garantir des fenêtres DISJOINTES (sinon une période-frontière se lit des deux côtés → faux delta nul) — équivalent du guard `previous===current` de buildScoreProgression ; (2) 2 reviewers frais convergents sur le même TIER1 = haute confiance (anti-padding bilatéral) ; (3) arrondir au point d'affichage les fractions brutes (filled/total×100). **Reste S6 = vraies actions Eliot** (inchangé) : 1ᵉʳˢ batchs réels prod. **PROCHAIN** : Session 7 plan-10 (Espace Admin) OU batchs réels. Détail → mémoire `fxmily_session_2026-06-13_session6_s3_counters_reports.md`.
>
> **MAJ-35 2026-06-14 (Session 7 plan-10 — Espace Admin : audit-as-brief + durcissement + re-challenge — MERGÉ + DÉPLOYÉ LIVE prod)** : `origin/main` = **`350a300`** (PR #316 squash, 15 fichiers, +955/-105, 0 migration, additif). Eliot relance la S7 (`07-SESSION-Espace-Admin.md`, édition Fable 5 ; **session conduite sur Opus 4.8** — Eliot l'a explicitement demandé, §10-11 « reste Fable 5 » overridés, cohérent S5/S6) en « traite tout à 100%, challenge tout » → **audit-as-brief, PAS rebuild** (S7 = `🟡 LIVE jamais audité` [:441]). **Reprise §28①** first-hand (PROJECT_STATE intégral + git + memory + brief). **Méthode** : workflow audit adversarial **18 agents** (6 dimensions DoD §29-31 + refute-par-defaut PAR finding) → **9 confirmés / 3 réfutés à juste titre / 1 by-design** (le cap-100 remonté 3× = 1 défaut, dédup). **Socle DoD §31 déjà solide** (admin voit tous les membres + profil/journal/track record + **toutes surfaces S3** ConstancyScore/écarts/alertes ; commentaire réel round-trip OK ; entraînement = carbon-mirror fidèle). **6 fixes + 2 panels livrés** : **(1, headline DoD#1/#2)** liste trades admin était `take:100` sans pagination → **curseur** (miroir exact `listTradesForUser` : `[enteredAt desc,id desc]` + `take limit+1`, footer « Voir les trades plus anciens » + « N au total ») [`trades-service.ts:68-101`] — avant, trades #101+ **indécouvrables/non-commentables** côté admin alors que le membre paginait tout ; **(2, DoD#4 + §22-23)** onglet **Check-ins** était un dead-link (`parseTab` le coerçait en overview, jamais actif) → vrai **panel read-only** [`member-checkins-panel.tsx` + `listMemberCheckinsAsAdmin` capé par JOURS, pas par lignes] + branché parseTab ; **(3, DoD#3 parité)** correction d'**entraînement** = **email immédiat best-effort** (miroir réel `sendAnnotationReceivedEmail`) — avant, membre sans souscription push ne recevait **RIEN** (dispatcher `return` avant le fallback sur `no_subscriptions`) ; template `NotificationFallbackEmail` prop `channel:'primary'` (footer correct, pas « repli après échec push ») ; **(4, §22-23)** panel **Pré-trade** admin read-only (réutilise `PreTradeAnalyticsCard`/`PreTradeCorrelationCard`, async SC userId-scopés) ; **(5, DoD#4)** overflow email hero @375px (`min-w-0`+`break-all`) ; **(6, intégrité S3)** KPI « Comptes » de l'onglet vérif dérive de `overview.accounts.length` (source de vérité, = la liste affichée) au lieu de `detectedAccountCount` (compte IA, `—` jusqu'à analyse, contredisait la liste). **Réfutés à juste titre (NON touchés)** : mediaType `video` (BY_DESIGN forward-compat J4.5), proof-image active-scoping (durcissement), processComplete (atteint déjà l'admin via le sous-score discipline). **Gate** : type-check 0 · lint 0 · prettier clean · **vitest** (2560 full + ciblés 152/72 ; les 2 « failed » du run full = timeouts de worker dus au build lancé en concurrence, prouvés flaky par re-run isolé vert) · **build 0** (`AUTH_URL=https` override — l'échec local sans override = AUTH_URL HTTP refusé en `NODE_ENV=production`, pré-existant [`env.ts:32-35`], non lié au diff ; CI utilise un placeholder HTTPS). **RUNTIME e2e RÉEL** (Postgres dev + Chromium) : `s7-admin-espace.spec.ts` **2/2 PASSED** — pagination réelle (50/page + « 55 au total » + page 2 + vieux trade atteignable) + **round-trip commentaire admin→membre** (annote → « Corrections envoyées » admin → membre /journal/[id] « Corrections reçues ») + onglets Check-ins actif & Pré-trade rendu. **4 reviews adverses fraîches** : security-auditor **SHIP-READY 0 T1/2** · verifier **READY 7/7** (1 finding : footer email-fallback trompeur en canal primaire → fixé prop `channel`) · code-reviewer **READY 0 T1/2** (1 finding : cap check-ins en lignes ampute le soir du jour le plus ancien → fixé cap-en-jours 2 requêtes) · a11y **WCAG 2.2 AA** (contrastes calculés `--ok` 11.62 / `--warn` 11.89 / `--t-4` 5.53 ; 1 finding saut heading h1→h3 → fixé h2/h3). **TOUR 2 (re-challenge « es-tu sûr 100% »)** : au lieu de re-asserter « c'est bon » (canon `challenge_reveals_misclassified_defects`), sondé le NON-vérifié → **3 gaps réels fermés** (additif) : **(a)** `processComplete` (« oublis » §28) jamais affiché par-trade → ajouté dans `TradeDetailView` à côté de plan/hedge (ferme le résiduel S2-MAJ-25(c)) ; **(b, DoD#3)** round-trip correction **entraînement** jamais prouvé e2e → **e2e étendu** (admin /training/[ttId] → membre « Corrections reçues ») ; **(c)** panel checkins testé vide seulement → e2e avec données seedées + **test unit frontière cap-jours**. + label checkins découplé. **2 reviews fraîches** (verifier + code-reviewer) **READY 0 bug** (canon re-review-after-fix). **e2e final 6/6** (3 tests × **chromium + mobile-iphone-15** = DoD#4 responsive 375 prouvé) ; **vitest 2597**. **SHIPPÉ EN AUTONOMIE** : re-post master-prompt + « je t'autorise tout » + re-challenge = autorisation (canon `feu_vert_total_means_ship` ; mon arrêt à la branche au tour 1 = sous-application = anti-pattern « rester sur branche finie »). **MERGÉ + LIVE** : PR #316 squash → `350a300` ; **CI 6/6 verte** (Lint/tc/build 2m54s + Playwright chromium 6m48s + CodeQL + Socket ×2) ; **deploy Hetzner SUCCESS** (build+push 1m59s + SSH 24s, migrate no-op) ; **prod health ok/ok/ok** [WebFetch `app.fxmilyapp.com/api/health` 2026-06-14T07:39:49Z]. **3 résiduels S2-MAJ-25 fermés** : (a) PreTradeCheck surface admin → panel Pré-trade ; (b) onglet Check-ins mort → panel Check-ins ; (c) processComplete par-trade → affiché. **CANONS** : (1) un `next dev` démarré PENDANT des éditions sert des **404 fantômes** sur routes dynamiques (`findUnique` 404 alors que `findMany` marche, même DB) = staleness module/Prisma → **kill+restart avant tout e2e fiable** (prouvé : probe résout 5/5, diag serveur frais → 200+bouton, ancien serveur → 404) ; (2) assert e2e sur un **Sheet de formulaire** = attendre la **fermeture** (`getByLabel().toBeHidden`, timeout long pour la 1ʳᵉ compile d'action serveur) AVANT d'asserter le rendu (le textarea encore rempli + le titre empty-state « Corrections envoyées (0) » = faux positifs) ; (3) `next build` force `NODE_ENV=production` → AUTH_URL HTTP refusé (pré-existant) ; (4) webserver Playwright timeout 120s sur disque D: lent → démarrer `pnpm dev` à la main (`reuseExistingServer`) + `--timeout` généreux. **PROCHAIN** : **Session 8** (Mode Entraînement, audit-as-brief) OU 1ᵉʳˢ batchs réels prod (décision Eliot). Détail → mémoire `fxmily_session_2026-06-14_session7_espace_admin.md`.
>
> **MAJ-36 2026-06-14 (Session 8 plan-10 — Mode Entraînement backtest : audit-as-brief + BUILD du conteneur « session de backtest » + durcissement — MERGÉ + DÉPLOYÉ LIVE prod)** : `origin/main` = **`a661b8a`** (PR #317 squash). Eliot relance la S8 (`08-SESSION-Mode-Entrainement.md`, édition Fable 5 ; **session conduite sur Opus 4.8** — Eliot l'a explicitement demandé, §10-11 « reste Fable 5 » overridés, cohérent S5/S6/S7). Reprise §28① first-hand (PROJECT_STATE intégral + SPEC §21/§23 + schéma + tout le code training + git). **DÉCOUVERTE CENTRALE — S8 ≠ pur audit** : le modèle Prisma `TrainingSession` (`schema.prisma:2620`, commentaire « S8 — crée une session de backtest ») + la FK `TrainingTrade.sessionId` (`:803`) étaient **au schéma ET en DB** (forward-déclarés pendant la S3, commit `698d55f`, migration `20260610155331`) mais **100 % DORMANTS** (0 usage applicatif : `db.trainingSession` = 0 hit hors generated) → **DoD#1 « un membre crée une SESSION DE BACKTEST » NON tenu au runtime**. C'était le cœur non-construit de la S8 (faux signal de complétude : table en DB, jamais peuplée/lue). **BUILD ADDITIF (0 nouvelle table)** : câblage complet du conteneur, miroir des patterns training existants. **9 fichiers neufs** : `lib/schemas/training-session.ts` (Zod, label/symbol(pairs)/timeframe/notes, safeFreeText) · `lib/training/training-session-service.ts` (membre : create/list/getWithTrades/belongsToUser BOLA/end/getMeta) · `training-session-admin-service.ts` (admin scoped memberId) · `app/training/sessions/actions.ts` (create+end, re-auth, audit PII-free, revalidate /training only) · `app/training/sessions/new/page.tsx` · `app/training/sessions/[sessionId]/page.tsx` (détail membre + « Ajouter un backtest » + « Terminer ») · `components/training/{training-session-form,training-session-card,end-training-session-button}.tsx` · `components/admin/member-training-sessions-panel.tsx` · `app/admin/members/[id]/training/sessions/[sessionId]/page.tsx` (détail admin → backtests → annoter). **8 modifiés** : wizard (`training-form-wizard.tsx` : prop `sessionId` + bandeau « Dans la session » + **focus firstMount guard** + **radiogroup Résultat APG roving-tabindex/flèches**) · `app/training/actions.ts` (lit `sessionId`, **ownership BOLA `trainingSessionBelongsToUser`**, redirect→session) · `training-trade-service.ts` (`sessionId?` optionnel, écrit si présent) · `app/training/new/page.tsx` (parse `?sessionId`, drop si ended/foreign) · `app/training/page.tsx` (section « Séances de backtest » + CTA « Nouvelle session ») · `app/admin/members/[id]/page.tsx` (2 panels sessions + rollup corrections) · `lib/admin/training-annotation-service.ts` (`countTrainingAnnotationsByMember` triage admin) · `lib/auth/audit.ts` (2 actions). **§21.5 (DoD#3)** : conteneur 100 % training-isolé (FK `member→User` only, 0 lien real-edge) ; **firewall ÉTENDU** (`BREACH_TOKENS` += `TrainingSession`/`db.trainingSession` + **Block I** : module session n'importe aucun real-edge, ne revalide pas /dashboard, canal d'activité compte TOUJOURS des `TrainingTrade` jamais des sessions). **Migration** `20260614103259_add_training_trade_session_index` : additif `CREATE INDEX training_trades_session_id_entered_at_idx` + réconcilie un **drift pré-existant** `notification_queue` (`ALTER INDEX … RENAME` du partial index J9 hand-named → nom auto attendu par le schéma ; prédicat partiel préservé, vérifié DB `pg_indexes`). **AUDIT adversarial** (workflow **33 agents, 2.17M tokens**, 6 dimensions DoD/brief + refute-par-defaut PAR finding) : 13 confirmés / 14 réfutés-or-by-design — les findings « non câblé / 404 / dormant » ont été **réfutés par les verifiers per-finding** (le build était déjà fait pendant l'audit ; les verifiers confirment « the proposed fix is already shipped verbatim »). **Findings réels fixés** : (T2) firewall TrainingSession gap, (T2) radiogroup Résultat clavier APG, (T2) saut titres /training/debrief/new (h2 sr-only), (T2) **RGPD sweep storage training au purge compte** (`deletion.ts` : balaie `entryScreenshotKey`+`mediaKey` best-effort avant cascade, +test ; ferme une partie du backlog §11), (T3) copy pill « non lue(s) », (T3) focus wizard firstMount, (T3) commentaires lime périmés, (T3) delete-annotation revalidate asymétrie, (T3) **badge corrections admin** (« À corriger »/« N corrections », signal triage), (T3) index session_id. **Backlog (non fait, blast radius/hors-cœur)** : rename `trainingSessionsCount`→`trainingBacktestsCount` (touche le pipeline rapport real-edge), monthly-debrief/{service,overdue,…} non couverts par un bloc firewall. **Gate** : type-check **0** · lint **0** · **vitest 2627/2627** (166 fichiers ; +session-service 13, +actions session 10, +anti-leak Block I 3, +deletion sweep 1, +trade-actions session 3) · **build 0** (routes S8 dans le manifeste ; `AUTH_URL=https` override pré-existant). **RUNTIME e2e RÉEL** (Postgres dev + Chromium + iPhone 15) : `s8-training-sessions.spec.ts` **4/4 PASSED** (2 tests × 2 projets) — **(A, DoD#1+#3)** membre : `/training` → « Nouvelle session » → form → redirect `/training/sessions/[id]` (route EXISTE) → « Ajouter un backtest » → wizard `?sessionId` (upload PNG réel) → submit → backtest **groupé** sous la session + **DB `sessionId` set** + capture `training/{userId}/` + carte landing « 1 backtest dans cette séance » + **`/journal` vide (isolation prouvée)** ; **(B, DoD#2+§27→S7)** admin : onglet training « Séances de backtest » → détail admin (label/instrument/TF/notes + backtest groupé) → annote → membre voit « Corrections reçues ». + `s7-admin-espace` **test E (round-trip training) re-vert** = 0 régression voisin. **Verifier frais** (contexte neuf, §31) : **SHIP-READY, 0 finding** (DoD #1-4 + §21.5 + BOLA membre&admin + RGPD + migration + 0 régression, tout file:line ; a re-run tsc/eslint/vitest indépendamment). **DoD §31 verdict** : #1 **PASS** (session + trade dedans, e2e) · #2 **PASS** (round-trip e2e) · #3 **PASS** (firewall + /journal vide + audit 0 fuite) · #4 **PASS** (e2e mobile 375 + a11y radiogroup/heading/focus fixés). **Interconnexion §27** : entrées **S1** (auth gate /training/sessions/\*, storage, db) / **S4** (wizard miroir, ScreenshotUploader) / **S9** (DS-v2 cyan) intactes ; sortie **S7** (admin voit + corrige les sessions) livrée. **SHIPPÉ EN AUTONOMIE** (Eliot : « ship complet en autonomie ») : commit `f218722` → branche `feat/session8-training-sessions` → **PR #317** → **CI 6/6 verte** (Lint/tc/build 3m1s + **Playwright chromium 7m46s incl. le nouveau `s8-training-sessions` 4/4** + CodeQL + Socket ×2) → squash **`a661b8a`** → **deploy Hetzner SUCCESS** (run 27499773457 ; `migrate deploy` = index session_id + rename `notification_queue` appliqués prod) → **prod health ok/ok/ok** [WebFetch `app.fxmilyapp.com/api/health` 2026-06-14T13:08:45Z, db:ok]. Le `s8-training-sessions` **PASSE en CI** (env frais) = la flakiness locale était bien environnementale (cold-compile D: + cache `.next` pollué). **Notes infra** : DB dev resyncée propre (`migrate reset`, 35 migrations) ; cache `.next` dev déplacé `apps/web/.next-trash-s8-2` (à nettoyer) ; serveur MCP `playwright` tué par le cleanup de process (reconnecte au prochain usage). **CANONS** : (1) un modèle **forward-déclaré au schéma sans câblage applicatif** = faux signal de complétude (« fait » au schéma, mort au runtime) → grep `db.<model>` pour distinguer ; (2) un **audit lancé pendant un build en cours** produit des findings périmés que les verifiers per-finding réfutent (bonus : confirme que le build implémente exactement les fixes proposés) ; (3) **cold-compile `next dev` sur disque D: lent** → warm les routes (fetch localhost) AVANT l'e2e sinon nav timeout 30s (les « 404 fantômes » du canon MAJ-35 étaient en réalité du cold-compile + pollution `.next` multi-serveurs) ; (4) le **rate-limiter login en mémoire serveur** s'accumule à travers les runs e2e répétés (le `syntheticCallerSeq` repart à 1 par run) → restart du dev server entre campagnes ; (5) un **index partiel hand-named** (Prisma 7 ne modélise pas les partiels) doit porter `map:` au schéma, sinon `migrate dev` veut le renommer en boucle. **TOUR 2 (re-challenge « es-tu sûr 100% » post-ship)** — au lieu de ré-asserter (canon `challenge_reveals_misclassified_defects`), sondé le non-vérifié → **2 vrais défauts + 1 trou de process fermés** (PR #318 squash **`8f4ae7a`**, deploy SUCCESS, **health ok/ok/ok** [WebFetch 2026-06-14T14:18:04Z], 0 migration) : **(B, §22 honnêteté)** le compteur d'entraînement (un nb de **backtests**) était rendu « N session(s) » dans le débrief **mensuel member-facing** (`monthly-debrief/prompt.ts:179`) + le rapport **hebdo admin** (`weekly-report/prompt.ts:147`) — ambigu/faux depuis que `TrainingSession` est une vraie entité (5 backtests peuvent vivre dans 2 sessions) → libellé « session »→« backtest » (champ interne inchangé ; le verifier d5 du tour-1 avait conclu à tort « aucun membre ne lit N sessions », **ratant le prompt mensuel member-facing**) ; **(C, robustesse/BOLA)** `createTrainingTradeAction` rejette désormais une session **étrangère OU TERMINÉE** via `getTrainingSessionMeta` (cohérent avec `/training/new` qui drop déjà ended/foreign) + retrait du `trainingSessionBelongsToUser` devenu mort + test ended ; **(A, process)** **exécuté le gate frontend-elite `runtime-audit.js`** (qui avait été substitué par l'e2e au tour-1) sur les 4 surfaces S8 desktop+mobile → **0 fail** (0 overflow, contraste WCAG AA calculé incl. pills cyan/CTA/« À corriger ») + **0 erreur console** ; warnings résiduels = patterns DS app-wide pré-existants (eyebrow 10px/cap 11px, live-dot pulse reduced-motion-safe, pas de forced-colors, back-links inline <24px exemptés par espacement). Gate tour-2 : tc 0 · lint 0 · **vitest 2627** · build 0 · e2e s8 2/2. **CANON tour-2** : un re-challenge post-ship trouve encore de vrais défauts (wording member-facing + edge-case guard) — ne jamais ré-asserter « c'est bon », sonder le non-vérifié (le runtime gate explicite > substitution par l'e2e). **PROCHAIN** : **Session 9** (Frontend/design system, audit-as-brief — dont la dette contraste `--acc` ~44 fichiers) OU 1ᵉʳˢ batchs réels prod (décision Eliot). Détail → mémoire `fxmily_session_2026-06-14_session8_mode_entrainement.md`.
>
> **MAJ-37 2026-06-14 (Session 9 plan-10 — Frontend ultra-premium : audit-as-brief + durcissement a11y/contraste/hydration — MERGÉ + DÉPLOYÉ LIVE prod)** : `origin/main` = **`fa4d15f`** (PR #319 squash) ; **CI 100% verte** (Lint/build + **Playwright e2e 7m5s** + CodeQL + Socket), **deploy Hetzner SUCCESS** (Docker build+push GHCR 1m54s + SSH compose pull/up/migrate 27s, migrate no-op 0 migration), **prod health ok/ok/ok** [WebFetch `app.fxmilyapp.com/api/health` 2026-06-14T20:38:52Z : status ok / env ok / db ok]. Branche `feat/session9-frontend-premium` (depuis `main`=`af4ffdc`). **Session conduite sur Opus 4.8** (Eliot explicite, §10-11 Fable-5 overridés, cohérent S5-S8). **Scope TRANCHÉ = audit-as-brief, PAS rebuild** (canon §7/§18.4 : app LIVE prod + DS-v3 mature ; un teardown casserait S1-S8 et viole « ne dégrade rien » §20). État réel découvert : S9 avait déjà été auditée (47 findings) + batch 1 WIP non commité. **PRODUIT (11 fichiers, additif, 0 migration)** : (a) **bug HYDRATION réel corrigé** — `trade-form-wizard.tsx`+`training-form-wizard.tsx` : `enteredAt:''` au SSR + remplissage `nowIsoLocal()` post-mount (canon `close-trade-form`) → supprime le mismatch `value` datetime-local (SSR-UTC≠client-Paris) ; (b) **contraste WCAG AA** : `pill`/`profile-status-widget`/`library-badge`/`info-dot` `--acc`→`--acc-hi` ; `sample-size-disclaimer` (warn-hi) + `empty-state` tip (`--t-1`) ; (c) **bloc `@media (forced-colors: active)`** (focus Highlight + glow-edge/[selected] outline + masque orbs) — unlayered, bat les utilities (backstop des 16+ `outline-none` ring) ; (d) `info-dot` hit-area 14→24px (WCAG 2.5.8, `-m-1.5`) ; (e) `score-gauge` halo « Excellent » réparé (`glow` box-shadow-token→couleur) ; (f) aria-labels inputs sport check-in matin ; (g) `--color-acc-dim` au `@theme` (réactive `bg-acc-dim` qui était une **classe morte** → **9 pages library/admin passent fond transparent→bleu-dim** : validation VISUELLE recommandée) ; (h) eslint ignore `.next-trash*`. **CAUSE RACINE majeure** : les classes typo **unlayered** `.t-cap`/`.t-mono-cap`/`.t-eyebrow` (`color:var(--t-3)`, définies APRÈS la fermeture de `@layer base` l.342) **écrasent** les utilitaires `text-[var(...)]` (cascade : unlayered > `@layer utilities`) → 2 fixes couleur d'abord INOPÉRANTS (rendu `--t-3` rgb(122,130,142) mesuré `getComputedStyle`, pas du stale), **contournés** en retirant la classe typo des 2 spans + reproduisant la typo en utilitaires (`text-[11px] leading-[1.45]` / `font-mono text-[11px] tabular-nums`). **VALIDATION RUNTIME RÉELLE** (spec `apps/web/tests/e2e/session9-runtime-audit.spec.ts` : loginAs seed-j6-demo admin, `runtime-audit.js` sur **11 routes × desktop1440+mobile390**, `rm .next` + reproduit 2×) : **22/22 PASS, 0 console error, 0 overflow, WCAG AA** (warn-hi **9.91:1**, t-1 **14.46:1** mesurés canvas ; hydration clean `/journal/new`+`/training/new` infinite-anim:0 ; forced-colors/reduced-motion/focus-visible présents partout). **Gate** : type-check **0** + lint **0** sur le code app (les erreurs résiduelles vivaient dans la spec de test = artefact non-commité) ; **build = CI au merge** (piège `AUTH_URL=https` local pré-existant + conflit dev server). **3 reviews fraîches** : code-reviewer READY-TO-MERGE (hydration verifié correct, 0 critical, 1 IMPORTANT = `bg-acc-dim` visuel) · accessibility-reviewer **0 BLOCKER/SERIOUS** (contrastes calculés, forced-colors backstop prouvé efficace via cascade) · subagent validation runtime (mesure couleur effective = preuve de l'override cascade). **Reframing dette** : la « dette `--acc` ~44 fichiers » (brief/backlog §11) était **surestimée** — le runtime n'a trouvé que **2** contrastes réels, tous fermés. **CONTRATS §27 (Sortie → S4/S6/S7/S8)** : DS = tokens `globals.css` INCHANGÉS en sémantique (ajouts additifs `--color-acc-dim` + bloc forced-colors) ; **aucune classe DS supprimée** ; toutes surfaces consomment sans friction (22/22, 0 bug). **RESTE** : (1) **wave « wow » additive** (célébrations streak 7/14/30 + 1er check-in, login aurora+orb, illustrations animées maison) = cadrée backlog avec recherche premium 2026 sourcée (compositor-only `box-shadow`→`::after` opacity/scale, `MotionConfig reducedMotion="user"`, orbs `transform` pas `background-position`, `canvas-confetti disableForReducedMotion`, View Transitions same-document Baseline, scroll-driven `@supports`) — **prochain incrément S9.1** ; (2) nettoyer la spec de test (5 `any` + 2 type) si commit en CI souhaité (sinon artefact local réutilisable) ; (3) **backlog DS systémique** : les classes typo unlayered écrasent les utilities `text-[var()]` app-wide (pattern latent — beaucoup de `t-cap text-[var(--x)]` rendent en réalité `--t-3`) → jalon DS dédié (déplacer ces classes en `@layer base`, blast radius app-wide à tester). **CHECKPOINT Eliot (outward-facing, prod live)** : merge+deploy = sa décision ; suppression trash `.next-trash-s8-2` 9.2 GB = sa décision ; valider visuellement les 9 pages où `bg-acc-dim` est réactivé. **CANONS** : (1) classes CSS **unlayered** battent les utilitaires Tailwind layered (vérifier `@layer` avant de croire qu'un `text-[var()]` peint) ; (2) **Tailwind v4 scanne le texte BRUT incl. commentaires** — un token littéral `text-[var(...)]` dans un commentaire génère un utility CSS invalide (`var(...)`) qui casse tout `globals.css` → **toutes routes HTTP 500** ; (3) le dev server Turbopack sert du **CSS/render STALE** même après kill+restart → SEUL `rm .next` + rebuild garantit la fraîcheur (mesurer `getComputedStyle` réel, pas calculer) ; (4) bug grep Git-Bash convertit `/route` en chemin Windows (`MSYS_NO_PATHCONV=1`). Détail → mémoire `fxmily_session_2026-06-14_session9_frontend.md`.
>
> **MAJ-38 2026-06-15 (Session 9.1 — « wave wow » frontend : animations premium additives — MERGÉ + DÉPLOYÉ LIVE prod)** : `origin/main` = **`1c61297`** (PR #320 squash). Jalon additif séparé (depuis `fa4d15f`), **Opus 4.8**, orchestré via subagent senior en contexte frais puis vérifié indépendamment. **Produit** : (A) **login aurora** premium (3 orbes dérivants `transform`-only + entrée form stagger) ; (B) **célébration 1er check-in** non-toxique (spring + halo bleu, `countCheckins===1`, copy Mark Douglas « tu as posé ta routine », 0 score/FOMO, dismissible) ; (C) **paliers de série** 7/14/30/100 (`crossedMilestone` SSOT dans `streak.ts` + 4 tests) ; (D) **dashboard first-run** (encart d'accueil membre 0-trade) ; (E) micro-interactions hover-lift + scroll-reveal `@supports`. **globals.css +243 lignes** (keyframes `transform`/`opacity` only, box-shadow peint une fois, bloc `forced-colors` étendu). **framer-motion v12, 0 nouvelle dépendance**, additif, 0 migration, login form intact. **Vérif** : verifier indépendant adversarial **READY 0 CRITICAL** (compositor-only 60fps + reduced-motion triple-filet + forced-colors + posture Douglas vérifiés LIGNE PAR LIGNE, tsc/eslint/vitest first-hand) ; polish (JSDoc honnête, palier 100, SSOT+tests). **2 RÉGRESSIONS e2e attrapées par la CI** (le verifier n'avait lancé que vitest, pas la suite e2e projet) : (1) célébration `role=status` concurrent décalait `getByRole('status').first()` → `485d743` ; (2) célébration **REMPLAÇAIT** le `DoneBanner` au 1er check-in → « Check-in matin enregistré · streak N » masqué, cassait l'e2e J5 → `3c58819` (DoneBanner TOUJOURS rendu, célébration additive au-dessus). **Gate** : type-check 0 · lint 0 · build 0 · **vitest 57 checkin** (streak 18) ; **CI 100% verte** (Lint/build + **Playwright e2e 7m55s PASS = 0 régression** + CodeQL + Socket) ; **deploy Hetzner SUCCESS** (Docker build+push + SSH compose pull/up/migrate 26s) ; **prod health ok/ok/ok** [WebFetch `app.fxmilyapp.com/api/health` 2026-06-14T22:24:45Z]. **CANON CAPITAL** : un verifier qui ne lance pas la **SUITE E2E COMPLÈTE** du projet rate (a) les **collisions DOM** (nouveau `role`/`aria` décalant un sélecteur `.first()` existant) et (b) les **remplacements de surface** (ternaire retirant un élément attendu) ; runtime-audit (contraste/overflow) ne couvre PAS la sémantique/texte des assertions e2e → la **CI e2e est le filet final indispensable**. Spec `session9-runtime-audit.spec.ts` reste artefact LOCAL (non commitée — follow-up CI si voulu). **Reste S9/S9.1** : aucun gap connu ; backlog DS systémique (classes typo unlayered, jalon dédié) inchangé. Détail → mémoire `fxmily_session_2026-06-14_session9_frontend.md`.
>
> **MAJ-39 2026-06-15 (Session 9 — re-challenge « es-tu sûr 100% » : audit runtime EXHAUSTIF mobile→ultra-wide + fermeture §245/§243 — MERGÉ + DÉPLOYÉ LIVE prod)** : `origin/main` = **`02fc11c`** (PR #321 squash). **Opus 4.8** (Fable-5 §10-11 overridés, cohérent S5-S8). Reprise §28① first-hand (PROJECT_STATE 498l + brief 09-SESSION + globals.css + shell complet + git). **Décision = EXÉCUTER le re-challenge §13.4#6** (« S6-S9 risque de faux done ») au lieu d'acquérir sur la foi. **GAP RÉEL TROUVÉ** : l'audit S9 (MAJ-37) ne couvrait que **11 routes × {390,1440}** → **ultra-wide jamais prouvé** + **breakpoint tablette 768 jamais testé** + critère §245 « prend tout l'écran » non mesuré. Construit `session9-fullscreen-audit.spec.ts` (**39 routes publiques+auth × 4 viewports {390,768,1440,2560}** + métrique full-bleed §245, loginAs seed-j6-demo admin) → 1ᵉʳ run **39 passed / 3 FAIL = 3 vrais bugs responsive invisibles avant**. **PRODUIT (8 fichiers, additif, 0 migration)** : (a) **§245 app-wide ambient backplate** — `globals.css .app-ambient` (`position:fixed z-index:-1` mesh deep-space oklch edge-biased) rendu **1×** dans `layout.tsx` ; **par construction, 0 double-aurora** : les pages à `<main>` bg-opaque + hero ambient (dashboard/library/login/reflect/mindset/training-debrief) le RECOUVRENT, les pages à `<main>` transparent (forms/journal/admin) le RÉVÈLENT dans leurs gouttières → **`shell=2560px (1× iw)` mesuré sur les 39 routes** = plus aucun letterbox plat (CSS bâtie prod vérifiée : `.app-ambient{z-index:-1;…background:radial-gradient(…#1485f414…)}`) ; (b) **§243 cookie/FAB no-overlap** — `cookie-banner.tsx` `data-slot="cookie-banner"` + règle `globals.css @media(max-width:815px) body:has([data-slot=log-express-fab]) [data-slot=cookie-banner]{bottom:max(5.5rem,calc(env(safe-area-inset-bottom)+4.75rem))}` (les 2 sont `fixed bottom z-40`, géométrie d'overlap vw<816) → **0 intersection de bounding-box PROUVÉE à 390 ET 768** (`banner.bottom` 12px→88px, FAB y768-824 vs banner y544-756) ; (c) **overflow tablette 768** — `pre-trade/new`+`mindset/new`+`calendar/questionnaire/new`+`onboarding/interview/new` manquaient `sm:px-6` → leur **barre CTA sticky `sm:-mx-6`** débordait de 8px (776>768) quand le conteneur `max-w-3xl/2xl` remplit le viewport (à 390 `sm:` inactif, à ≥1440 conteneur centré+gouttière → OK) ; +`sm:px-6` aligne barre/page (cf. reflect/review déjà corrects) ; (d) **overflow mobile 390** — `/admin/system` note footer `<p className="flex items-center">` = phrase+2`<code>` sur **1 ligne non-wrappable** → 481>390 ; restructuré `items-start` + icône `shrink-0` + texte dans `<span>` wrappable. **VALIDATION RUNTIME RÉELLE** (canon §21) : re-audit des 3 routes FAIL → **PASS sur les 4 viewports** (0 overflow, 0 console error, WCAG AA, structure) [tool-output reaudit] ; assertion géométrique cookie/FAB **2/2** ; screenshots ultra-wide dashboard/journal (gouttières ambient, 0 double-aurora). **Gate** : type-check **0** · lint **0** · **build 0** (override `AUTH_URL=https` CI-style) · CSS prod bâtie contient `.app-ambient` + règle `:has`. **CI 100% verte** (Lint/build 2m58s + **Playwright e2e 7m37s PASS = 0 régression** + CodeQL + Socket) ; **deploy Hetzner SUCCESS 2m35s** ; **prod health ok/ok/ok** [WebFetch `app.fxmilyapp.com/api/health` 2026-06-15T00:55:09Z]. **CANON RÉ-CONFIRMÉ** (MAJ-37 #3) : **Turbopack HMR sert du CSS STALE** — la règle `:has` cookie était sur disque (`globals.css:1271`) mais ABSENTE du CSSOM après restart (sonde CSSOM probante), réveillée seulement par un re-poke du fichier ; `sm:px-6` (utility déjà cachée par reflect/new) appliquée vs une 1ʳᵉ tentative `break-all` (de toute façon mauvaise cible). **`rm .next`/`Remove-Item .next` BLOQUÉS par garde-fou destructif** (cache régénérable) → validation finale = **build prod (CSS déterministe) + CI e2e**, pas le dev HMR. **CONTRATS §27 (Sortie→S4/S6/S7/S8)** : DS sémantique INCHANGÉE (ajouts additifs `.app-ambient` + 1 règle `:has` + `sm:px-6`/structure de 5 pages) ; **DoD §32 4/4 PROUVÉ runtime** : #1 plein écran mobile→ultra-wide (shell=2560 sur 39 routes) · #2 0 chevauchement tous breakpoints (overlap géom + 3 overflows fermés, 4 viewports) · #3 anim 60fps compositor-only/DS premium cohérent (audit motion ok + agent ui-designer) · #4 DS consommé sans friction S4/S6/S7/S8 (agent researcher : CLEAN, 0 lime app, v18 confiné). **RESTE (déféré, documenté, NON-bloquant, NON-bug)** : (1) **jalon DS systémique** classes typo unlayered → `@layer` (inchangé, blast radius app-wide à tester) ; (2) **surface admin = la plus tiède** (tokens legacy/`rounded-lg` vs `Pill`/`HoverLift`/`.t-h3` — fonctionnelle+a maintenant l'ambient) → reskin premium possible ; (3) couche shadcn résiduelle (Dialog/Sheet/Tabs `transition-all` + focus ring legacy vs `--ring`) ; (4) drifts tokens MED (cyan littéral vs `--cy-edge-soft`, `--ok` drop-shadow inline, hex `drawn-rule.tsx`) ; (5) **emails encore lime DS-v2** (hors-scope « frontend web app » mais désaligné marque). **Specs audit = artefacts LOCAUX** (`session9-fullscreen-audit`/`-screenshots`/`-overlap-check`/`-css-probe`, référencent `runtime-audit.js` hors-repo → casserait la CI ; précédent MAJ-38). **CANON NOUVEAU** : un audit runtime élargi en **viewports** (ajouter 768 tablette + 2560 ultra-wide) ET en **routes** (toutes les archétypes layout, pas un échantillon) trouve des bugs responsive qu'un audit 2-viewports rate — l'ultra-wide et la tablette sont des angles morts classiques. Détail → mémoire `fxmily_session_2026-06-15_session9_fullscreen_audit.md`.
>
> **MAJ-40 2026-06-15 (Session 9 — re-challenge tour-2 « es-tu sûr 100% » : ÉLÉVATION PREMIUM — reskin admin DS-v3 + gaps hover §30 — MERGÉ + DÉPLOYÉ LIVE prod)** : `origin/main` = **`ce068a0`** (PR #323 squash). **Opus 4.8**. Re-post master-prompt → canon MAJ-36 tour-2 (ne PAS ré-asserter « c'est bon », sonder le non-vérifié). **VERDICT HONNÊTE** : MAJ-39 a fermé §245/responsive mais **DÉFÉRÉ l'élévation premium** que le brief §30 exige centralement (« chaque module poussé à la plus haute qualité, **mouvement au survol** »). **3 sub-agents parallèles** : (a) admin reskin → drift confiné à **exactement 5 fichiers** (reste de l'admin déjà DS-v3) ; (b) risques responsive routes non testées → **0 BLOCKER/HIGH**, les 2 wizards fixés (calendar/onboarding) confirmés barre/page cohérents ; (c) gaps hover §30 → SlotCard check-in + 2 listes nues + `.celebrate-pop` dormante. **ANTI-FAUX-DONE** : `session9-untested-audit.spec.ts` (14 routes : 2 wizards fixés + dynamiques `[id]` journal/admin + statiques jamais auditées) = **14/14 PASS** → **0 bug responsive caché** (tour-1 confirmé complet côté responsive). **PRODUIT (8 fichiers, additif, 0 migration)** : (a) **reskin admin DS-v3** (`member-row`/`member-trades-list`/`member-douglas-panel`/`member-tabs`/`card-actions-row`) — alias shadcn bruts (`bg-card`/`text-muted`/`text-foreground`/`rounded-lg`/`transition-all`) → tokens sémantiques (`--bg-1`/`--b-*`/`--t-*`), `rounded-card`, `<Pill>` partagé (Admin/Suspendu/Long/Short/Ouvert), typo `.t-h3`/`.t-eyebrow`/`.t-cap`, **`HoverLift`** (spring calm 1.02+2px, reduced-motion-safe) sur les rows cliquables = le « mouvement au survol » §30 ; (b) **gaps hover member** : check-in `SlotCard` (action quotidienne #1) → `HoverLift` ; lignes DouglasInbox + MonthlyDebrief → `.wow-hover-glow`. **1 BUG INTRODUIT + ATTRAPÉ PAR LE GATE type-check** : commentaire JSDoc `(--bg-1/--b-*/--t-*)` contenait la séquence `*/` qui **fermait le bloc commentaire** → tout le reste parsé comme code (TS1005) ; corrigé (slashes→virgules, member-row.tsx). **VALIDATION RUNTIME** : `session9-reskin-audit.spec.ts` **8/8 routes PASS × 4 viewports** (admin members/detail/cards + checkin + dashboard + debrief-mensuel), **0 console error** (pas d'hydration mismatch du `HoverLift` client dans les listes server), 0 overflow, WCAG AA ; screenshot admin/members confirme le premium (cards rounded + Pill badges bleus + typo DS vs shadcn brut « tiède »). **Gate** : type-check **0** · lint **0** · **CI 100% verte** (Lint/build 2m49s + **Playwright e2e 7m34s PASS = 0 régression** + CodeQL + Socket) ; **deploy Hetzner SUCCESS 2m42s** ; **prod health ok/ok/ok** [WebFetch `app.fxmilyapp.com/api/health` 2026-06-15T08:28:54Z]. **CONTRATS §27** : DS sémantique INCHANGÉE (consomme les primitives existantes Pill/HoverLift/`.wow-hover-glow`/tokens, **0 nouvelle classe**) ; **posture Mark Douglas non-toxique préservée** (HoverLift calm, pas de FOMO/pulsing/badge ; les surfaces statiques-par-choix §27.4 — MindsetTimeline/HabitCorrelation/TrainingTradeCard — laissées intactes). **CANON tour-2** : un re-challenge post-ship trouve encore du vrai travail — pas un BUG cette fois (le responsive était complet, prouvé 14/14) mais une **dimension DÉFÉRÉE du brief** (premium §30) ; sonder le non-vérifié bat ré-asserter. **RESTE (déféré, NON-bug, choix Eliot)** : jalon DS `@layer` typo-unlayered (inchangé) · `.celebrate-pop` dormante (câbler sur checkmark check-in OU supprimer) · couche shadcn modale (Dialog/Sheet/Tabs) · 8 panels admin non-cités par grep (0 drift détecté, lecture exhaustive non faite) · emails lime DS-v2. Détail → mémoire `fxmily_session_2026-06-15_session9_fullscreen_audit.md`.
>
> **MAJ-41 2026-06-15 (Session 10 — INTERCONNEXION GLOBALE + VALIDATION RÉELLE + LIVRAISON : suite E2E cross-session NEUVE + fix monitoring permanence — committée branche `feat/session10-interconnexion-validation`)** : commit **`d0050919`** (Opus 4.8, directives Fable5 §10/§11 du brief neutralisées sur ordre explicite Eliot). **Session finale du plan-10** : S1–S9 toutes DONE+LIVE (cf. §13.1) → S10 = assembler/souder/valider/livrer, **0 nouvelle feature member-facing**. **Reprise contexte §28①** : PROJECT_STATE intégral + 3 sub-agents researcher parallèles (état MAJ-24→40 · carte des flux d'interconnexion dans le code réel · inventaire exhaustif routes/écrans/API/crons/e2e) + brief S3/S10 + mémoire D--Fxmily. **MÉTHODE** : baseline gates → cartographie → **workflow d'audit adverse 13 agents / 10 dimensions** (chaque finding REAL_DEFECT réfuté par un sceptique indépendant) → **suite E2E cross-session NEUVE** (le seul vrai trou S10 : « suite E2E cross-session absente ») → fix → re-gates → hand-off. **BASELINE + RE-GATES (runtime réel, [tool-output])** : type-check **0** · lint **0** · **vitest 2631/2631** (166 fichiers) · build prod **0**. **LIVRÉ — 4 specs E2E `tests/e2e/session10-*` (Postgres réel + UI réelle, +775 lignes)** : (1) **`anti-mensonge-chain`** = DoD §30 #2, la couche d'intégration manquante (les cœurs S3 n'étaient unit-testés qu'en db mockée) : seed (2 BrokerAccount + 1 trade honnête + 1 ExtractedPosition match + 1 position non déclarée + 2 trades fabriqués) → **POST réel `/api/cron/verification-scan`** → asserte en DB **1 missing + 2 false discrepancies + ScoreEvents (−3 / −8×2) + ConstancyScore effondré (honesty < 20) + Alert `false_declaration_repeat` `delivered` + MarkDouglasDelivery `sourceAlertId` (S3→S5) + trade honnête flippé `mt5_verified`** → puis MEMBRE voit sa réalité `/verification` (S3→S4, posture §2, jamais « vérifié 100% ») + ADMIN voit la vérité `?tab=verification` (S3→S7) — **3/3 PASS** ; (2) **`member-journey`** = DoD §30 #1 : ~37 surfaces membre en série, **0 rupture / 0 overlay Next / 0 pageerror** (couvre les trous jamais testés : `/debrief-mensuel`, `/library*`, `/track/*`, `/account/data`, `/account/delete`) — **PASS** ; (3) **`admin-journey`** = DoD §30 #1 : toutes surfaces admin (couvre `/admin/reports`, `/cards`, `/reunions`, `/system`) + **isolation des rôles** (un membre sur `/admin/*` est éjecté) — **PASS** ; (4) **`cron-permanence`** = DoD §30 #3 : **les 16 crons protégés** rejettent POST sans/mauvais secret (401/503) + GET (405), `/api/health` public 200/503 — **17/17 PASS**. **AUDIT ADVERSE — verdict** : système **très sain**, **0 TIER1, 0 rupture**. Toutes les interconnexions §27 confirmées **CÂBLÉES + RENDUES** (S3→S4 dashboard/`/verification` · S3→S5 alertes→coaching · S3→S6 ConstancyScore+alertes dans weekly/monthly · S3→S7 admin) ; tous les axes S2 consommés par le scoring ; 16 crons gated+rate-limited+heartbeat ; isolation auth solide. **2 défauts DOWNGRADED** : (a) `DailyCheckin.hedgeRespectedToday` non consommé par le scoring **MAIS** lu par weekly/monthly loaders + panneau admin = **report-only by design** (SPEC mande le hedge-respect au niveau _trade_ via `Trade.hedgeRespected`, pas le bilan du soir) — **non un bug**, copie wizard vérifiée non-trompeuse ; (b) **3 crons prod émettaient un heartbeat mais étaient ABSENTS de `health.ts` EXPECTATIONS** → non monitorés (panne silencieuse jamais rouge). **FIX (code prod, additif)** : `health.ts` **EXPECTATIONS 13 → 16** + union type (`meeting.generated` generate-meetings weekday tol×4 · `cron.mindset_check_reminders.scan` · `cron.purge_access_requests.scan`) + `health.test.ts` (7/7) → **les 16 crons wired sont désormais monitorés** (ferme un vrai trou de permanence/RGPD : une panne de `purge-access-requests` laissait s'accumuler des PII non-membres sans alerte). Finding « confusion Cohérence/constance » dashboard = **non matérialisé** (aucun label « Cohérence » sur le dashboard ; seul « constance » = ConstancyScore S3 ; les 2 moteurs sont disjoints par design). **DoD §30 SESSION 10 = SATISFAIT (runtime réel)** : #1 parcours BE membre+admin ✓ · #2 interconnexions S1-9 + chaîne anti-mensonge ✓ · #3 0 bug + stable **après redémarrage** (app tuée+relancée proprement, gates+e2e verts) ✓ · #4 bilan débutant ✓. **CANONS S10** : (1) valider une chaîne human-in-the-loop (cron secret-gated) en e2e = **injecter `CRON_SECRET` dans le dev server ET le worker Playwright** (`CRON_SECRET=… PLAYWRIGHT_NO_SERVER=1`) — en dev `.env` il est absent donc les crons sont à 503 par défaut ; (2) un **sweep de routes** (goto + assert `not /login` + 0 `[data-nextjs-dialog-overlay]` + 0 pageerror) attrape en UN spec une régression de N'IMPORTE quelle session 1-9 ; (3) `meeting.generated` est un slug heartbeat **intentionnel** (audit.ts:312) — l'ajouter à EXPECTATIONS, ne PAS le renommer (casserait route.test + doc). **LIMITES honnêtes** : le spec `anti-mensonge` **SKIP en CI** (pas de `CRON_SECRET` dans l'env CI placeholder) → prouvé **localement** avec secret injecté + couvert par les unit tests des cœurs ; les journeys ont `test.setTimeout(600_000)` (cold-compile `next dev` sur D: / CI). **RESTE / LIVRAISON (action Eliot)** : (a) **merge PR + deploy Hetzner** de `feat/session10-interconnexion-validation` (CI doit être verte ; changements = tests + monitoring additif, **0 member-facing**) ; (b) décisions Eliot inchangées de §13.4 : **1ers batchs RÉELS prod** calendrier/mensuel/vision (human-in-the-loop §5.4, jamais lancés) ; (c) backlog DS `@layer` / emails lime DS-v2 (NON-bug). **Le projet FXmily Suivi est validé bout-en-bout en runtime réel et prêt à livrer.** Détail → mémoire `fxmily_session_2026-06-15_session10_interconnexion_validation.md`.
>
> **MAJ-10 2026-06-07 (Session 3 — Espace membre : audit + durcissement, branche non mergée→MERGÉE cf MAJ-11)** : `origin/main` inchangé (`204564f`) — travail sur branche `feat/session3-espace-membre` (worktree), **0 migration, 0 schema change**, prête PR. **Audit profond** (brief `03-SESSION-Espace-Membre.md`, workflow 16 sous-agents : 5 axes DoD §30 + vérification adverse par finding → 20 findings, **11 confirmés**). **Verdict DoD §30** : **#1** (track record + journal + trade + photo) **PASS** — `/journal` new/[id]/close + wizard 6 étapes « Capture avant entrée » (`screenshotEntryKey` requis Zod + gate UI) + uploader magic-byte/BOLA ; track record `/dashboard` equity/R-dist/expectancy/DD/streaks/émotion×outcome/sessions/paires ; **#3** (commentaires admin) **PASS** — `TradeAnnotation` « Corrections reçues » rendues `/journal/[id]` + badges non-lus (`AdminNote` reste privé) ; **#4** (responsive 0 bug) **PASS** — a11y/responsive très élevé, seulement TIER3 nits ; **#2** (débriefs + alertes dérive) **PARTIEL** : mensuel `/debrief-mensuel` **PASS** ; **débrief hebdo MEMBRE = différé V2 par SPEC.md (`:624`/`:639`/`:811`)** ≠ littéral brief §28 → **décision de périmètre V1/V2 (Eliot)** ; dérive **détectée** (triggers, 8 évaluateurs : tilt/plan/sommeil/émotion/win-streak/no-checkin/hedge/training) **PASS** ; l'**alerte push « immédiate » (§28) — émission jamais branchée (J9 réception OK : enum/buildPayload/TTL/préférence ; émission absente, `engine.ts` n'enqueuait rien → PULL only) — est désormais CONSTRUITE sur la branche** (durcissement #7) → **reste outward-facing : Eliot contrôle le merge = la barrière de mise en prod**. **8 durcissements livrés (branche, gate vert)** : (1) **posture §2** win-rate jamais rouge [`scoring/session-perf-bars.tsx`,`pair-top-five.tsx`] (TIER1, contradisait « win-rate JAMAIS rouge » des cartes pre-trade) ; (2) **RGPD/storage** `deleteTrade` balaie en best-effort les captures entrée/sortie + médias d'annotations (sinon orphelins disque indéfinis ; +2 tests) [`trades/service.ts`] ; (3) **rate-limit** `/api/uploads` per-membre (429 AVANT buffer 8 MiB) [`rate-limit/token-bucket.ts`+route] ; (4) **découvrabilité** carte calme `/debrief-mensuel` sur `/dashboard` (n'était atteignable que par push/email/URL) ; (5) **§21 « sur quoi travailler »** nouveau `ScoreBreakdown` — sous-scores (déjà calculés/persistés, jamais affichés ; vérifié `rate`=goodness monotone donc tri/couleur corrects) triés faible→fort, `<details>` natif 0-JS accessible, posture neutre ; (6) **§28 alerte de dérive immédiate (AX3-F2)** : `enqueueDouglasDeliveryNotification` + appel best-effort dans `engine.ts` après chaque delivery + **push-only** (`douglas_card_delivered` ajouté à `EMAIL_FALLBACK_SKIP_TYPES` — anti email-fatigue) ; copy J9 calme inchangée (« Nouvelle fiche… selon ton activité récente », deep-link `/library/<slug>`) ; ≤1/jour (anti-spam engine) ; +3 tests ; **0 migration** ; firewall §21.5 vert ; (7) **§28/§21 graphe scores comportementaux DANS LE TEMPS (AX2-01)** : loader `getBehavioralScoreHistory` (série déjà persistée, lue avant seulement par l'export RGPD) + `ScoreTrendChart` Recharts 4 lignes (dynamic ssr:false, hex iOS, légende, a11y figcaption, état <2-snapshots calme, posture neutre 0 rouge) sur `/dashboard` sous les jauges ; +4 tests loader ; mount runtime vérifié desktop+mobile ; **0 migration** ; (8) tests. **Gate** : type-check 0, lint 0, **vitest 2027/2027** (144 fichiers, +8 cette session), build success [tool-output 2026-06-07]. **Runtime réel** : DB fraîche `migrate deploy` clean (prouve que la migration P3009 sale de la DB dev partagée passe sur une base saine) + dashboard rendu Playwright desktop (30d/7d/6m) & mobile + spec déterministe asserte les nouveaux éléments. **⚠️ Dette infra Eliot** : la DB **dev partagée** a une migration en **échec P3009** (`20260605114211_add_trade_emotion_during`) → `pnpm --filter @fxmily/web exec prisma migrate resolve` requis pour ré-utiliser la DB dev locale (n'affecte PAS la prod ni la CI). **À TRANCHER (Eliot)** : (a) débrief hebdo membre — V1 maintenant vs rester V2 (SPEC) ? (b) **merger** l'alerte de dérive push (construite, push-only) — c'est outward-facing (les membres recevront un push calme ≤1/j sur dérive) → ton go avant merge. Détail → §11 + hand-off §12.
>
> **MAJ-9 2026-06-07 (S2 challenge #5 — meeting-window join-floor, LIVE)** : `origin/main` = `e67d9765`. Fermeture de **la seule imperfection résiduelle** flaggée fin #4. Vérifiée RÉELLE : l'invariant §30.7 T3-1 (« le dénominateur d'un membre ne compte jamais les réunions tenues avant son arrivée ») était dans `meetingWindowStart` + respecté par `/reunions`, mais les **3 consommateurs** per-window de `countMeetingAttendance` passaient leur fenêtre brute **sans floor** : `scoring/service.ts` (sous-score engagement — **le vrai score comportemental**), `weekly-report/loader.ts` + `monthly-debrief/loader.ts` (compteurs Claude #254). Effet : un membre arrivé en milieu de semaine/mois voyait les réunions plateforme programmées **avant son arrivée** comptées au dénominateur → assiduité (envoyée à Claude) + engagement sous-évalués. **Fix #258 `e67d9765`** : helper partagé `floorMeetingWindowAtJoin(windowStartUtc, joinedAt)` = `max(windowStart, minuit-Paris(joinedAt))`, appliqué aux 3 ; `joinedAt` plumbé dans le scoring via `ComputeScoresOptions` + le `findMany` du cron (**+1 colonne, 0 query en plus**). **Byte-identique** pour tout membre arrivé avant le début de sa fenêtre (seuls les nouveaux, 1ʳᵉ fenêtre, impactés — la correction voulue). Gate : type-check 0, lint 0, **vitest 143 fichiers / 2019 tests** (+4 cas floor ; scoring/loaders byte-identiques). CI verte (Playwright 5m47s), deploy SUCCESS [run 27098555341], prod ok/ok/ok [WebFetch 16:44:31Z], image `:e67d9765ada0` LIVE. **0 migration.** **CANON** : tout compteur d'une entité plateforme planifiée indépendamment du membre (réunions) doit flooré sa fenêtre au `joinedAt`. **DoD §29 Session 2 = 100% ; aucune imperfection résiduelle connue.**

---

## 0. Comment lire ce fichier (hiérarchie des sources de vérité)

Fxmily a **21+ sessions de build réel** derrière lui et tourne **LIVE en production**. La série « 9 sessions » est une **couche de consolidation + pilotage** posée sur cet existant, **PAS un démarrage from-scratch**. Ce fichier ne duplique rien — il **pointe** :

| Source                                                                     | Rôle                                                                                                                                              | Quand la lire                               |
| -------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------- |
| **`PROJECT_STATE.md`** (ce fichier)                                        | Source de vérité d'**ÉTAT** : où on en est, le plan maître des 9 sessions, les contrats d'interface, les invariants.                              | Au démarrage de **chaque** session.         |
| [`SPEC.md`](./SPEC.md) (1463 l, v1.6+)                                     | Source de vérité **PRODUIT** : la vision, le quoi/pourquoi, la posture §2, les critères « Done quand » §15. **En cas de conflit, SPEC.md gagne.** | Pour comprendre l'intention d'un domaine.   |
| [`apps/web/CLAUDE.md`](./apps/web/CLAUDE.md)                               | Détail **jalon-par-jalon** (J0→§26) : décisions verrouillées, scars, pièges, modèle de données par jalon.                                         | Avant de toucher un domaine précis.         |
| [`CLAUDE.md`](./CLAUDE.md) (racine)                                        | **Conventions** repo + règle §18.4 (1 session = 1 jalon).                                                                                         | Toujours active.                            |
| `~/.claude/projects/D--Fxmily/memory/`                                     | Historique **session-par-session** (chronologique inverse) + feedbacks Eliot.                                                                     | Pour le contexte historique d'une décision. |
| `docs/FXMILY-V2-MASTER.md`, `docs/decisions/ADR-*.md`, `docs/runbook-*.md` | Roadmap V2, décisions d'archi, procédures ops.                                                                                                    | Référence ponctuelle.                       |

**Règle d'or** : ne jamais affirmer un état sans l'avoir vérifié (Read / build / `gh` / WebFetch). Ce fichier date ses affirmations et cite ses preuves.

**Convention de versionnement (lire AVANT d'interpréter un hash)** : chaque entrée **`MAJ-N`** (blockquotes en tête de fichier) cite le commit **feature** qu'elle documente — PAS le commit _docs_ qui introduit la `MAJ-N` elle-même (un fichier ne peut pas citer le hash du commit qui le crée). Donc le « dernier `origin/main` » affiché dans la `MAJ-N` la plus récente est **décalé d'un commit docs** par rapport au HEAD réel. Le **HEAD courant exact** de `origin/main` vit dans `~/.claude/projects/D--Fxmily/memory/auto_session_resume.md` (hand-off de reprise). Ex. : MAJ-21 cite `775c103` (feature #284) ; le HEAD réel après le commit docs #285 est `4cf6ebf`. Le `Snapshot` §1 ci-dessous est un instantané **daté** volontairement figé — l'état courant est porté par les `MAJ-N`, pas par lui.

---

## 1. Réalité vérifiée — snapshot daté (2026-06-05)

Vérifications faites **en réel** par la Session 1, pas de mémoire :

| Élément                           | Valeur                                                                                                    | Preuve                                                                                |
| --------------------------------- | --------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------- |
| `origin/main`                     | `5dce847` (`fix(batch): isolate claude --print`, #233, 2026-06-04)                                        | `git log`                                                                             |
| CI applicative sur le commit prod | **SUCCESS** — `CI` 2m25s + `Deploy` 2m25s + `E2E (Playwright)` 5m29s                                      | `gh run list --workflow {ci,deploy,e2e}.yml`                                          |
| Santé prod LIVE                   | `{"status":"ok","environment":"production","checks":{"env":"ok","db":"ok"}}` HTTP 200 @ 2026-06-04T23:05Z | WebFetch `app.fxmilyapp.com/api/health`                                               |
| Modèle de données                 | **34 modèles + 22 enums**                                                                                 | [`apps/web/prisma/schema.prisma`](./apps/web/prisma/schema.prisma) (lu intégralement) |
| Migrations Prisma                 | **25** (de `20260505152759_init` à `20260603120000_calendar_questionnaire`)                               | `ls apps/web/prisma/migrations/2026*/ \| wc -l` = 25 [tool-output]                    |
| Code source                       | **552** fichiers `.ts/.tsx`, **119** fichiers `.test.*`                                                   | `find apps/web/src`                                                                   |
| Tests Vitest                      | **1762/1762 passed** (119 fichiers) — **re-run first-hand**                                               | `vitest run` EXIT 0 [tool-output 2026-06-05]                                          |
| Gate qualité local                | `prisma generate` ✓ · `type-check` EXIT 0 ✓ · `lint` EXIT 0 ✓ · build Turbopack EXIT 0 ✓                  | [tool-output 2026-06-05]                                                              |

**Bruit CI à ignorer** : des runs `Dependabot Updates` `hono` apparaissent en `failure` sur main — `hono` est une dépendance **dev-only** (via `@prisma/dev`), pas la CI applicative. C'est un point de **backlog** (§11), pas une régression de build.

---

## 2. Stack technique (réelle, installée, justifiée) — §28

Source : [`apps/web/package.json`](./apps/web/package.json) + [`SPEC.md`](./SPEC.md) §4. La stack « à choisir » de la Session 1 est **déjà installée et en prod** :

| Couche       | Choix réel                                                                      | Version                                                       | Rationale                                                                       |
| ------------ | ------------------------------------------------------------------------------- | ------------------------------------------------------------- | ------------------------------------------------------------------------------- |
| Front + Back | **Next.js 16** (App Router, Turbopack) + **React 19** + **TypeScript strict**   | `next 16.2.6`, `react 19.2.6`, `typescript ^6.0.3`            | Codebase unique web + PWA → Capacitor V2. `middleware.ts`→`proxy.ts` (Next 16). |
| CSS          | **Tailwind CSS 4**                                                              | `^4.3.0`                                                      | Thème custom dark-only (DS-v2 lime).                                            |
| Composants   | **shadcn/ui** (Radix) + `class-variance-authority` + `lucide-react`             | `radix-ui ^1.4.3`                                             | Ownership total du code, a11y.                                                  |
| Animations   | **Framer Motion**                                                               | `^12.40.0`                                                    | LazyMotion `m.*` (bundle).                                                      |
| Charts       | **Recharts** (pivot J6.6 — Tremor abandonné)                                    | `^3.8.1`                                                      | Couleurs hex `C` (bug `var()` WebView iOS).                                     |
| Auth         | **Auth.js v5** (Credentials argon2id + JWT strategy)                            | `next-auth 5.0.0-beta.31` (pin exact — pas de stable à venir) | Self-hosted, `proxy.ts` edge gate.                                              |
| ORM          | **Prisma 7** (Rust-free, `@prisma/adapter-pg`)                                  | `^7.8.0`                                                      | `url` dans `prisma.config.ts`, client généré `src/generated/prisma`.            |
| DB           | **PostgreSQL 17** self-hosted Hetzner                                           | —                                                             | Pool pinné v6-defaults (`lib/db.ts`).                                           |
| Médias       | **Cloudflare R2** (S3-compat)                                                   | —                                                             | Abstraction `lib/storage` (Local dev / R2 prod).                                |
| Email        | **Resend** + React Email                                                        | `resend ^6.12.3`                                              | Free 100/jour = vrai cap.                                                       |
| Validation   | **Zod**                                                                         | `^4.4.3`                                                      | `.strict()` partout (API + forms + env + output IA).                            |
| Push         | **Web Push** + VAPID + Service Worker `public/sw.js`                            | `web-push ^3.6.7`                                             | Apple Declarative (Safari 18.4+).                                               |
| IA           | **Claude en LOCAL** (`claude --print`, abonnement Max, **$0 API marginal**)     | Opus 4.8 §8                                                   | Chemin SDK API (`@anthropic-ai/sdk ^0.98`) **dormant**. Cf. §5.                 |
| Monitoring   | **Sentry** (DSN-gated)                                                          | `@sentry/nextjs ^10.53`                                       | `reportError/Warning/Info` + URL scrub.                                         |
| Tests        | **Vitest** + RTL + **Playwright**                                               | `vitest ^4.1.7`                                               | Unit (logique critique) + e2e (auth gates + happy-path).                        |
| Build/CI     | **Turborepo** + **pnpm 10** + **Node 22 LTS** + GitHub Actions + Docker + Caddy | —                                                             | Cf. §8.                                                                         |

**Pivots stack documentés** (SPEC §20.1) : Tremor→Recharts · DB session→JWT · bleu→DS-v2 lime · API Claude payante→batch local Claude Max. Aucune décision de Session 1 ne ré-ouvre ces pivots.

---

## 3. Architecture globale & orchestration — §28 / §23-24

Monorepo **Turborepo + pnpm workspaces** ; app principale `apps/web`. Pattern Next.js 16 App Router :

```
Client (PWA mobile-first, dark-only DS-v2)
   │  HTTPS  (Caddy reverse-proxy, HSTS, XFF rewrite)
   ▼
proxy.ts  ── auth gate edge (status==='active'), public-route allowlist
   │
   ├── Server Components (force-dynamic) ── lectures DB scoped
   ├── Server Actions ('use server') ────── mutations (pattern J5 carbone)
   └── Route Handlers (runtime nodejs)
         ├── /api/health, /api/auth/[...nextauth]
         ├── /api/cron/*           (X-Cron-Secret, timingSafeEqual, token-bucket)
         ├── /api/admin/*-batch/*  (X-Admin-Token, moteur Claude — §5)
         └── /api/uploads/*        (BOLA, magic-byte, ownership)
   │
   ▼  couches transverses (toutes server-only)
Zod schemas (lib/schemas) → Services (lib/<domaine>/service.ts) → Prisma (lib/db.ts)
   │
   └── transverses : audit (PII-free) · safeFreeText (anti-Trojan-Source) ·
       crisis/injection/AMF detection · firewall anti-leak §21.5/§27.7 ·
       rate-limit token-bucket · observability Sentry · DS-v2 tokens
```

**Principe directeur (SPEC §23-24, directive Eliot)** : _« pas de simplicité bête, pas de complexité gratuite — une architecture parfaitement orchestrée »_. Chaque ajout passe par la **même grammaire** (schema → service server-only → action/route → UI) ; aucune couche n'est superposée ad-hoc.

### 3.1 Arbre `lib/` par domaine (`apps/web/src/lib/`)

- **Infra runtime** : `db.ts` (singleton Prisma 7 + adapter-pg, pool pinné `max:10`/`connectionTimeoutMillis:5_000` — les défauts v7 hangeraient à l'infini) [`lib/db.ts:20-29`] · `env.ts` (Zod sur `process.env`, fail-fast, tokens batch/VAPID/Sentry) [`lib/env.ts`, 267 l] · `instrumentation.ts` (importe `@/lib/env` au boot Node).
- **Domaines IA (moteur Claude — §5)** : `weekly-report/` (réf canonique : `builder·loader·service·claude-client·prompt·pricing·batch·week-window`) · `monthly-debrief/` · `onboarding-interview/` (+`instrument-v1`·`safety`) · `calendar/` (+`instrument-v1`·`snapshot`·`format`).
- **Domaines real-edge** : `scoring/` (4 dimensions + `dashboard-data`) · `analytics/` (maths pures : `wilson·correlations·expectancy·streaks·equity-curve·drawdown·habit-trade-correlation`) · `triggers/` (engine fiches Mark Douglas) · `cards/` · `pre-trade/` (`service·analytics·correlation`) · `trades/` · `annotations/` · `checkin/` · `habit/` · `account/` · `push/` · `admin/`.
- **Domaines isolés (firewall §21.5/§27.7)** : `training/` + `training-debrief/` · `mindset/` · `reflection/`.
- **Transverses** : `auth/` (`audit.ts` **~121 slugs** · `admin-token.ts` · `password.ts` argon2id · `invitations.ts`) · `schemas/` (~25 fichiers Zod, un par entité) · `safety/crisis-detection.ts` (`detectCrisis`, regex FR) · `ai/` (`injection-detector.ts` 9 patterns · `prompt-builder.ts` wrap XML untrusted) · `text/safe.ts` (`safeFreeText`) · `rate-limit/token-bucket.ts` · `observability/` (Sentry + url-scrub) · `storage/` · `email/`.

### 3.2 Couches transverses (invariants partagés par tous les domaines)

- **Frontière server-only stricte** : `@/lib/db`, `@/lib/env`, `@/auth`, les services → **jamais** importés depuis `'use client'`. Sentinelle `import 'server-only'`.
- **Validation runtime Zod** : input API + forms (RHF + zodResolver) + env (`lib/env.ts`, fail-fast au boot via `instrumentation.ts`) + **output IA** (`.strict()`).
- **Audit log PII-free** (`lib/auth/audit.ts`) : IPs SHA-256 + sel `AUTH_SECRET`, jamais de texte brut ; ~100+ slugs `domaine.action`.
- **Sécurité texte** : `safeFreeText` (NFC + strip bidi/zero-width) sur tout free-text user-controlled → bloque Trojan-Source avant tout prompt LLM.
- **Firewall anti-leak** (`src/test/anti-leak/*`) : isolation statistique **§21.5** (Entraînement ↔ real-edge) + **§27.7** (Mindset) + **§26** (Calendrier). Tests « Block A→H » qui glob `lib/{scoring,analytics,trades,habit}/**` et échouent si un module real-edge importe un token training/mindset/calendar.
- **Design system DS-v2** : tokens lime/deep-space (`globals.css`) ; overlay `.v18-theme` (REFLECT blue+black) ; cyan `--cy` (Entraînement §21.7) ; Recharts hex `C` jamais `var()`.

---

## 4. Modèle de données central, global et unifié — §28 (la colonne vertébrale)

**34 modèles, 22 enums** dans [`apps/web/prisma/schema.prisma`](./apps/web/prisma/schema.prisma) — lu intégralement par la Session 1. Le tableau ci-dessous **prouve que le data model couvre 100 % des axes des sessions 2-9** (critère DoD §29). Tous les modèles métier cascadent sur `User` delete (RGPD §17).

| #   | Domaine (→ session)                                | Modèles Prisma                                                              | Enums                                                                                                      | Isolation / note                                                                                                        |
| --- | -------------------------------------------------- | --------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- |
| —   | **Auth / socle**                                   | `User`, `Account`, `Session`, `VerificationToken`, `Invitation`, `AuditLog` | `UserRole`, `UserStatus`                                                                                   | Contrat PrismaAdapter Auth.js. `User` = hub de 30+ relations. Soft-delete RGPD (`deletedAt`).                           |
| A   | **Journal de trade** (real-edge)                   | `Trade`, `TradeAnnotation`                                                  | `TradeDirection`, `TradeSession`, `TradeOutcome`, `RealizedRSource`, `TradeQuality`, `AnnotationMediaType` | `realizedR` computed/estimated ; V1.5 `tradeQuality`/`riskPct` ; tags LESSOR.                                           |
| B   | **Tracking quotidien**                             | `DailyCheckin`, `HabitLog`                                                  | `CheckinSlot`, `HabitKind`                                                                                 | `@db.Date` Europe/Paris ; upsert `(userId,date,slot/kind)`.                                                             |
| C   | **Pré-trade anti-FOMO** (différenciateur)          | `PreTradeCheck`                                                             | `PreTradeReason`, `PreTradeEmotion`                                                                        | Auto-link `Trade` 15 min, **no-FK** race-safe (dangling toléré). ADR-003.                                               |
| D   | **Scoring & track record**                         | `BehavioralScore`                                                           | —                                                                                                          | Snapshot quotidien 4 dimensions ; `Int?` nullable = `insufficient_data` (jamais fake-0).                                |
| E   | **Module Mark Douglas**                            | `MarkDouglasCard`, `MarkDouglasDelivery`, `MarkDouglasFavorite`             | `DouglasCategory`                                                                                          | 50 fiches ; trigger engine ; cooldown white 7j/black 14j. Fair use ≤30 mots.                                            |
| F   | **REFLECT** (introspection)                        | `WeeklyReview`, `ReflectionEntry`                                           | —                                                                                                          | CBT Ellis ABCD ; crisis routing FR ; thème `.v18-theme`.                                                                |
| G   | **Notifications**                                  | `NotificationQueue`, `PushSubscription`, `NotificationPreference`           | `NotificationType`, `NotificationStatus`                                                                   | Dispatcher J9 ; 410 Gone→DELETE ; cap email §18.2.                                                                      |
| H   | **Admin & coaching**                               | `AdminNote`                                                                 | —                                                                                                          | Notes privées admin (membre ne voit jamais).                                                                            |
| I   | **Mode Entraînement / backtest** (§21)             | `TrainingTrade`, `TrainingAnnotation`, `TrainingDebrief`                    | `TrainingOutcome`, `TrainingAnnotationMediaType`                                                           | **§21.5 isolation béton** : enums séparés, 0 FK vers `Trade`.                                                           |
| J   | **Rapports IA** (moteur Claude — §5)               | `WeeklyReport`, `MonthlyDebrief`                                            | —                                                                                                          | Cost-tracking `costEur` Decimal(10,6) ; idempotent `(userId,week/monthStart)`.                                          |
| K   | **Mindset QCM** (§27)                              | `MindsetCheck`                                                              | —                                                                                                          | Déterministe **0-AI** ; instrument versionné ; profil dérivé au render (jamais stocké).                                 |
| L   | **Onboarding & Profil IA** (§V2.4, moteur Claude)  | `OnboardingInterview`, `OnboardingInterviewAnswer`, `MemberProfile`         | `InterviewStatus`                                                                                          | ~30 questions ; Claude synthétise axes psycho/process (jamais analyse trade).                                           |
| M   | **Calendrier adaptatif** (SPEC §31, moteur Claude) | `WeeklyScheduleQuestionnaire`, `AdaptiveCalendar`                           | `CalendarSlot`, `CalendarBlockCategory`                                                                    | Organise le **TEMPS**, jamais les trades. 0 champ P&L. EU AI Act 50(1) disclosure.                                      |
| ⚠   | **Track Record public** (DÉPRÉCIÉ)                 | `PublicTrade`, `PublicTradePartial`                                         | `PublicTradeSegment`, `PublicTradeStatus`                                                                  | **Migré vers repo standalone `trackrecord-fxmily`** (2026-05-25). Gardés `@deprecated` pour sync DB Hetzner avant drop. |

**Invariant data-model #1 (§2 posture)** : tout modèle « adaptatif IA » (Calendar, MemberProfile, WeeklyReport, MonthlyDebrief) porte des données de **temps / process / psychologie** — **jamais** un avis de marché ni un setup.

**Invariant data-model #2 (§21.5 / §27.7)** : `TrainingTrade`/`TrainingAnnotation`/`TrainingDebrief`/`MindsetCheck`/`AdaptiveCalendar` n'ont **aucune FK** vers les modèles real-edge (`Trade`, `BehavioralScore`, `WeeklyReport`). L'isolation statistique est **prouvée par la forme du schéma** + les tests anti-leak (Blocks A→H, cf. §5.3).

> **⚠ Note de numérotation SPEC** (relevée à l'audit) : le **Calendrier** est spécifié en **`SPEC.md §31`** (`:1388`) — le « §26 » des labels de jalon dans `apps/web/CLAUDE.md`/MEMORY est en fait le _changelog_ v1.3→v1.4. Les **réunions §30** ne sont **pas encore** dans `SPEC.md` (numéro réservé, PR #206 `/spec` en attente de merge) — elles sont référencées comme dépendance _future_ du calendrier (`CalendarBlockCategory.meeting`). C'est le seul domaine « planifié-non-construit » du data model.

---

## 5. Moteur Claude **Opus 4.8** en LOCAL — service IA central réutilisable — §28

> **MAJ-25 (2026-06-11)** : moteur ré-épinglé **`claude-opus-4-8`** (PR #293) — Fable 5 quitte les modèles inclus de l'abonnement Max après le **22 juin 2026** (usage credits $10/$50 MTok au-delà, anthropic.com/news/claude-fable-5-mythos-5). Opus 4.8 = le moteur pérenne à $0 marginal. `claude-fable-5` reste allowlisté pour runs manuels tant qu'il est inclus.

**LE point névralgique de la Session 1.** Fxmily refuse catégoriquement l'API Anthropic payante (décision Eliot). Toute génération IA passe par **`claude --print` headless sur la machine d'Eliot, abonnement Claude Max → $0 API marginal** (SPEC §8). Le chemin SDK API (`@anthropic-ai/sdk`) existe mais reste **dormant**.

> **MAJ-24 (2026-06-10, PR #291)** : le moteur est désormais **réellement central** — lib bash partagée `ops/scripts/lib/claude-batch-core.sh` (LE seul endroit qui connaît modèle/flags/parse/validations ; les 4 orchestrateurs sont des carbons minces) + helpers TS partagés `lib/ai/claude-response.ts` (parsing unique des 3 `claude-client.ts`). Modèle épinglé **`claude-fable-5`** (effort `xhigh`, budget cap **$15**, pre-flight CLI ≥ 2.1.170 ; Opus 4.8 reste allowlisté en fallback MANUEL — jamais automatique). Les mentions « Opus 4.8 / 5.00 » résiduelles ci-dessous décrivent l'ancien défaut (historique).

### 5.1 Flux end-to-end (pattern carbone, réutilisé par 4 pipelines)

```
Machine Eliot (Windows)                    HETZNER PROD (Caddy → fxmily-web)
  /<pipeline>-batch  (slash command)
  bash ops/scripts/<pipeline>-batch-local.sh
   │ curl POST X-Admin-Token ───────────→  POST /api/admin/<pipeline>-batch/pull
   │                                        │ requireXAdminToken (SHA-256 + timingSafeEqual
   │                                        │   + token-bucket → 503/429/401)
   │                                        │ loadAllSnapshots… (Promise.allSettled batch=5)
   │ ◄──── enveloppe JSON (pseudonymisée) ──┘ pseudonymizeMember (8-char hex, sel MEMBER_LABEL_SALT)
   │
   │  Loop par membre :  claude --print  (Opus 4.8 — `claude-opus-4-8`, re-pin MAJ-25)
   │    --setting-sources ""    ← #233 : 0 CLAUDE.md/hooks de l'opérateur
   │    --system-prompt <REPLACE>   (system prompt + JSON schema voyagent AVEC l'enveloppe)
   │    --max-turns 8  --max-budget-usd 15.00   (circuit-breaker, recalibré Fable 5)
   │    60-120s jittered, contexte FRAIS par membre
   │
   ▼  curl POST X-Admin-Token ───────────→  POST /api/admin/<pipeline>-batch/persist
                                            │ MAX_BODY_BYTES 16 MiB + Zod .strict() (results.max 1000)
                                            │ persistGenerated… → GATES (cf. 5.3) → DB
   ◄──── { persisted, skipped, errors, total }
```

### 5.2 Les 4 pipelines (tous LIVE prod, même squelette)

| Pipeline                        | Sortie                                      | Modèle DB          | Statut                                                                                              |
| ------------------------------- | ------------------------------------------- | ------------------ | --------------------------------------------------------------------------------------------------- |
| **Weekly report** (J8 / V1.7.2) | digest hebdo admin                          | `WeeklyReport`     | LIVE — `/api/admin/weekly-batch/{pull,persist}`, `/sunday-batch`                                    |
| **Monthly debrief** (V1.4 §25)  | débrief mensuel membre (dual-section §21.5) | `MonthlyDebrief`   | LIVE — `/api/admin/monthly-batch/*`, `/monthly-batch`                                               |
| **Onboarding profile** (V2.4)   | `MemberProfile` (axes psycho/process)       | `MemberProfile`    | LIVE — `/api/admin/onboarding-batch/*`, `/onboarding-batch`                                         |
| **Calendrier adaptatif** (§26)  | plan hebdo de **temps**                     | `AdaptiveCalendar` | LIVE (infra activée, attend 1ᵉʳ run data réelle) — `/api/admin/calendar-batch/*`, `/calendar-batch` |

Chacun expose `prompt.ts` (system prompt §2-hardcodé + JSON schema strict) + `batch.ts` (`loadAllSnapshots…` + `persistGenerated…`). **Asymétrie vérifiée** [`ls lib/*`] : `weekly-report` + `calendar` ont aussi `claude-client.ts` (`Mock` déterministe / `Live` SDK dormant) **et** `pricing.ts` (sentinel `claude-code-local` → $0) ; `onboarding-interview` a `claude-client.ts` **sans** `pricing.ts` ; `monthly-debrief` a `pricing.ts` **sans** `claude-client.ts` (génération pass-through). **Depuis MAJ-24 (PR #291)** : les 3 `claude-client.ts` partagent le parsing via `lib/ai/claude-response.ts` (extractText/safeParseJson/extractUsage, testé en propre) et les 4 scripts bash sourcent `ops/scripts/lib/claude-batch-core.sh` — les divergences délibérées (regex pseudonyme stricte monthly, trap/dry-run onboarding, exit codes) restent dans les carbons.

### 5.3 Couches de validation/sécurité de l'output IA (gates `persist…`)

Ordre exact (ne pas réordonner) : **(1)** `error` field d'abord → **(2)** `parseLocalDate(weekStart)` → **(3)** active-user `findMany` (anti forged userId) → **(4)** existence du questionnaire/interview (gate pipeline-spécifique) → **(5)** `<output>Schema.strict().safeParse` (anti-fuzzing enum/clés hallucinées) → **(6)** `detectCrisis(corpus IA complet)` → skip + Sentry (HIGH `reportError` / MEDIUM `reportWarning`) → **(6b)** `detectAMFViolation` (posture §2) → skip + audit `*.amf_violation` → **(7)** `persist`. Onboarding ajoute anti-clinical + evidence verbatim NFC.

### 5.4 Mitigation ban-risk (9 règles, invariantes)

Machine + IP + fingerprint + compte Max d'Eliot · jitter 60-120s (floor 30) · 1 `claude --print` par membre (contexte frais) · données pseudonymisées · system prompt + schema voyagent depuis le repo · **binaire `claude` officiel uniquement** (jamais OpenClaw/Roo/Goose) · human-in-the-loop (Eliot déclenche) · double-net validation serveur (`.strict()` + active-user set) · audit `*.batch.*` PII-free.

### 5.5 Précisions vérifiées (audit architecture)

- **Flags `claude --print` réels** [`ops/scripts/lib/claude-batch-core.sh:320-334`, SSOT depuis PR #291] : `--setting-sources ""` + `--system-prompt` (REPLACE) + `--max-turns 8` (PAS 1 — le thinking consomme 1 turn avant le JSON) + `--max-budget-usd 15.00` + `--model claude-fable-5` + `--effort xhigh` + `$(<file)` literal read (anti shell-expansion). `--bare` **retiré** (cassait l'auth OAuth Max).
- **`requireXAdminToken`** [`lib/auth/admin-token.ts:31-180`] : `verifyAdminToken` = SHA-256 des deux côtés + `timingSafeEqual` (CWE-208). Ordre **503** (token absent, refuse-by-default) → **401** (consomme le bucket via `callerIdTrusted` last-XFF non-spoofable) → **429** → null. **3 tokens env pour 4 pipelines** : `ADMIN_BATCH_TOKEN` (**weekly + onboarding le PARTAGENT** [`onboarding-batch/pull/route.ts:3` importe `requireAdminToken`]), `MONTHLY_ADMIN_BATCH_TOKEN`, `CALENDAR_ADMIN_BATCH_TOKEN` [`lib/env.ts:159/172/186`].
- **Firewall anti-leak Blocks A→H** [`src/test/anti-leak/training-isolation.test.ts:90-634`] : A (import firewall) · B (primitive count-only) · C (engagement P&L-invariance) · D (trigger) · E (weekly snapshot `.strict()`) · F (TrainingDebrief §23) · **G** (MonthlyDebrief §25 — lit _légitimement_ le P&L réel, isolation training-only) · **H** (MindsetCheck §27, le plus isolé). `BREACH_TOKENS` interdit `TrainingTrade`/`db.trainingTrade`/`@/lib/training` dans tout module real-edge. Le firewall calendrier glob `lib/calendar/**`.
- **Fichiers réels du moteur** (asymétrie par pipeline vérifiée `ls`) : `weekly-report/` + `calendar/` = les 4 (`claude-client·prompt·batch·pricing.ts`) ; `monthly-debrief/` = `prompt·batch·pricing.ts` (**pas** de `claude-client.ts`) ; `onboarding-interview/` = `claude-client·prompt·batch.ts` + `safety·instrument-v1.ts` (**pas** de `pricing.ts`). Transverses : `lib/auth/admin-token.ts` · `lib/ai/{injection-detector,prompt-builder}.ts` · `lib/safety/crisis-detection.ts` · `app/api/admin/*-batch/{pull,persist}/route.ts` (8 routes) · `ops/scripts/{weekly,monthly,onboarding,calendar}-batch-local.sh` · `.claude/commands/{sunday,monthly,onboarding,calendar}-batch.md`.

---

## 6. Conventions du repo (invariantes) — §6 CLAUDE.md

- **TypeScript strict partout** (`noUncheckedIndexedAccess`, `exactOptionalPropertyTypes`). Pas de `any` non motivé.
- **Validation runtime systématique** (Zod) sur input API, forms, env, output IA.
- **Server-only par défaut** : Prisma/env/secrets jamais depuis un `'use client'`.
- **Conventional Commits en anglais**, courts, scopés.
- **Une feature = une branche** ; pas de commit direct sur `main`.
- **Tests pour la logique critique** (`lib/scoring`, `lib/triggers`, `lib/analytics`, `lib/*/service`) en Vitest ; UI pure non testée en unit (Playwright pour les gates/render).
- **Mobile-first strict** : iPhone SE (375×667) + iPhone 15 (393×852).
- **Dark-only V1** (DS-v2). `maximumScale`/`userScalable:false` **interdits** (WCAG 1.4.4).
- **🔴 Règle §18.4 (non négociable)** : **1 session Claude Code = 1 jalon, `/clear` entre chaque.** Quand Eliot dit « fais tout d'un coup » → rappeler la règle, l'appliquer dans le scope du jalon courant.

---

## 7. PLAN MAÎTRE — mapping des 9 sessions sur les domaines réels — §28

La série « 9 sessions » se mappe **proprement** sur la décomposition métier réelle du projet. **Toutes les sessions de build (2-9) sont DÉJÀ LIVRÉES et LIVE prod** — leur travail n'est donc pas « construire » mais **auditer en profondeur, durcir, et pousser 100× plus loin** (directive Eliot), dans le scope d'un jalon par session (§18.4).

> **Convention d'interconnexion** : chaque session lit `PROJECT_STATE.md` (entrées) **avant**, met à jour son §domaine + le hand-off §12 (sorties) **après**. La colonne vertébrale partagée par toutes = §4 (data model) + §5 (moteur Claude) + §3.1 (transverses).
>
> **⚠ Note de numérotation (relevée Session 4, 2026-06-08)** : ce tableau §7 est une décomposition par **DOMAINE métier** ; elle ne correspond **PAS 1:1** à la numérotation des **fichiers brief** `0N-SESSION-*.md` qu'Eliot copie-colle. Le canon **opérationnel** est : **« Session N » = le fichier `0N-SESSION-\*.md »**, et chaque session est auditée contre la **DoD §29 de SON brief**. Ex : le brief **`04-SESSION-Coaching-Psychologie.md`** = **coaching Mark Douglas (§E) + moteur d'analyses autonomes Claude (§5/§J)** — PAS la ligne « 4 = Journal de trade » de ce tableau (mapping domaine alternatif). Les sessions réelles S2 (`02-…`), S3 (`03-…`), S4 (`04-…`) ont toutes suivi les **fichiers brief**, pas la numérotation §7.

| Session | Domaine                                                         | Statut réel                    | Modèles                                                                                            | Routes membres                                                                         | « 100× plus loin » (backlog d'amélioration)                                                                                                                                                                                                                                                             |
| ------- | --------------------------------------------------------------- | ------------------------------ | -------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **1**   | **Fondations** (ce fichier)                                     | ✅ Consolidé ici               | —                                                                                                  | —                                                                                      | Maintenir ce fichier à jour à chaque session.                                                                                                                                                                                                                                                           |
| **2**   | **Onboarding & Profil IA** (V2.4 §L) + **émotion-pendant** (§A) | ✅ LIVE prod (2 jalons mergés) | `OnboardingInterview(+Answer)`, `MemberProfile`, `Trade.emotionDuring`                             | `/onboarding/interview*`, `/profile`, **`/dashboard` (widget)**, `/journal/[id]/close` | ✅ **#235 pipeline profilage branché sur le parcours** (`d43ae2b`, était orphelin) + ✅ **#236 émotion AVANT/PENDANT/APRÈS le trade complète** (`929cfdc`, migration prod appliquée). Reste : re-profilage périodique ; dette `questionText:''` ; allowlist `opus-4-8` ; contraste DS `--acc→--acc-hi`. |
| **3**   | **Tracking comportemental** (§B + §C)                           | ✅ LIVE prod                   | `DailyCheckin`, `HabitLog`, `PreTradeCheck`                                                        | `/checkin*`, `/track*`, `/pre-trade/new`                                               | Étendre corrélation pré-trade×outcome aux 5 piliers ; HealthKit/wearables (V2).                                                                                                                                                                                                                         |
| **4**   | **Journal de trade** (§A)                                       | ✅ LIVE prod                   | `Trade`, `TradeAnnotation`                                                                         | `/journal*`                                                                            | Vidéo Zoom 500 MiB (J4.5) ; `Trade.tags` admin filter ; account balance (V2).                                                                                                                                                                                                                           |
| **5**   | **Scoring & Track record** (§D + analytics)                     | ✅ LIVE prod                   | `BehavioralScore`                                                                                  | `/dashboard`                                                                           | Calibration ADR-002 (cohort drift) ; nouvelles corrélations habit×trade.                                                                                                                                                                                                                                |
| **6**   | **Calendrier adaptatif** (SPEC §31 · jalons §26 §M)             | ✅ LIVE prod (4/4)             | `WeeklyScheduleQuestionnaire`, `AdaptiveCalendar`                                                  | `/calendrier`, `/calendar/questionnaire/new`                                           | **Flip ADR-005 Accepted** (1ᵉʳ batch réel + validation §2 sur 5+ calendriers) ; **réunions §30** (PR #206 en attente).                                                                                                                                                                                  |
| **7**   | **Rapports IA hebdo/mensuel** (§J — moteur Claude)              | ✅ LIVE prod                   | `WeeklyReport`, `MonthlyDebrief`                                                                   | `/debrief-mensuel`                                                                     | Outbox exactly-once (V2) ; observabilité coût ; smoke batch réel.                                                                                                                                                                                                                                       |
| **8**   | **Mode Entraînement** (§21 §I) + **Mindset** (§27 §K)           | ✅ LIVE prod                   | `TrainingTrade(+Annotation)`, `TrainingDebrief`, `MindsetCheck`                                    | `/training*`, `/mindset*`                                                              | Maintenir le firewall §21.5/§27.7 sur tout ajout ; placeholder « suivi formation ».                                                                                                                                                                                                                     |
| **9**   | **Mark Douglas & REFLECT** (§E §F) + **Admin/coach** (§H §G)    | ✅ LIVE prod                   | `MarkDouglasCard/Delivery/Favorite`, `WeeklyReview`, `ReflectionEntry`, `AdminNote`, notifications | `/library*`, `/review*`, `/reflect*`, `/admin/*`, `/account/notifications`             | 50→+ fiches ; CRUD admin fiches ; multi-admin (V2 `SetNull`).                                                                                                                                                                                                                                           |

**Ordre / checkpoints** : Sessions 2-9 sont **indépendantes** (chaque domaine est déjà livré et isolé). L'ordre suggéré suit la valeur produit : profilage (2) → tracking (3) → analytics (5) → IA (6,7). Chaque session = 1 jalon = 1 PR atomique, gate vert + audits Opus + vérif réelle (Playwright/screenshots) **avant** merge, `/clear` après.

---

## 8. Infra / Deploy / CI / Crons — §28

- **Runtime prod** : Hetzner Cloud (Falkenstein UE, RGPD) — **Docker Compose** (`docker-compose.prod.yml`) : Postgres 17 + web standalone (`Dockerfile.prod` non-root) + **Caddy 2** (`ops/caddy/Caddyfile`, HSTS preload, XFF rewrite non-spoofable).
- **Deploy auto** : `DEPLOY_PATH=hetzner` ⇒ `.github/workflows/deploy.yml` déploie chaque push `main` non-docs (build → GHCR → SSH → `docker compose pull/up` → `prisma migrate deploy`). **0 carry-over manuel.**
- **CI** : `ci.yml` (lint + type-check + build + Vitest), `e2e.yml` (Playwright chromium + Postgres service), `codeql.yml`, `zizmor.yml` (Actions SHA-pinned, hard-gate medium+), `cron-watch.yml` (heartbeat horaire → issue auto si 503).
- **Crons Hetzner** (`ops/cron/crontab.fxmily`, LF-forcé `.gitattributes`) : backup `pg_dump`→GPG→R2 · `dispatch-notifications` (2 min) · `recompute-scores` (02:00) · `dispatch-douglas` (6h) · `checkin-reminders` · `weekly-reports` (dim 21:00) · `mindset-check-reminders` (lun 09:00) · purges (deleted / push-subs 90j / audit-log 90j) + le backup `caddy_data`.
- **Tooling** : `turbo.json` (tasks build/lint/type-check/test, env `DATABASE_URL`/`AUTH_SECRET`/`AUTH_URL`) · `pnpm-workspace.yaml` (`packageManager: pnpm@10.33.2`, Node ≥22, `pnpm.overrides` CVE postcss/hono) · Husky `.husky/pre-commit` → lint-staged (prettier root + **ESLint délégué au workspace** `pnpm --filter @fxmily/web exec eslint`) · commitlint.

### 8.1 Fichiers infra réels (audit)

- **Docker** : `ops/docker/Dockerfile.prod` (multi-stage `node:22-bookworm-slim` non-root uid 1001, Next standalone, `HEALTHCHECK /api/health`) · `ops/docker/docker-compose.prod.yml` (3 services postgres-17/web/caddy, réseau interne `10.42.42.0/24`, secrets file) · `docker-compose.dev.yml` (Postgres dev seul).
- **Caddy** : `ops/caddy/Caddyfile:20-52` — `app.fxmilyapp.com` → `web:3000`, HSTS preload 2 ans, `-Server`, zstd/br/gzip, **overwrite `X-Forwarded-For {remote_host}`** (`:35-37`, single-hop non-spoofable, aligné `callerIdTrusted`).
- **Workflows** (`.github/workflows/`, **toutes SHA-pinned**) : `ci.yml` (paths-ignore `**/*.md`,`docs/**`) · `e2e.yml` (Playwright chromium + Postgres service réel + seed J6) · `codeql.yml` · `zizmor.yml` (hard-gate `--min-severity medium`) · `deploy.yml` · `cron-watch.yml` (heartbeat horaire `:15`).
- **Crons Hetzner** (`ops/cron/crontab.fxmily`, **11 jobs**, wrapper `fxmily-cron` allowlist 9 routes, LF-forcé `.gitattributes`) : `checkin-reminders` (15 min fenêtres) · `recompute-scores` (`0 2`) · `dispatch-douglas` (`0 0,6,12,18`) · `weekly-reports` (`0 21 * * 0`) · `dispatch-notifications` (`*/2`) · `purge-deleted` (`0 3`) · `purge-push-subscriptions` (`0 5 * * 0`) · `purge-audit-log` (`0 4`) · `fxmily-backup` pg_dump→GPG→R2 (`30 2`) · `fxmily-caddy-backup` (`30 6 * * 0`) · `mindset-check-reminders` (`0 9 * * 1`) · **`calendar-overdue-alert` (`0 11 * * *`, S5 §26 — détection-only, monitoré `health.ts`, MAJ-20)** · **`monthly-debrief-overdue-alert` (`10 11 * * *`, S5 §25 — détection-only, monitoré, MAJ-21)**. **Les 4 batchs IA générateurs ne sont PAS dans le crontab** — déclenchés manuellement depuis le PC d'Eliot (§5) ; les 2 overdue-alert (ci-dessus) sont des crons de **détection** qui nudgent l'admin quand calendrier/débrief manquent, ils ne génèrent rien (permanence DoD#2/#4 sans casser le human-in-the-loop ban-risk §5.4).
- **Deploy flux** (`deploy.yml`) : gate `vars.DEPLOY_PATH=='hetzner'` → `build-and-push` (buildx → GHCR) → `ssh-deploy` (appleboy, `docker compose pull web` + `up -d`, refresh `/opt/fxmily/prisma` depuis l'image, **`prisma migrate deploy`** via container one-shot) → `notify`.

---

## 9. Contrats d'interface & invariants non-négociables (pour sessions 2-9)

Toute session 2-9 **hérite** de ces contrats et ne doit **jamais** les violer :

1. **Posture §2 (BLOQUANT)** : ❌ aucun conseil sur les analyses de trade (setups/tendances/prévisions). ✅ exécution (sessions/hedge/plan/discipline) + ✅ psychologie Mark Douglas (citations ≤30 mots + paraphrases attribuées). Tout output IA passe le gate `detectAMFViolation`.
2. **Isolation statistique §21.5 / §27.7 / §26** : pas de FK ni d'import cross entre surfaces Entraînement/Mindset/Calendrier et le real-edge (scoring/analytics/journal). Le firewall `test/anti-leak/*` doit rester vert.
3. **IA = $0 (batch local Claude Max)** : aucune session ne réintroduit l'API Anthropic payante sans décision Eliot explicite. Pattern §5 obligatoire (isolation `claude --print` #233).
4. **Audit PII-free** (RGPD §16) : jamais d'email/texte brut/endpoint dans `metadata` ni dans un sink externe (Sentry).
5. **RGPD** : tout nouveau modèle cascade sur `User` delete ; free-text passe `safeFreeText`.
6. **Data model = SSOT** : pas de donnée dérivée stockée (profil mindset, stats debrief = calculés au render). `.strict()` sur tout schema.
7. **Mobile-first + dark-only + a11y WCAG 2.2 AA** ; DS-v2 tokens (jamais de hex/`var()` magiques hors convention).
8. **§18.4** : 1 session = 1 jalon = 1 PR atomique revertable.

---

## 10. Definition of Done §29 — vérification réelle

| Critère §29                                                                  | Statut                                          | Preuve                                                                                                                                                                                                                                                                              |
| ---------------------------------------------------------------------------- | ----------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Le dépôt démarre, build vert, 0 warning critique                             | ✅ **VÉRIFIÉ EN RÉEL (local + CI)**             | **Local 2026-06-05** (`pnpm install --frozen-lockfile` + `prisma generate`) : `type-check` EXIT 0 ✓ · `lint` EXIT 0 ✓ · **Vitest 1762/1762** EXIT 0 ✓ · **build Turbopack** EXIT 0 ✓ [tool-output]. **CI** : CI+Deploy+E2E SUCCESS sur `5dce847` [gh].                              |
| Le modèle de données couvre **100 %** des besoins sessions 2-9               | ✅ **VÉRIFIÉ champ par champ**                  | §4 — 34 modèles mappés sur 8 domaines 2-9, schéma lu intégralement [`schema.prisma`, 1914 l].                                                                                                                                                                                       |
| Le moteur Claude Opus 4.8 local répond en full performance sur un appel réel | ✅ **VÉRIFIÉ (e2e documenté + logique testée)** | Calendar validé e2e DEV : Opus 4.8 → calendrier §2-clean persisté tous gates, $0 (PR #233, MEMORY sess.21). Logique batch (`loadAllSnapshots`/`persist` + gates) couverte par les `batch.test.ts` du **1762 vert** [tool-output]. Infra batch prod activée (token + health ok/401). |
| Le plan maître est sans zone d'ombre, validé contre le CONTEXTE GLOBAL       | ✅ **FAIT**                                     | §7 (9 sessions ↔ domaines réels) + §9 (invariants §2/§21.5/IA-$0) + §1 (réalité datée).                                                                                                                                                                                             |

> ⚠️ Tableau historique (plan-9, snapshot 2026-06-05). **La DoD canonique du plan-10 est §13.2** (4/4 PASS depuis MAJ-24 : 46 models incl. S3, moteur central — ré-épinglé **Opus 4.8** en MAJ-25).

---

## 11. Backlog consolidé « 100× plus loin » + dettes connues

- **S2 re-audit MAJ-25 — résiduels confirmés non-bloquants (2026-06-11)** : (a) **PreTradeCheck invisible des analyses** — zéro surface admin et absent des snapshots Claude/scoring ; ajouter un compteur count-only nommé (pattern #254) + exposition admin (TIER2, candidat jalon court) ; (b) **onglet admin « Check-ins » mort** — lien rendu mais `parseTab` le rejette, aucune surface admin granulaire DailyCheckin/HabitLog (TIER2 → entrée d'audit naturelle de la S7 Espace Admin) ; (c) `Trade.processComplete` capturé/scoré mais affiché nulle part par-trade (TIER3) ; (d) Trade déclaré sans attribution `BrokerAccount` alors que le monde S3 est account-scoped — ambiguïté de réconciliation à trancher au jalon S3 ; (e) ~~`lib/triggers` ne consomme pas `emotionDuring`~~ **FERMÉ MAJ-28 (#303)** : consommé par `engine.ts`/`evaluators.ts evalEmotionLogged` (axe « pendant » désormais dispatché — D4-03 doc-fix MAJ-29) ; (f) formatters du journal non TZ-pinnés (rendu heure serveur, TIER3) ; (g) **données `planRespected` pré-2026-06-11 non fiables** (inversion TIER1 fixée #294 — les « Non » historiques sont stockés `true`, non réparables ; dater toute analyse longitudinale de l'axe au fix) ; (h) e2e Playwright du wizard 30Q (la couverture pilotée est RTL #294 ; l'e2e reste page-load + invariants DB) ; (i) durcissements bas-bruit : fixture bash pour `core_parse_response`, token batch en argv curl (machine mono-opérateur), exiger un offset TZ explicite sur `exitedAt` wire.

- **CI** : runs `Dependabot hono` en failure (dep dev-only via `@prisma/dev`) → triage override ou pin. _(Candidat jalon séparé.)_
- **Doc-hygiène** : `README.md` (en-tête status) figé à un état antérieur — la vérité courante vit dans `apps/web/CLAUDE.md` ; numérotation SPEC §26(changelog)/§30(réunions, absent)/§31(calendrier) prête à confondre → harmoniser les labels de jalon « §26 » un jour.
- **ADR-005 Calendrier → Accepted** : nécessite 1ᵉʳ run batch réel + validation Eliot §2 sur 5+ calendriers (0 avis marché).
- **Session 5 (Guidage quotidien) — décisions/actions Eliot restantes** (audit MAJ-15 ; le jalon guidage est LIVE, ces 4 points sont hors-scope du jalon unique §18.4) :
  - **(a) DoD#1 « testé runtime » — PROUVÉ** (MAJ-16) : le pipeline questionnaire→`claude --print`→calendrier est désormais prouvé end-to-end au runtime réel (vrai Opus génère un calendrier adapté aux contraintes, persisté via tous les gates §2/AMF). Reste uniquement l'**activation prod** = lancer `ops/scripts/calendar-batch-local.sh` contre les vrais membres prod (action Eliot depuis son PC, ban-risk §5.4 human-in-the-loop ; token prod). Tant que non lancé, un membre prod voit « ton calendrier se prépare ».
  - **(b) DoD#2 rapport hebdo MEMBRE** : le weekly est **admin-only par SPEC** (digest Eliot ; member-weekly différé V2 SPEC:624/639/811). À trancher : V1 maintenant (route `/rapport-hebdo` + reader carbone monthly + nouveau push `weekly_member_recap_ready`) ou rester V2 ? **Le défaut légal adjacent est CORRIGÉ** (MAJ-16, PR #275 `3f78fbb` LIVE) : `legal/ai-disclosure` ne ment plus (« seul Eliot reçoit » le weekly, membre = mensuel). Reste juste la **décision produit** member-weekly V1 vs V2 (indépendante du fix légal).
  - **(c) DoD#4 garde-fou permanence (trou A5-4) — ✅ FERMÉ (MAJ-20, PR #282 `f73196f` LIVE)** : cron serveur `calendar-overdue-alert` (daily 11:00 UTC) construit + **provisionné + runtime-testé prod** (HTTP 200 + heartbeat audit count-only). Scan read-only (questionnaire rempli >18h & calendrier absent) → email admin `WEEKLY_REPORT_RECIPIENT` count-only ; détection-only (ne génère JAMAIS, batch reste manuel ban-risk §5.4) ; allowlist `fxmily-cron` + monitoré `health.ts` EXPECTATIONS. Reste seulement l'action Eliot : lancer le 1er batch réel quand un membre aura rempli le questionnaire (prod : 0 questionnaire aujourd'hui).
  - **(d) Calendrier — édition de blocs membre** (audit A1-03, TIER3) : aujourd'hui le membre ajuste son calendrier en **re-remplissant** le questionnaire (régénération). Pas d'édition/déplacement de bloc fin. Décision produit : documenter ce choix (défendable, calme) ou construire l'édition de bloc (server action + re-validation Zod + re-persist).
- **Profilage IA** : smoke `onboarding-batch --dry-run` sur data réelle (taux de refus Opus 4.8 à observer).
- **Profilage IA — dettes** : (a) `questionText=''` → ✅ **RÉSOLU PR #240 (`cdbf0f7`, 2026-06-06)** — peuplé depuis `instrument-v1` au write (`appendAnswer`). (d) prompt « Instrument: **vv1** » (double `v`) → ✅ **RÉSOLU PR #203 (`fb9587d`, 2026-06-06)**. (b) `env.ANTHROPIC_MODEL` allowlist exclut `claude-opus-4-8` ([`env.ts:89`]) → ⏳ ouvert : touche l'invariant §9 (API payante) + exige les vrais tarifs Opus dans la pricing table ; le path bash prod ($0 Opus 4.8) est correct, donc §28 respecté en pratique. (c) re-profilage périodique (one-shot `@unique userId`) → ⏳ ouvert (`/spec`).
- **DS — contraste `--acc` sur fond `acc-dim`** (a11y WCAG 1.4.3, **app-wide ~44 fichiers**, pas une régression) : eyebrow/CTA `text-[var(--acc)]` = 3.15–3.23:1 < 4.5:1 ; fix = `--acc`→`--acc-hi` ([`globals.css:92`], « text-accent ») sur les eyebrows/CTA posés sur `acc-dim`. **Jalon DS dédié** (ne pas fixer un widget isolé → incohérence avec les voisins). UI polish lié : icône état `pending` du widget profil → `--t-3` (réserver l'accent au `ready`).
- **Tracking — axes §28 manquants (M8, « la seule directive restante » SPEC:1382, chacun un `/spec` + build dédié §18.4)** : (1) **suivi-formation** (placeholder UI seul [`dashboard/page.tsx`], jalon #4 séquence §21.6) ; (2) **analyse-marché déclarée** (booléen « as-tu fait ton analyse ? » conforme §2, 0 contenu) ; (3) **présence réunions §30** (PR #206 `/spec` en attente) ; (4) **émotion PENDANT le trade** (`Trade` n'a que `emotionBefore`/`emotionAfter`).
- **Session 2 — VRAI 100% (2026-06-06, `origin/main`=`dc438fe`)** : tous les axes de tracking §28 LIVE prod. Réunions §30 J-M1→J-M4 (#207/#243/#244/#245) + **cron wiré prod** (10 réunions générées en base, recurring `0 6 * * 1-5`). Scoring DoD#3 (#247) consomme marketAnalysisDone/émotions-trade/sommeil (additif null-skip). suivi-formation (#248) `formationFollowed` evening + engagement. Les 6 PRs backlog (#200/#201/#202/#204/#205/#206) landées (MAJ-2). **DoD §29 satisfait** : inscription→profil LIVE (CI Playwright vert), tous axes persistés (migrations prod), consommables S3-S6 (schéma+scoring), 0 QCM cassé (CI vert chaque PR). **Backlog produit FUTUR (hors S2)** : re-profilage périodique (`@unique userId` one-shot) ; DS `--acc`→`--acc-hi` contraste app-wide ; ADR-005 calendrier flip (1er batch réel) ; drift crontab prod `mindset-check-reminders` (repo a la ligne, prod ne l'a pas — à scp un jour) ; outbox exactly-once rapports IA.
- **Rapports IA** : outbox exactly-once (V2, touche weekly+monthly) ; observabilité coût.
- **Pré-trade** : extension corrélation aux 5 piliers (différenciateur Fxmily).
- **Track Record public** : drop des tables `PublicTrade*` dépréciées (runbook §21) une fois la sync standalone confirmée.
- **Sécurité infra** (Eliot, SSH) : firewall Hetzner → plages Cloudflare + SSH key-only + fail2ban (le scrub HEAD ne corrige pas l'historique git public).
- **V2 multi-admin** : `TradeAnnotation`/`AdminNote`/`TrainingAnnotation` → `onDelete: SetNull` + `adminId` nullable.
- **Session 3 (Espace membre) — follow-ups à trancher/jalonner** (audit 2026-06-07, branche `feat/session3-espace-membre`) :
  - **DÉCISION Eliot — débrief hebdo MEMBRE** : le brief §28 le demande, mais SPEC.md le diffère explicitement en V2 (`:624`/`:639`/`:811`). Soit assumer « mensuel seul côté membre » (mettre à jour le brief/DoD), soit le construire — jalon dédié (route `/rapport-hebdo` + reader carbone du `monthly-debrief-reader` + **nouveau** type push `weekly_member_recap_ready` car `weekly_report_ready` a un copy/deep-link ADMIN, AX3-F3). **NE PAS exposer le rapport admin brut au membre** (posture/cadrage).
  - **AX3-F2 alerte de dérive « immédiate » — CONSTRUITE sur la branche (durcissement #6)** : `enqueueDouglasDeliveryNotification` (carbone monthly) + appel best-effort dans `engine.ts` après chaque delivery + **push-only** (`douglas_card_delivered` ∈ `EMAIL_FALLBACK_SKIP_TYPES`). Émission unit-testée (+3 tests) ; réception déjà J9-prouvée. **Outward-facing** (les membres recevront un push calme ≤1/j sur dérive) → **sign-off Eliot avant le MERGE** (la barrière prod). NB : l'e2e trigger→push complet (seed cartes + loss-streak) n'est pas couvert localement — proportionné : l'émission est unit-testée et l'intégration confirmée par le build.
  - **AX2-01 — LIVRÉ (durcissement #7)** : graphe « scores comportementaux dans le temps » construit + vérifié (loader + `ScoreTrendChart`). Reste possible un jour : sélecteur de plage (7j/30j/90j) sur ce graphe (aujourd'hui fixé 90j).
  - **RGPD purge de compte** : `purgeMaterialisedDeletions` (`account/deletion.ts`) cascade les lignes mais **ne balaie pas le storage** (captures, médias annotations, training) → jalon dédié `collectUserMediaKeys(userId)` + sweep avant `user.delete` + test (l'analogue per-trade `deleteTrade` est fait dans cette session).
  - **Polish TIER3** (cosmétique, 1 PR groupable) : résidu lime hue-130 en glow décoratif (15 sites/10 fichiers wizards journal/checkin/pre-trade — DS-v3 = bleu) ; `EmotionPerfTable` sans `overflow-x-auto` à 375px ; `LogExpressFab` z-40 peut recouvrir le footer légal sur pages courtes ; `score-gauge.tsx`/`track-hero.tsx` SVG natif `stroke=var()` (risque WebView <iOS15.4, hors cibles) ; `session-perf-bars` sans garde `insufficient_data` ; code mort `continueToClose`/`WIZARD_STEPS[6]` + champ `dd` non tracé dans `track-record-chart-impl`.
  - **Produit (à confirmer)** : « photos » d'analyse pluriel (§29) = 1 seule capture entrée/sortie aujourd'hui (table `TradeMedia` 1-N si multi-captures voulu) ; mot personnel admin libre dans le débrief mensuel (champ `adminNote` nullable visible membre, distinct de `AdminNote` privé) si désiré.

---

## 12. Hand-off — comment reprendre (§27)

**Au démarrage de toute session 2-9 :**

1. Lire **ce fichier** (état + plan maître + invariants §9) + le §domaine ciblé (§7).
2. Lire `SPEC.md §<N>` (intention produit) + la section `apps/web/CLAUDE.md` du jalon.
3. Vérifier l'état réel **avant d'agir** : `git log origin/main`, `gh run list` (CI verte ?), WebFetch `/api/health` (prod ok ?), Read des fichiers concernés.
4. Respecter §18.4 (1 session = 1 jalon) + tous les invariants §9.

**À la fin de toute session :** mettre à jour le §domaine (statut, ce qui a été poussé) + §11 (backlog) + §1 (snapshot daté si l'état global a bougé) ; écrire le détail atomique dans un topic-file MEMORY ; `/clear`.

**Session 1 — hand-off** : Fondations **consolidées** (non reconstruites — choix validé par Eliot). `PROJECT_STATE.md` créé comme fichier d'état canonique. Stack/data-model/archi/moteur-Claude documentés sur le réel + vérifiés (CI verte, prod ok/ok/ok). Les 8 domaines des sessions 2-9 sont **tous LIVE prod** ; leur travail futur = audit profond + durcissement + « 100× plus loin », jamais reconstruction.

**Session 2 — hand-off (Onboarding & Profilage & Tracking)** : audit profond du cœur d'absorption de données via **4 researchers parallèles** (onboarding / profilage IA / tracking quotidien / QCM+axes). Verdict : cœur **solide et LIVE** (signup race-safe, pipeline profilage 6-gates posture §2 tenue, tracking idempotent TZ-robuste, QCM mindset défensif). **Défaut #1 trouvé + corrigé** (confirmé par 2 audits indépendants) : le pipeline de profilage (entretien → Opus 4.8 → `MemberProfile` → `/profile`) était **construit mais orphelin** — `/dashboard` n'avait **0 lien** vers `/onboarding/interview` ni `/profile`, donc 0 % des membres atteignaient le « profilage initial » (§28) → §29 « profil » non tenu. **Jalon livré** : `ProfileStatusWidget` sur `/dashboard` (carbone `CalendarStatusWidget`, 4 états, frontend-only, 0 migration). Gate vert (type-check/lint/Vitest 1762/build) + e2e réel 3/3 + screenshots iPhone SE/15 + **4 audits Opus 0 TIER1** (ui + a11y SHIP-READY + code-reviewer + verifier 7/7). Backlog Session 2 → §11 (dette `questionText`, allowlist `opus-4-8`, contraste DS `--acc`→`--acc-hi` app-wide, axes M8 manquants).

**Session 2 — FINALISÉE (3 jalons LIVE prod)** : (1) **PR #235** widget profil **MERGÉ `d43ae2b`** + deploy SUCCESS + prod ok/ok/ok → le profilage est découvrable. (2) **PR #236** **émotion PENDANT le trade** (§22 « avant/pendant/après ») **MERGÉ `929cfdc`** — `Trade.emotionDuring`, migration additive **appliquée prod**, gate vert (Vitest **1768**), e2e réel **4/4** (full form submit), **4 audits Opus** (verifier 7/7). (3) **PR #238** **analyse-marché déclarée** (§28/§22) **MERGÉ `1bd66b9`** — `DailyCheckin.marketAnalysisDone Boolean?` (« as-tu préparé ton analyse de marché ? » au check-in matin ; posture §2 = le FAIT booléen, jamais le contenu), byte-mirror de `morningRoutineCompleted`, migration `20260605170000` **appliquée prod**, gate vert (Vitest **1770**), e2e réel **2/2** (ligne DB `marketAnalysisDone=true` asserted), **5 audits Opus 0 TIER1** (verifier 8/8). **Accounting honnête §29.2** : tous les axes buildables-seul désormais LIVE (trade avant/pendant/après, checkin + analyse-marché, habit, pre-trade, mindset, training, REFLECT, onboarding→profil) ; **réunions §30 EN CONSTRUCTION** (#206 spec + #207 data + #208 UI, non mergées) ; **suivi-formation déféré** (`docs/defer-suivi-formation-jalon4`). **CANON** : survoler `gh pr list` + `git branch -a` AVANT d'auditer « ce qui manque ». **CANON e2e** : ajouter un champ REQUIS à un wizard partagé casse TOUS les specs qui le pilotent → grep `tests/e2e` avant le 1er push (miss `smoke-tour.spec.ts` rattrapé par CI sur #238).

**Session 2 — durcissement + backlog (2026-06-06)** : re-vérif hardcore → S2 **PAS 100%** (mon « finalisée » précédent sous-pondérait le backlog PR — canon `gh pr list` réaffirmé). **Déployés prod (ok/ok/ok)** : #240 (`questionText`←instrument + JSDoc skip-fix + `tradeCloseSchema.strict()`, `cdbf0f7`) + #203 (« vv1 »→« v1 », `fb9587d`), gate vert Vitest **1770**. **Reste pour un vrai 100% S2** (cf. §11) : lander #200/#202/#205 (CLEAN, flux `strict:true`) ; rebaser+lander #201/#204/#206 ; **construire réunions §30 J-M3 (admin) + J-M4 (engagement) puis merger #207/#208 → ferme DoD#2** ; faire le scoring consommer les axes ignorés (DoD#3) ; `/spec` suivi-formation. Détail + conflict-map → mémoire `D--Fxmily/fxmily_session_2026-06-06_s2_hardening_pr_backlog.md`.

**Session 2 — FINALISÉE vrai 100% (2026-06-06, `origin/main`=`dc438fe`)** : après challenge Eliot (« es-tu sûr d'avoir fini ? » + vrai brief `02-SESSION-Onboarding-Tracking.md` DoD §29), tous les axes restants livrés + LIVE prod, non-stop, autonomie totale. **(A) Cron réunions wiré prod** (SSH root `hetzner-dieu` : backup `/root/fxmily-cron-bak`, allowlist wrapper `+generate-meetings` (bash -n OK), crontab `0 6 * * 1-5`, seed `runuser fxmily` → 10 réunions 06-08→06-12 en base, idempotent re-run=10). **(B) Scoring DoD#3** #247 `85492d9` : marketAnalysisDone→discipline W10, tradeEmotionFootprint→emotional-stability W15 (mirror negativeEmotionRate, jamais P&L), sleepQuality→engagement W10 ; tous additifs null-skip, byte-identique prouvé (68/100/63) ; deploy 1er run échec TRANSITOIRE (Docker Hub registry timeout au buildx, PAS le code) → `gh run rerun --failed` → SUCCESS. **(C) suivi-formation** #248 `dc438fe` : `DailyCheckin.formationFollowed` evening (migration `20260606120000` ADD COLUMN appliquée prod) + wizard toggle optional + engagement W10 additif ; nommé « formation » (≠ §21 Entraînement, firewall clean). CI Playwright a cassé (toggle a poussé le submit evening sous la bannière cookie `fixed bottom`) → fix e2e (dismiss bannière dans smoke-tour, mirror user) → PASS. **CANONS NOUVEAUX** : (1) **crontab serveur = scp MANUEL** (`/etc/cron.d/fxmily-app` via setup-host.sh:99, hors deploy.yml) — tout nouveau cron route exige re-scp + ajout allowlist wrapper. (2) deploy échec « Docker Hub registry timeout au Set up Docker Buildx » = TRANSITOIRE → `gh run rerun --failed`, pas un bug code. (3) ajouter un champ au wizard partagé peut pousser un bouton sous la bannière cookie fixed-bottom → dismiss la bannière (localStorage `fxmily.cookie.dismissed`, bouton « J'ai compris ») dans les e2e parcours. (4) PR stacked dont la base merge → enfant auto-CLOSED → recréer par cherry-pick. (5) résolution conflit = merge-IN (pas rebase force-push) + script déterministe pour docs/schema. **Méthode** : builds délégués à des sub-agents (map researcher) puis re-vérif main-loop (gate indép + audits + verifier) — protège le contexte sur une session énorme (13 PRs landées). **Session 3** (Espace membre, brief `03-SESSION-Espace-Membre.md`) = prochaine, audit+durcissement (PAS reconstruction), `/clear` d'abord.

**Session 2 — réunions §30 J-M1→J-M4 LIVE (2026-06-06, `origin/main`=`3689756`)** : DoD#2 (axe présence réunions) **code-complet + déployé prod ok/ok/ok**. 4 PRs sérialisées : #207 J-M1 (data layer ; conflit schema.prisma+runbook résolu par `--theirs` main + splice net-new ; migration `20260530150000` appliquée prod [« All migrations successfully applied » 13:17Z]) → #243 J-M2 (member `/reunions` ; recréé après auto-close de #208 par cherry-pick sur main) → #244 J-M3 (admin : cron `generate-meetings` carbone recompute-scores idempotent `@@unique(date,slot)` + `/admin/reunions` cancel/uncancel + `?tab=presence` reuse `admin.member.viewed` + slugs `meeting.generated`/`admin.meeting.cancelled` + crontab `0 6 * * 1-5`) → #245 J-M4 (engagement : `WEIGHT_MEETING=15` ADDITIF ; `rateSubScore(completed,scheduled)` skip si scheduledCount===0 → byte-identique 63==63 ; scheduled>0 & completed=0 → 54 ; 4/4 → 68 ; firewall anti-leak green). **Méthode** : J-M3/J-M4 buildés par sub-agents (map researcher) puis re-vérifiés (gate indép + 2 audits 0 TIER1 security+code-review + verifier adversarial avec re-impl math) + CI + deploy + health par jalon. **⚠️ OPS RESTANT (rend l'axe FONCTIONNEL)** : le cron route est LIVE mais le crontab serveur n'est PAS auto-déployé — `scp ops/cron/crontab.fxmily fxmily@<prod>:/etc/cron.d/fxmily-app` (chmod 644) + reload, sinon aucune réunion générée → `/reunions` vide. **CANON** : crontab serveur = provisioning manuel (setup-host.sh:99), hors deploy.yml ; tout nouveau cron route exige de re-scp le crontab. **PICKUP reste S2** : (1) installer le crontab (SSH) ; (2) **scoring DoD#3** (consommer marketAnalysisDone/émotions/sommeil en sous-scores additifs null-skip, pattern J-M4) ; (3) **`/spec` suivi-formation** (interactif Eliot) ; (4) vérif DoD §29 e2e (signup→profil + tous axes persistés en base + 0 QCM cassé).

**Session 2 — campagne PR backlog landée (2026-06-06, `origin/main`=`eb25d85`)** : **6 PRs du backlog landées + déployées prod ok/ok/ok**, flux `strict:true` sérialisé (update-branch BEHIND→CI verte→squash→watch deploy→health après CHAQUE ; overlap CI/deploy canon sess.20 ; **jamais `--admin` sur du code**). Pre-merge review (4 sub-agents parallèles) = **4/4 SHIP 0 blocker**. Ordre : #200 `a7aa16e` (tests onboarding-batch) → #202 `868ea32` (a11y) → #205 `9f959d5` (e2e de-flake) → #206 `68409b1` (spec §30, `--admin` docs-pur, 0 deploy ; conflit SPEC.md résolu par script : §30 réunions + §31 Calendrier préservé + §32 Changelog renuméroté + Fin v1.7) → #204 `ac25b90` (**JWT tokenVersion revocation, sécu** ; migration `20260529150000` **appliquée prod** [log « All migrations successfully applied » 08:42:01Z] ; conflit runbook résolu : §24 calendrier préservé + §25 J4 renuméroté) → #201 `eb25d85` (validate answers vs instrument ; conflit code #240 résolu — #201 **subsume** #240 : résout+valide `item` du catalogue puis utilise `item.id`/`item.text`, closes aussi la dette `questionText` vide ; dédup import). **CANON résolution conflit** : merge main IN (pas rebase force-push) → résoudre → push normal (0 force-push) ; doc-conflits = script déterministe (0 transcription manuelle) ; code-conflit validé par **CI réelle** (Vitest+tsc) avant merge. **PICKUP next jalon (ferme DoD#2)** : `/clear` → « Construis réunions §30 J-M3 (admin) puis J-M4 (engagement) ; rebase #207/#208 ; merge le stack » — SPEC §30.8 (désormais sur main) découpe J-M1→J-M4 **jamais bundlés** (§18.4) ; `model Meeting`+`MeetingAttendance` migration **additive** (`prisma-migration-runner`) ; helper DST `localInstantToUtc` (`lib/weekly-report/week-window.ts:138`, PROUVÉ 8/8). Puis **scoring DoD#3** (consommer `marketAnalysisDone`/émotions trade/sommeil, pondération neutre & sûre) · **`/spec` suivi-formation** (interactif Eliot) · **Session 3** audit Espace membre (durcissement, PAS reconstruction).

---

## 13. Plan maître « 10 sessions » — réconciliation du nouveau jeu de prompts Downloads (Session 1, 2026-06-09)

> Source : `C:\Users\eliot\Downloads\fxmily app suivie sessions prompt\` (10 fichiers `0N-SESSION-*.md`). Ce jeu **remplace** l'ancien plan « 9 sessions » (§7) en **insérant une S3 neuve** et en décalant tout de +1. Le §7 reste valide comme décomposition par DOMAINE ; cette §13 est le **mapping canonique session-brief → réalité** pour le plan à 10.
>
> **MAJ-24 (2026-06-10)** : les prompts vivent désormais dans `C:\Users\eliot\Documents\FXmily-Suivi-PROMPTS-Fable5\` (édition **Fable 5** : même structure 10 sessions, le modèle imposé passe d'Opus 4.8 à **`claude-fable-5`** avec anti-bascule). Les décisions 13.4 #1/#2/#3 sont **TRANCHÉES** (SPEC v1.8 §33 + PRs #289/#290/#291) — voir les annotations ci-dessous.

### 13.1 Mapping & statuts (vérifié first-hand)

| Plan-10 (Downloads)                                                     | Ex plan-9 | Statut réel                                                                                                                                            | Preuve / MAJ                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| ----------------------------------------------------------------------- | --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **S1 Fondations**                                                       | 1         | ✅ **DONE+LIVE (consolidé + clôturé MAJ-24)**                                                                                                          | ce fichier · DoD 13.2 **4/4 PASS** (#2 via #289/#290, #3 via #291)                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| **S2 Onboarding · Profilage · Tracking**                                | 2         | ✅ DONE+LIVE — **re-auditée plan Fable-5 + fermée (MAJ-25)**                                                                                           | MAJ-1→9 + MAJ-25 · 12/12 axes · 2 TIER1 réels fixés (#294) · filet permanence (#295) · vitest 2249                                                                                                                                                                                                                                                                                                                                                                                                                  |
| **S3 Vérification & Honnêteté radicale (preuve MT5 + score constance)** | —         | ✅ **DONE+LIVE (MAJ-26)** — preuves + vision + réconciliation + constance + alertes                                                                    | MAJ-26 · PRs #297/#298/#299 · DoD §31 5/5 runtime-prouvés · cycle adverse 0 TIER1 résiduel                                                                                                                                                                                                                                                                                                                                                                                                                          |
| **S4 Espace Membre**                                                    | 3         | ✅ DONE+LIVE — **re-auditée plan Fable-5 + fermée (MAJ-27)**                                                                                           | MAJ-10/11 + MAJ-27 · PR #301 `10c58a9` · DoD §31 4/4 (e2e réels) · sorties S3 branchées au hub                                                                                                                                                                                                                                                                                                                                                                                                                      |
| **S5 Coaching Psychologie + moteur analyses**                           | 4         | ✅ DONE+LIVE — **14 challenges, DoD §30 4/4 re-MET (MAJ-33)**                                                                                          | MAJ-12/13/14 + MAJ-28→33 · gate §2 instrument-anchored durci (#312 `55c013c` + #313 `359cd25`) · 53 fiches · vitest 2581                                                                                                                                                                                                                                                                                                                                                                                            |
| **S6 Fonctionnalités avancées (calendrier, automatisation, rapports)**  | 5         | ✅ **DONE+LIVE — auditée-as-brief + DOD3-01 fermé (MAJ-34)**                                                                                           | MAJ-15→22 + MAJ-34 · DoD §31 4/4 (compteurs S3 câblés weekly+monthly, PR #314 `bfb9807`) · vitest 2592 · reste = 1ᵉʳˢ batchs réels Eliot                                                                                                                                                                                                                                                                                                                                                                            |
| **S7 Espace Admin**                                                     | 6         | 🟢 DONE + LIVE (MAJ-35, PR #316 `350a300`)                                                                                                             | audit 18-agents · 6 fixes+2 panels+processComplete · e2e 6/6 + 6 reviews · deploy SUCCESS, health ok/ok/ok                                                                                                                                                                                                                                                                                                                                                                                                          |
| **S8 Mode Entraînement (backtest TradingView)**                         | 7         | 🟢 **DONE + LIVE — conteneur session câblé (DoD#1) + tour-2 (MAJ-36, PR #317 `a661b8a` + #318 `8f4ae7a`)**                                             | §7 + MAJ-36 · `TrainingSession` dormant→câblé · audit 33-agents · DoD §31 4/4 · e2e 4/4 (chromium+mobile, CI) · verifier SHIP-READY · firewall §21.5 étendu · runtime-audit gate PASS · tour-2 (wording §22 + ended-guard) · health ok/ok/ok                                                                                                                                                                                                                                                                        |
| **S9 Frontend ultra-premium**                                           | 8         | 🟢 **DONE + LIVE prod — 2 re-challenges fermés (MAJ-39 responsive + MAJ-40 premium), DoD §32 4/4 prouvé runtime (PR #323 `ce068a0`, health ok/ok/ok)** | MAJ-37 (durcissement a11y) + MAJ-38 (wave wow) + **MAJ-39** (audit ÉLARGI 39 routes × 4 viewports incl. ultra-wide → §245 ambient backplate + cookie/FAB overlap + 3 overflows fermés ; DoD §32 4/4 runtime) + **MAJ-40** (élévation premium §30 : reskin admin DS-v3 5 fichiers — Pill/HoverLift/tokens/typo — + gaps hover member ; 8/8 reskin-audit + 14/14 untested-audit PASS) · CI verte + deploy SUCCESS · reste = jalon DS `@layer` + celebrate-pop dormant + shadcn modal + emails lime (déférés, non-bug) |
| **S10 Interconnexion · Validation · Livraison**                         | 9         | ⬜ **PENDING** (session finale)                                                                                                                        | aucune MAJ · suite E2E cross-session absente · **bloquée par S3**                                                                                                                                                                                                                                                                                                                                                                                                                                                   |

**Bilan (MAJ-27) : S1-S5 DONE+LIVE+auditées-éditions-Fable-5 (S4 re-fermée avec e2e réels + sorties S3 branchées au hub), S6-S9 LIVE-à-auditer-comme-brief, S10 pending.** La chaîne S3 est désormais aussi 100% opérationnelle côté ops prod (cron+token provisionnés, Cron Watch vert). Prochain jalon recommandé : **S6** (audit-as-brief `06-SESSION-…` + câblage des compteurs S3 count-only — `constancyScore`, `openDiscrepancyCount`, `alertCount` — dans les prompts weekly/monthly, point d'entrée documenté MAJ-26).

### 13.2 DoD Session 1 — état honnête (vérifié, pas « ça devrait marcher »)

| DoD §29                                                               | Verdict                                 | Preuve                                                                                                                                                                                                                                                                                                                                                |
| --------------------------------------------------------------------- | --------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| #1 Build vert, 0 warning critique                                     | ✅ PASS                                 | `type-check` EXIT 0 + `lint` EXIT 0 + `pnpm build` EXIT 0 [tool-output, worktree off `origin/main`]                                                                                                                                                                                                                                                   |
| #2 Data model couvre 100 % sessions 2→10 (incl. preuves MT5 & scores) | ✅ **PASS (MAJ-24)**                    | **46 models** : 8 entités S3 (`BrokerAccount`/`Mt5AccountProof`/`ExtractedPosition`/`Discrepancy`/`ConstancyScore` dédié/`ScoreEvent`/`Alert` + `Trade.source/verifiedAt/matchStatus`) + 11 enums + migration `20260610155331` ADD-only, **vérifié champ-par-champ vs design 13.3** (verifier frais, 0 TIER1/2) [PRs #289/#290 mergées sur `698d55f`] |
| #3 Moteur Claude **Fable 5** local répond (appel réel)                | ✅ **PASS (MAJ-24, réserve §28 levée)** | Service central RÉEL : `claude-batch-core.sh` + `lib/ai/claude-response.ts` (PR #291). Preuve runtime first-hand : `core_invoke_claude_print` (flags prod exacts) → `claude_exit=0` + JSON strict parsé + appel métadonnées `model_usage_keys: ["claude-fable-5"]` [tool-output 2026-06-10]                                                           |
| #4 Plan maître documenté, validé vs CONTEXTE GLOBAL                   | ✅ PASS                                 | cette §13 ; posture §2 (zéro conseil d'analyse) respectée dans le design S3 (vérif factuelle, pas avis marché)                                                                                                                                                                                                                                        |

### 13.3 Design data-model S3 « Vérification-Honnêteté » (prêt-à-migrer — NE PAS migrer avant décision 13.4)

> Aucune de ces entités n'existe ([tool-output] grep). Conçu pour **réutiliser** l'infra (R2 `lib/storage`, pseudonymisation §5, gates `persist` §5.3, posture §2) et **respecter les invariants** (cascade `User`, audit PII-free). À construire dans le **jalon S3 dédié** (§18.4), pas ici.

- `BrokerAccount` : `id`, `memberId`(FK User cascade), `label`, `type` enum `PROP_FIRM|PERSONAL`, `brokerName?`, `detectedByAI` Bool, `confidence` Float?, `createdAt`. + `User.detectedAccountCount` Int?.
- `Mt5AccountProof` : `id`, `memberId`, `brokerAccountId`(FK?), `fileKey`(R2), `fileHash`(SHA-256 anti-doublon/falsification), `accountType` enum, `ocrStatus` enum `PENDING|DONE|FAILED`, `uploadedAt`, `claudeRunId?`.
- `ExtractedPosition` (sortie vision Claude) : `id`, `brokerAccountId`(FK), `symbol`, `side`, `openTime`, `closeTime?`, `volume`, `entryPrice?`, `exitPrice?`, `pnl?`, `source` enum `MT5_SCREEN_OCR`, `confidence` Float?.
- `Trade.source` enum `SELF_DECLARED|MT5_VERIFIED` (**champ NEUF** — aujourd'hui tout `Trade` est implicitement déclaratif) + `Trade.verifiedAt` DateTime? + `Trade.matchStatus` enum?.
- `Discrepancy` : `id`, `memberId`, `type` enum `MISSING_DECLARED|FALSE_DECLARED|MISMATCH|UNFILLED_NO_REASON`, `declaredTradeId`(FK?), `extractedPositionId`(FK?), `severity`, `detectedAt`, `claudeReasoning`(texte, passe gate §2/AMF).
- `ConstancyScore` (**dédié, distinct de `BehavioralScore`**) : `id`, `memberId`, `value` Float 0-100, `breakdown` Json `{honesty, regularity, discipline}`, `period`, `computedAt`. + `ScoreEvent` : `id`, `memberId`, `delta`, `reason` enum `FILLED|FORGOT_NO_REASON|REALITY_GAP|FALSE_DECLARATION`, `relatedDiscrepancyId?`, `createdAt`.
- `Alert` (sortie → S5 coaching) : `id`, `memberId`, `triggerType`, `repeatCount` Int, `threshold` Int, `category`=`'PSYCHOLOGICAL'` (jamais trading), `status`, `createdAt`. **Déclenche uniquement sur RÉPÉTITION** (DoD S3 §30). Jonctions aval : `CoachingMessage.sourceAlertId?`, snapshots `WeeklyReport/MonthlyDebrief`.
- **5ᵉ pipeline Claude** (vision/OCR) : carbone des 4 existants (`pull`/`persist` + `claude --print` local) MAIS **avec image input** → **inconnu technique à valider** : le batch actuel envoie du JSON texte ; il faut prouver que `claude --print` accepte une image (chemin/attachement) en local Max. C'est un pré-requis bloquant de S3.

### 13.4 Décisions produit — RÉSERVÉES À ELIOT (à trancher avant tout code S3)

> **MAJ-24 (2026-06-10)** : **#1 TRANCHÉ** = option (a) screenshots MT5 + vision Claude, avec limites anti-survente documentées (SPEC §33.6 : un screenshot reste falsifiable ; forensique = API broker V3). **#2 TRANCHÉ** = SPEC amendée v1.8 (§33 + §13 + §14, PR #289) — la vérification par screenshot est au scope V1, l'API broker reste V3. **#3 TRANCHÉ** = `ConstancyScore` **dédié** (distinct de `BehavioralScore`, PR #290). #4/#5/#6/#7 restent ouverts.
>
> **MAJ-27 (2026-06-12)** : **#4 TRANCHÉ** (délégation explicite Eliot) = le débrief hebdo IA membre **reste V2** — SPEC gagne ; l'hebdo membre V1 = REFLECT + débrief training + QCM mindset (auto-rédigés) + fold ConstancyScore ; le mensuel reste la cadence IA member-facing. **#7 TRANCHÉ** (délégation) = photos = **1 capture entrée obligatoire + 1 sortie optionnelle** ; `TradeMedia` 1-N reste dormant (migré, 0 usage) pour un futur multi-captures V2. **#5 partiellement levé** : la chaîne S3 ops est 100% opérationnelle (cron `verification-scan` + token provisionnés MAJ-27) — restent les 1ᵉʳˢ batchs RÉELS calendrier/mensuel/vision (action Eliot, human-in-the-loop §5.4). #6 reste ouvert.

1. **Périmètre S3** (chemin critique de tout le plan) : (a) **screenshots MT5 + vision Claude** = fidèle au brief, faisable, mais **falsifiable** (ne garantit ni « nombre exact de comptes » ni « mensonge impossible ») ; (b) **rester auto-déclaration §21** (« cela viendra plus tard ») ; (c) viser une **connexion broker read-only** (MT5 investor password / API prop firm / Myfxbook) = vraie preuve, plus lourd, = roadmap V3 du SPEC. **Recommandation** : cadrer S3 comme **« renforcement intelligent de l'auto-déclaration + signal psychologique Mark Douglas »** (option a), en disant honnêtement que la preuve forensique absolue exige (c).
2. **Conflit SPEC** : `SPEC.md:616` (broker hors-scope V1) / `:645` (roadmap V3) vs S3. Règle d'or §0 : SPEC gagne. → mettre à jour SPEC pour accueillir la **vérification-par-screenshot** (≠ API broker) OU cadrer S3 dans le déclaratif. Édition SPEC = décision Eliot.
3. **`ConstancyScore`** : model dédié (confronté à la réalité) OU enrichir `BehavioralScore.consistencyScore` ? Change la migration.
4. **Débrief hebdo MEMBRE (S4)** : V1 maintenant vs rester V2 (SPEC `:624/639/811`).
5. **Actions PROD (human-in-the-loop, ban-risk §5.4)** : lancer le 1ᵉʳ batch réel **calendrier** + **mensuel** depuis le PC (prod = 2 users, 0 questionnaire, 0 calendrier, 0 débrief jamais envoyé → « LIVE » non encore exercé).
6. **Audits S6/S7/S8/S9** : les passer au cycle de re-challenge complet (comme S2-S5) OU les acquérir sur la foi du §7 ? (risque de faux « done »).
7. Mineurs : `TradeMedia` 1-N (« photos » pluriel §28) · re-profilage périodique (`/spec`) · mot perso admin dans le mensuel.

### 13.5 Ordre d'exécution recommandé (1 session = 1 jalon, §18.4)

`(0)` ✅ **FAIT (MAJ-24)** — Eliot a tranché 13.4 #1+#2+#3, SPEC v1.8 §33 mergée (#289), data-model S3 migré (#290) → `(1)` **jalon S3 (suite)** : valider l'image-input `claude --print` (pré-requis bloquant §33.4), 5ᵉ pipeline vision, moteur réconciliation, scoring constance, alertes-sur-répétition, UI membre/admin → `(2)` brancher les sorties S3 sur S4/S5/S6/S7 → `(3)` audit-as-brief S6 → S7 → S8 → S9 (un par jalon) → `(4)` **S10** validation globale + bilan. **S1-closure** : ✅ **FAIT (MAJ-24, PR #291)** — service Claude central extrait (`claude-batch-core.sh` + `lib/ai/claude-response.ts`), épinglé `claude-fable-5`.

---

_Fichier maintenu par la série de sessions structurées (plan « 9 » → réconcilié « 10 » le 2026-06-09, cf. §13). Toute affirmation y est datée et sourcée. En cas de conflit avec `SPEC.md`, SPEC.md gagne._
